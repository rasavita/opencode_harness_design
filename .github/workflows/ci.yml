name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          # Full history helps gitleaks scan commits on PRs
          fetch-depth: 0

      - uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm

      - name: Install JS deps
        run: npm ci

      - name: Lint harness JS (hooks, scripts, tests)
        run: npm run lint

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Python deps for the AST code indexer + deck-builder tests
        # Pinned to the versions the suite is validated against locally; prebuilt
        # wheels (no compiler). Without tree-sitter the code-index AST tests fail
        # (ModuleNotFoundError: tree_sitter); without python-pptx the
        # build_matrices_deck tests fail.
        run: >-
          python -m pip install
          tree-sitter==0.25.2
          tree-sitter-javascript==0.25.0
          tree-sitter-typescript==0.23.2
          tree-sitter-java==0.23.5
          tree-sitter-c-sharp==0.23.5
          tree-sitter-go==0.25.0
          python-pptx==1.0.2

      - name: Install gitleaks (Security pillar / enhanced secret scan)
        # Makes agent-readiness Security pillar "active" in CI (gitleaks on PATH).
        # Separate gitleaks job still scans history; this is for readiness + tooling parity.
        env:
          GITLEAKS_VERSION: 8.24.3
        run: |
          curl -sL "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz" \
            | tar -xz -C /tmp
          sudo mv /tmp/gitleaks /usr/local/bin/gitleaks
          gitleaks version

      - name: Unit tests (plugin/skill/hook schema, eval task specs + runner orchestration)
        run: npm test

      - name: Test-integrity proof (G43)
        # The merge-level backstop for the G42 session lock. A turn-level gate is
        # steppable — hooks can be disabled, `--no-verify` exists, an agent can
        # work outside Claude Code — so the invariant is re-proved here where no
        # in-session override reaches: no test file changed between the run that
        # made it RED and the run that made it GREEN.
        #
        # Runs NON-STRICT deliberately: the recorder that writes the ledger is not
        # yet wired in .opencode/settings.json, so there is nothing to check and
        # --strict would fail every build. Non-strict still prints a loud WARNING
        # and marks the emitted verdict `vacuous: true`, so this step cannot be
        # mistaken for a proof. ADD --strict THE MOMENT THE RECORDER IS WIRED —
        # until then this step is a placeholder, not a gate.
        run: npm run test-integrity

      - name: Agent readiness ratchet (Project Zero)
        # Hard-fails when active pillars drop below min_active_pillars or the
        # committed baseline (project-manifest.json#quality.agent_readiness).
        # PATH includes gitleaks from the previous step.
        run: |
          npm run agent-readiness
          npm run agent-readiness:assert

      - name: Package SKU smoke (harness-core)
        run: |
          npm run package:core
          test -f dist/skus/harness-core/.opencode-plugin/plugin.json
          test -f dist/skus/harness-core/skills/build/SKILL.md
          test ! -d dist/skus/harness-core/skills/pe-ic-memo

  gitleaks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Secret scan (gitleaks)
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  evals:
    # Behavioral evals drive a real model and so are gated on the ANTHROPIC_API_KEY
    # secret. The runner skips cleanly (exit 0) when the key is absent, so this job
    # is a no-op on forks and PRs without the secret — it never blocks a merge. The
    # deterministic parts (task-spec validation + runner orchestration) always run
    # in the `test` job above.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm
      - name: Install JS deps
        run: npm ci
      - name: Behavioral golden evals (model-gated)
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: npm run test:evals
