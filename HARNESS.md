# HARNESS.md — the control system, made legible

> **Agent = Model + Harness.** The model writes code; the *harness* is everything around it that makes the output trustworthy: **guides** that steer before it acts, **sensors** that detect after it acts, and a human **steering loop** that improves both over time.
>
> This file is the registry of that control system. Its machine-readable companion is [`harness-manifest.json`](harness-manifest.json) — the single source of truth for *what governs what* and *where the holes are*. The gap roadmap lives in [`docs/archive/internal/HARNESS_ENGINEERING_GAP_ANALYSIS.md`](docs/archive/internal/HARNESS_ENGINEERING_GAP_ANALYSIS.md).
>
> Framing follows Thoughtworks/Fowler *Harness Engineering for Coding Agents* and *Maintainability Sensors for Coding Agents*.
>
> **Doc map:** this file is canonical for the *control system* (gates/sensors). For architecture rationale see `design.md`; for install/usage see `README.md`; for the "which doc for what" index see `CODEBASE_MAP.md`.

> **Authority narrowing (2026-08).** `merge` is a privileged action **only when `.claude/trust/issuers.json` is provisioned**. With no approval service no capability can be issued, so gating merge offered no path to yes: it blocked every merge — including ones the repository owner had explicitly authorized — and the only way past was to switch the control off, which is strictly worse than never having gated it. `deploy`, `modify_branch_protection` and `execute_production_change` are unchanged and remain gated either way; production execution stays non-delegable and is refused before the envelope check. Provision an issuer per `docs/zero-trust-authority.md` to restore merge gating.

## Why this exists

The harness already has most of the pieces a good control system needs — they were just scattered across hooks, skills, agents, and scripts, with no one place to answer *"what is my behaviour harness?"* or *"is anything ungoverned?"*. This registry is that place. Read it to:

- see every guide and sensor organised by what it governs and when it runs;
- spot coverage holes deliberately (gaps are listed, not hidden);
- know where to wire a new sensor so it joins the loop instead of becoming shelfware.

## The model

**Two control types.** A *guide* (feedforward) prevents or steers behaviour *before* the agent acts — a skill, a spec, a config, a convention. A *sensor* (feedback) observes *after* and feeds the result back for self-correction. You need both: feedback-only repeats mistakes; feedforward-only encodes untested rules.

**Sensor implementations** are **computational** (deterministic, fast, CPU — linters, type checkers, structural rules, tests), **inferential** (LLM semantic judgement — review agents), or **hybrid**.

**Four axes of quality** (the article's three, plus one we add):

| Axis | Governs |
|---|---|
| **Maintainability** | Internal quality — keep the code easy and low-risk to change. |
| **Architecture** | Structural fitness — layering, module boundaries, performance budgets. |
| **Behaviour** | Functional correctness, verified against a *running* system. |
| **Traceability** | *(harness extension)* Deterministic grounding of every artifact against its upstream. The articles' examples rely on human review here; we hard-block it. |

## Cadence — keep quality left

Sensors are spread across the lifecycle by cost and speed. Earlier is cheaper.

```
planning ──► session ──► commit ──► integration ──► drift
(grounding) (inner loop) (pre-commit  (running app:     (repeatedly,
            on every     / gate)      evaluator / CI)   outside the
            write)                                      change cycle)
```

- **planning** — grounding/trace gates before code exists.
- **session** — fast computational sensors on every write (`verify-on-save`, `pre-write-gate`); self-correction inside one agent loop.
- **commit** — `git pre-commit` + the on-demand `/gate`; the expensive inferential reviews run here, boundary-gated.
- **integration** — the GAN evaluator runs the app: API · Playwright · vision · security · perf.
- **drift** — recurring checks *outside* the change lifecycle that catch accumulated decay. The architecture / dead-code / dependency-CVE / design-vs-code signals are live via `drift-report.js` (`npm run drift`; wire to `/schedule`, CI, or the optional `harness-drift.yml` workflow template).

## The matrix — guides × sensors, by axis

Status: ✅ active · 🟡 partial (limited/opt-in/report-only) · ⛔ planned (gap id).

### Maintainability

| | Guides (feedforward) | Sensors (feedback) |
|---|---|---|
| | `code-gen` skill (10 principles) · `clarify` | ✅ ruff/eslint (session+commit; **per-rule self-correction guidance**, G5) · ✅ mypy/tsc · ✅ length caps (30-line fn / 300-line source file / 500-line test file — a test is read one case at a time and grows table-wise, so the concentrates-change argument behind the source cap is weaker there; the function cap is deliberately NOT raised for tests, and both remain ratchets, not exemptions) · ✅ coverage ratchet + per-diff coverage · ✅ `code-reviewer` — structure lens (inferential) · ✅ coupling/dead-code report *(report-only)* · ✅ **drift: dead-code accumulation** (`drift-report.js`) · ✅ **drift: biting-meta** (commit gates never firing/blocking over run history, `loop-health.js`, report-only) · ✅ **mutation-smoke gate** (diff-scoped, /auto, `mutation-gate.js`) · ✅ **modularity review** (inferential, grounded in `modularity-pack`, manually/human-triggered via /brownfield --full or /design --delta Step D3.5, no periodic component of its own, G6; now applies Khononov's published Balanced Coupling rubric — Integration Strength × Distance × Volatility, flags only high-strength+far-distance+high-volatility, G27) · ✅ **drift: modularity-review staleness** (unstable hubs new since the last real review vs `.claude/state/modularity-review-marker.json`, `drift-report.js`, G19) · ✅ **control-budget ratchet** (`control-budget-gate.js`: the count of registered controls may only stay flat or drop unless each new control carries a `net_add_justification` — a ratchet on the harness's OWN complexity, the counter-force to accretion; see *The control budget* below) · ✅ **sensor-value-meter** (`sensor-value-report.js`, report-only: turns `recordOutcome`'s `sensor-outcomes.jsonl` into a ranked never-fired/never-blocked cut list for `/retro`) |

### Architecture

| | Guides | Sensors |
|---|---|---|
| | `architecture.md` · `project-manifest.json#architecture` (layer config) · ✅ **observability conventions** (RED metrics + /metrics scaffolded into generated server apps, G9) · ✅ **topology-templates** (per-topology manifest-knob presets, G10) · ✅ **architecture constitution** (cross-sprint invariants, sprint-delta lane) | ✅ layered-import check (every write) — *horizontal only* · ✅ API schema validation · ✅ perf ratchet (p95) · ✅ **drift: new cycles / unstable hubs** (`drift-report.js`) · ✅ **drift: design-vs-code** (Canvas `Governs` vs disk, G4) · ✅ **vertical bounded-context rules** (`contexts.js`, opt-in, G8) · ✅ **import-cycle ratchet** (`cycle-gate.js`, G8) · ✅ **unstable-hub ratchet** (`coupling-gate.js`, commit-time, /auto Gate 4 + /gate, G18) · ✅ **API contract-drift** (`oasdiff` breaking-change gate, /gate when the OpenAPI spec changes, G12) |

### Behaviour

| | Guides | Sensors |
|---|---|---|
| | BRD/spec/design + acceptance criteria + sprint contracts · legacy-preservation skills · ✅ **REASONS Canvas** (living artifact + `Governs`, G4) · ✅ **first-window init split** (`/auto` SECTION 2, G13) · ✅ **pr-respond** (bounded post-PR CI/review response loop, opt-in via `--respond`; deterministic poller `pr-poll.js`; never merges) · ✅ **AT-first acceptance tests** (business-readable Given/When/Then against a Ports-and-Adapters seam + test-double adapter, written before implementation, primary per-story verification loop ahead of E2E, G20 — now backed by a mechanical commit-time gate, G23) · ✅ **canary-first mechanical rollout** (/refactor + upgrading-dependencies: trial a large mechanical fix on ~3 files before extending it to the full affected set, G32) · ✅ **tiered dual adversarial code review** (Bun Phase A / 2.2.0: `review-tier.js` + two independent `code-reviewer` instances + `merge-review-verdicts.js` union policy; auto on size/security/`strict`) · ✅ **process-rules.md** + **git-safety** parallel deny · ✅ **no stub-to-green** (code-gen + code-reviewer + `stub-smell-gate`) · ✅ **diagnostics work queue** (Phase B / 2.3.0: `fix-from-diagnostics` + `diagnostics-shard.js`) · ✅ **cyclic-dependency pre-pass** (`fix-from-diagnostics` Step 2: break a known import cycle before sharding when ≥3 packages are error-dense on it, G33) · ✅ **canary on implement/feature** + **`/refactor --mechanical`** migrate templates · ✅ **semantic-divergence checklist** + **review-commit-msg** + **`/fix-diagnostics` workflow** (Phase C / 2.4.0) · ✅ **boundary-test-doubles** (shipped record/replay DB/HTTP/LLM kit + AT template, bound under `HARNESS_TEST_REPLAY=1`, G34) | ✅ unit tests · ✅ evaluator Layer 1 API · ✅ evaluator Layer 2 Playwright · ✅ evaluator Layer 3 vision · ✅ `code-reviewer` — correctness lens (same agent as the maintainability row) · ✅ `security-reviewer` (OWASP) · ✅ secret scan (baseline regex, pre-write + commit; gitleaks tier at /gate) · ✅ SAST (semgrep, /gate) · ✅ dep-audit (npm/pip, /gate) · ✅ **drift: new dependency CVEs** (`drift-report.js`) · ✅ **resume smoke check** (boots app on fresh-process resume before building, G14) · ✅ **runtime-SLO** (5xx error-rate vs SLO, scrapes product /metrics, G9) · ✅ **axe/WCAG accessibility** (default-on for UI stories, Full FAIL / Lean WARN, G12) · ✅ **approved-fixtures** (snapshot-oracle lock, /gate, G12) · ✅ **flake detection** (N× re-run, drift cadence, G12) · ✅ **regression-suite-full** (accumulated e2e/ Playwright + prior sprint-contract API re-check, full unabridged sweep at /gate + /auto pre-merge, G15) · ✅ **impact-scoped-regression** (deterministic TIA over code-graph.json selects only the affected prior group's e2e/contract checks + golden_paths, fast local complement at /change + /vibe every iteration, G16) · ✅ **legacy-discipline-proof** (mechanical proof the checking-coverage-before-change/pinning-down-behavior/sprouting-instead-of-editing discipline actually ran — no receipt or an unproven UNCOVERED edit BLOCKs at pre-commit, G17) · ✅ **coverage-preflight** (real-time PreToolUse block on an Edit/MultiEdit touching an UNCOVERED symbol range; predates this session, found unregistered during the G17 review and registered here) · ✅ **at-first-proof** (mechanical proof the writing-acceptance-tests-first discipline actually ran — a story with NEW production files staged must have both its `specs/test_artefacts/acceptance/{story-id}.*` file and a matching `record-at-red.js` red receipt, or pre-commit BLOCKs, G23) · ✅ **test-deletion-guard** (blocks a staged commit that deletes a test with live cases, decreases a test file's live test count, or newly skips a test — a refactor/dependency-bump must not make its own suite pass by removing the test that catches the regression, G31) · ✅ **evidence-integrity** (cross-checks the evaluator's evidence ledger against the read-only sprint contract — a claimed browser PASS satisfied via `browser_evaluate` instead of the user path, a pass with no interaction, a contracted check with no evidence, or an invented check all BLOCK at /gate, G39; runtime trace/video artifacts G40) · ✅ **live-externals** (blocks a staged `tests/integration`/`e2e` test that reaches a real DB/HTTP/LLM instead of the boundary-doubles kit, G36; `regression-suite-full`/`impact-scoped-regression` add `--replay` so a missing fixture = live-external reach = hard fail) · ✅ **security-baseline ratchet** (strict-tier commit gate, `gates-strict.js` order 160, `runsWithoutSource`: secrets are absolute — any new one BLOCKs, never grandfathered, honours `harness:secret-ok` — and semgrep high/critical ratchets down vs `.claude/state/security-baseline.txt`; tier-aware `security-scan.js` is fail-closed on a missing required scanner in strict; surfaced at /auto Gate 7 via registry membership, Increment 1) · ✅ **secure-baseline-wiring invariant** (strict-tier presence gate, `gates-strict.js` order 165: BLOCKs if the propagated `.github/workflows/security.yml` gitleaks/sast jobs are absent or non-blocking, `.gitleaks.toml` is missing, or `quality.sast_engine` is unset — the anti-tamper counterpart that keeps the guards in place, Increment 1) · ✅ **red-phase-record** (PostToolUse(Bash) recorder: classifies a test run as pass/fail/**env-broken** and appends a hash-chained event to `.claude/state/red-phase.jsonl` carrying the test files and a sha256 of each *as it ran*. `tdd-test-first` proves a test EXISTS; its own comment says "pair with tdd-guard for red-green ordering" — this is that pair. env-broken is load-bearing: `pytest` exiting non-zero because pytest is absent is not a red phase, and recording it would arm a lock against a test that never ran. Observation-only, never blocks, G41) · ✅ **test-write-lock** (session BLOCK on editing a test whose latest run is still failing, wired into **both** `pre-write-gate.js` and `pre-bash-gate.js` — a lock covering only Edit/Write is theatre when the agent has `sed -i`. Deliberately narrow, and both narrowings were settled by reading the legacy skills rather than bolting on exemptions: **green-first never arms** (characterization pin-downs are green-first by definition, `pinning-down-behavior` Step 3, so the lane needs no carve-out and the mutation-smoke checkpoint cannot lock a pin), and the lock **closes once the file legitimately goes green** (locking for the whole task would block adding test 2 to a file whose test 1 already passes — obstruction, not tamper-proofing). Bypass `HARNESS_TEST_LOCK=off`, G42) · ✅ **test-integrity** (commit-cadence backstop, order 155, `runsWithoutSource`: a test file must not change between the run that made it RED and the run that made it GREEN — changed means the test was weakened, unchanged means production code is what made it pass. The **pair** carries the proof; no single anchor can, because a tamperer edits and re-runs, after which every single-anchor rule agrees with itself. Turn-level gates are steppable (`--no-verify`, hooks off, work outside Claude Code), so this is the merge-level control with no in-session override; emits `specs/reviews/test-integrity.json` because the ledger is gitignored and a hash chain would conflict across branches. Silent on still-red files, green-first pin-downs, and post-green refactoring. Bypass `HARNESS_TEST_INTEGRITY_GATE=off`, G43) |

### Traceability *(harness extension — a strength)*

| | Guides | Sensors |
|---|---|---|
| | FRD/PRD as immutable baseline · ✅ **sensor arbitration policy** (blocking levels + waivers) · ✅ **Token Governor / living navigation** (`token_governor`: DeepWiki/code-map before broad reads) · ✅ **`plan-review-loop`** (the human half of the planning gates: `/spec`, `/design` and `/test` each run their stop as a brainstorming dialogue — a review brief leading with the load-bearing decisions, the confidence drivers, and where the planner would push back at itself, then one question at a time in `/clarify` format, 2–3 alternatives at each contested fork, and a revise-and-re-present changelog that surfaces declined feedback rather than silently absorbing it; round cap 5. Shared sub-skill, invoked by all three phases, so the discipline is not triplicated) | ✅ `trace-check` (ID linkage across the planning trace chain — BRD vs FRD/interview spine via the `grounding-check.js` adapter, spec vs BRD, test vs AC+obligation; hard block) · ✅ `vocabulary-check` (entity/model names vs CONTEXT.md glossary terms, vertical-seeded via vertical-glossary-pack.js for any registered vertical plugin) · ✅ `verification-matrix-gate` (BRD/story AC -> unit/API/E2E evidence matrix, hard-blocking before PR; commit-time backstop via pre-commit hook, executed phase) · ✅ `constraints-extract` · ✅ `plan-confidence` · ✅ `seam-confidence` · ✅ `living-navigation-refresh` (graph/wiki freshness + token-savings status) · ✅ `canvas-sync-check` (changed files vs REASONS Canvas) · ✅ `ownership-check` (changed files vs component-map story ownership) · ✅ **amendment-provenance-check** (specs/design/ change requires a matching amendment, sprint-delta lane) · ✅ **impact-classifier** (routes /feature stories to /change vs /design --delta) · ✅ **custom-sensor-runner** (sensors-cli parity: runs project-declared sensors from `project-manifest.json#custom_sensors[]` through the default parser at commit + on demand, opt-in blocking) · ✅ **branch-protection-verify** (portfolio-scoped drift sensor, `provision-protection.js --verify`: reads back the live GitHub ruleset and diffs it against the config-driven desired branch-protection spec, exit-non-zero on drift; the platform-state counterpart to the in-repo secure-baseline-wiring guard) · ✅ **deploy-gate-verify** (portfolio-scoped drift sensor, `provision-environments.js --verify`: reads back each live GitHub deployment-approval Environment (per repo) and diffs it against the config-driven desired spec plus the approval-gate floor — >=1 required reviewer AND protected_branches — exit-non-zero on drift; the deploy-time platform-state counterpart, Increment 3) · ✅ **compliance-attestation** (repo-scoped, `generate-attestation.js`: assembles a per-commit durable evidence bundle from the control inventory + the two verify outputs + the gate verdict + ratchet baselines, each control resolved to a neutral standard clause via `standard-map.json`, and writes an immutable sha256-hashed `.claude/attestations/<sha>.json` + append-only `index.json`; `--verify` re-checks the integrity hash for **corruption** — the checksum is stored inside the file it covers, so an editor who rehashes verifies clean; authenticity needs signing (GPG/cosign/Sigstore), and until then the tamper-evidence comes from the committed git history plus branch protection, not the hash; every absent input recorded `null` — auditable, never a silent vacuous pass; remappable to a real evidence standard, no client literals; Increment 4a) · ✅ **portfolio-compliance-rollup** (portfolio-scoped drift sensor, `portfolio-rollup.js`: aggregates a collection of per-repo 4a attestations — a local collection dir, or `--fetch` via `fleet.json` + `gh api` — into one integrity-hashed portfolio report, verifying each input attestation's sha256 integrity first (a tampered/failed attestation is `integrity_failed`, never counted compliant), recording an unattested fleet repo as a `not-attested` gap, and computing per-repo harness-version drift (current/behind/ahead/unknown) against `--target-version` or the running harness version; `portfolio_compliant` is fail-safe — every repo must be compliant+integrity_ok+attested and the portfolio non-empty; the rollup is itself sha256-checksummed (`--verify`); identity from fleet/git at runtime, no client literals; Increment 4b) · ✅ **fleet-gate-retrofit** (portfolio-scoped live-platform aggregate, `fleet-retrofit.js`: audits — read-only, default — or `--applies` the branch-protection ruleset + deploy-approval environment across every repo in `fleet.json` in one command, invoking `provision-protection`/`provision-environments` per repo with a shared injected gh runner and classifying each from their real apply/verify exit codes; per-repo failures are isolated (a repo's 404/no-admin is a `failed` row, never aborts the fleet); the aggregate `specs/reviews/fleet-retrofit.json` marks each repo gated/drifted/not-gating/not-configured/failed with a fail-safe `fleet_gated` (an unconfigured gate is `not-configured`, never a vacuous `gated` — configured-ness is read from the manifest since the provisioners return exit 0 for "nothing to provision") — the live cross-gate counterpart to portfolio-compliance-rollup's evidence aggregate; specs from `project-manifest.json#github`, identity from fleet, no client literals) · ✅ **story-ownership-clusters** (planning-cadence hard gate, `story-clusters.js`: computes the OWNERSHIP decomposition the wave graph cannot express — `dependency-graph.json` groups stories by topological depth, which answers scheduling order, not who can own what end to end, so a depth level mixes unrelated subsystems into one branch while a vertical slice is torn across three. Connected components over HARD edges only (`data`/`behavior`), with `contract`/`ui` edges cuttable because publishing the interface first genuinely parallelises two engineers; oversized components split on the point-balancing bridge, a biconnected blob is reported `oversized` rather than split on an invented boundary. BLOCKs when an `interface_contracts` entry has `contract_story: null` — the cut is fiction and the clusters are not independent. A hard edge that had to be cut is never sold as parallelism: the downstream cluster is marked `independently_startable: false`. Feeds the INVEST `independent` letter, the only one measured rather than judged. Waves untouched, G37) · ✅ **brd-taxonomy-floor** (planning-cadence hard gate, `brd-taxonomy-check.js`: the only ABSOLUTE check in the planning chain. Every other traceability control is relative — grounding, trace-check and the AC round-trip all verify downstream-matches-upstream, so a gap present in the FRD propagates cleanly through all of them. Asserts a fixed ten-slot requirement floor (functional, data_lifecycle, integration, performance, security_authz, privacy_retention, observability, operability_failure, ux_accessibility, constraints); each slot needs a tagged BR requirement or a substantive committed `na_reason`. Placeholder excuses ("N/A", "none", "TBD", <25 chars) are rejected — the gate exists to force the question to be asked, and a box-tick means it was not. Also catches untagged requirements and misspelled slots, and fails loudly on an empty spine rather than passing vacuously. BLOCKs at /brd Step 4.45 before the rubric; the rubric completeness criterion is anchored to the verdict instead of judging prose, G38) · ✅ **plan-approval** (planning-cadence hard gate, `plan-approval.js`: the feedback half of the human planning gates, which were prose-only and therefore unobservable — nothing downstream could tell whether `/spec`, `/design` or `/test` ever stopped for a human, what was said, or whether the plan still matches what was approved. Owns one invariant no other sensor covers: *human review happened and still applies* — distinct from `trace-check`, which proves machine ID-linkage between artifacts and is silent on sign-off. Records each round to `specs/reviews/<phase>-approval.json` with verbatim feedback, changes, declined items, and a sha256 digest per approved artifact. Three anti-rubber-stamp properties: placeholder feedback ("n/a", "lgtm", <20 chars) is not feedback; an approval following a changes-requested round must account for it via `--changes` or `--declined`; and **an approval is void once the approved artifacts change** — the load-bearing part, since without it an agent can rewrite the stories post-approval and inherit a receipt for a plan the human never saw. `/design` blocks on `spec`, `/test` on `design`, `/auto` on all three (`check --phase all`). Headless lanes record a waiver naming the lane rather than leaving the receipt absent — an absent receipt and a deliberately-headless run are different facts; `--require-human` refuses waivers so a gated lane cannot be waived into. Flags `low_engagement` (zero feedback, zero answered questions) as a rubber-stamp trend signal for `/retro`, never a block — a plan can be right the first time) · ✅ **spec-decisions-gate** (planning-cadence hard gate, `validate-spec-decisions.js`: the control that makes the `/spec` shaping→rendering split real rather than prose. `/spec` runs in the main session and records the load-bearing calls — milestone scope, epic boundaries, real-vs-defensive dependencies — to `specs/decisions/spec-decisions.json`; `spec-render` forks onto the sidekick model and expands only what was decided. Owns one invariant no other control covers: *a human chose the scope before generation*, which is upstream of `plan-approval` (that records a review of artifacts already written). BLOCKs unless the file is well-formed, names at least one `milestone.epics` entry, carries at least one decision with `basis: "human"`, and marks every `load_bearing: true` decision human-made — the audited failure was six clarifications whose every basis ended "Original planner reasoning", the planner authoring both question and answer. Headless lanes waive **only** the human requirement, never the structural checks, and the waiver is reported rather than silent. Re-run at `spec-render` Step 0 so the block holds even if `/spec` is bypassed) · ✅ **design-decisions-gate** (planning-cadence hard gate, `validate-design-decisions.js`: sibling of spec-decisions-gate for the `/design` split, sharing its spine via `hooks/lib/decision-record.js` rather than duplicating it. Carries one rule no other control has — a load-bearing architecture decision must name what it **rules out**, with `n/a`/`none`/`TBD` rejected, and the committed stack must be named. Taken from what the audited design got right: its decision table is literally `| Decision | What it rules out |` and its alternatives-rejected section was the most useful content in 632 KB of output. `trace-check` proves ID linkage and `validate-canvas` proves structure; a design can satisfy both while every decision in it is a preference an implementer can quietly reverse) · ✅ **prd-shape-gate** (planning-cadence hard gate, `validate-prd.js`: the PRD is the root of the grounding chain and the only artifact a human authors, and nothing checked one — while `--autonomous`/`--auto` refuse to run without it. Since R2 `brd-adopt.js` carries PRD text **verbatim** into the spine, so there is no longer a rewriting step that might incidentally fix a malformed requirement. Owns one invariant nothing else covers: **every functional requirement has an observable postcondition** — `trace-check` proves ids link and `brd-taxonomy-floor` proves the ten slots are addressed, but neither notices a requirement the evaluator has nothing to check, and a check with no oracle passes by default. Measured on a real PRD: 27 of 38 had none. Accepts bullet/table/heading/bold layouts and inline `AC:` lines — ids and postconditions are load-bearing, heading text is not) |

> This row is **ahead of the source material**: the deterministic FRD→BRD→spec→test grounding chain has no equivalent in the SPDD example, which leans on human review. Keep it; the SPDD idea to *add* is the living, code-synced design artifact (G4), not its traceability.

## The control budget — a ratchet on the harness itself

Every sensor and guide in this registry earns its keep, and this file's doctrine —
*gaps are explicit; wire a control so it joins the loop* — is purely **additive**.
That was the whole problem: for months every gap analysis argued to *add*, nothing
ever argued to *subtract*, and a detailed 2026-06-10 cut-to-half proposal
(`docs/archive/internal/SIMPLIFICATION_PROPOSAL.md`) partially executed and was then
overwhelmed by accretion in five weeks (27→45 skills, 22→69 hook files). The harness
ratchets *product* code (coverage, cycles, coupling, length, perf) one way — better —
but had **no ratchet on its own size**.

The `control-budget` sensor is that missing counter-force. It treats the count of
registered controls (guides + sensors, non-planned) as a monotonic ratchet against
`.claude/state/control-budget-baseline.json`, the same decision shape as
`cycle-gate.js`: the count may only stay flat or drop. **A new control must either
replace an existing one (keeping the count flat/down — frictionless) or carry a
written `net_add_justification` in its manifest entry.** Unjustified net growth
`BLOCK`s at `npm run control-budget` (and in `/retro`), naming the offending
additions. Removing controls ratchets the baseline down.

Its companion `sensor-value-meter` (`npm run sensor-value`, report-only) supplies the
*removal* half: it turns the always-wired `recordOutcome` ledger
(`sensor-outcomes.jsonl`) into a ranked list of commit gates that never fire or never
block — the shelfware `/retro` should propose retiring. Budget ceiling + shelfware
nominations together make subtraction a standing force, not an occasional cleanup.

A preventive gate that blocks 0, though, is ambiguous: a working deterrent looks
identical to shelfware, and the ledger cannot tell them apart. `sensor-canary`
(`npm run sensor-canary`) resolves it — each canary feeds a gate's REAL detector a
known-bad input it must catch and a known-good input it must ignore; only a gate that
discriminates is PROVEN-LIVE. The value meter reads that set, so "never blocked" splits
into proven-live (kept) vs no-canary-yet (still a candidate). A canary that stops biting
fails the suite, so this is also a liveness ratchet on the preventive gates themselves.

> **When you add a control**, register it in `harness-manifest.json` as before — then
> either retire one to stay net-flat, or add a one-line `net_add_justification` saying
> why it earns its keep, and run `npm run control-budget` to re-ratchet the baseline.

## Steering loop (the human layer)

The harness improves itself between runs: `.claude/program.md` (the steering input that biases `/auto`), `.claude/state/learned-rules.md` (failure-derived **code** rules, injected into future prompts, never deleted), `.claude/state/process-rules.md` (monotonic **workflow** constraints — e.g. no destructive git during parallel implement, no stub-to-green — Bun Phase A), and `review-on-stop.js` (surfaces session learnings as suggested `CLAUDE.md` edits — applied *between* sessions, never mid-run, to preserve the prompt cache). Mid-session writes to `CLAUDE.md`, `.mcp.json`, and `.claude/settings*.json` are **blocked** by the `prompt-cache-prefix` sensor (`lib/prefix-cache.js` via pre-write + pre-bash gates; escape `HARNESS_PREFIX_EDIT=1` only between sessions).

### Self-audit against Anthropic's named generator-verifier failure modes

Anthropic's engineering writing on the generator-verifier pattern names two specific
failure modes. This harness's rubric agents and `/auto`'s convergence loop were
checked against both, 2026-07-09:

- **Rubber-stamping** ("a verifier told only to check whether output is good, with
  no further criteria, will rubber-stamp"): `evaluator.md`'s artifact mode uses a
  weighted 5-criteria rubric with a hard `>= 7.0` average and `>= 5` per-criterion
  floor; runtime mode requires all three verification layers plus the security gate
  plus the perf ratchet to independently pass — never a bare accept/reject.
  `security-reviewer.md` requires a mandatory find-then-refute adversarial pass
  before any BLOCK finding survives. `design-critic.md` scores 4 named criteria on
  a defined 1-10 rubric with worked calibration examples. None of the three ever
  emits an unstructured "looks good."
- **Oscillation without convergence** ("if the generator can't address the
  verifier's feedback, the system oscillates without converging"): `/auto` has
  three independent backstops — a 50-total-iteration hard stop, a 3-consecutive-
  failed-self-heal per-story escalation (marks BLOCKED, logs to `failures.md`,
  extracts a learned rule, moves on rather than looping forever), and a wall-clock/
  agent-spawn/est-cost budget cap checked every iteration. `/change` and `/vibe`
  contain risk by **scope** instead of iteration count — escalate out of the lane
  the moment a fix would expand past its micro-contract — a deliberate,
  blast-radius-appropriate alternative to iteration capping, not a gap.

No fix was required by this audit; it documents and cites existing coverage.

A related but distinct concern — single-verifier variance, not either named failure
mode above — is addressed separately: `/gate`'s security-boundary trigger spawns 3
independent instances each of `evaluator` and `security-reviewer`, majority-voted
per axis, so no single instance's blind spot decides the verdict alone (see the
"Bounded re-verification" step in `.claude/skills/gate/SKILL.md`).

## Skill-description conventions

Skill-description markers: pipeline *stage* skills carry a leading `[Internal pipeline stage — …]` prefix; discipline micro-skills carry a trailing `[Internal discipline — …]` suffix instead, because their leading "Use when…" phrase is the auto-invocation trigger and must stay first (pinned by `test/skills-consistency.test.js`).

The seven **legacy-change disciplines** (`checking-coverage-before-change`,
`pinning-down-behavior`, `sprouting-instead-of-editing`, `keeping-refactors-pure`,
`writing-acceptance-tests-first`, `checking-migration-safety`, `upgrading-dependencies`)
are a *family* — each fires in a different situation and each is proved by its own
distinct gate (deliberately not merged into a god-gate). One map of the whole family,
with the decision flow and the discipline→gate table, lives in
[`docs/legacy-change-disciplines.md`](docs/legacy-change-disciplines.md).

## The current holes (so they're not invisible)

The point of a registry is that gaps are explicit. As of 2026-07 **gaps G1–G30 are closed** (including G21 and G22) — the section is kept as the shipped-control record, not a backlog. The post-gap hardening items now active are: SPDD-grade BRD analysis, sensor arbitration + waiver schema, optional drift workflow template, Canvas sync check, cross-run flake-history trend, pre-code greenfield modularity assessment, G23's disclosed timestamp-ordering limitation (a receipt proves the AT was confirmed red by commit time, not strict chronological precedence over the later implementation diff), G29's own disclosed narrower-scope items (below), and G30's own disclosed scoping choices (below).

- **G1** *(this file)* — make the harness legible. ✅ done by `HARNESS.md` + `harness-manifest.json`.
- ~~**G2 (P0)** — no continuous **drift** sensors.~~ ✅ **done** — `drift-report.js` diffs architecture (cycles/hubs), dead-code (orphans), and dependency CVEs against a committed snapshot, flagging only *new* regressions; exit 1 on drift for cron/CI/`/schedule`. (Design-vs-code drift remains blocked on G4.)
- ~~**G3 (P0)** — no computational security sensors.~~ ✅ **done** — baseline secrets enforced at pre-write + commit; gitleaks/semgrep/npm+pip-audit wired into `/gate` via `security-scan.js`, degrading loudly when a tool is unprovisioned.
- ~~**G4 (P1)** — no SPDD-style living, code-synced design artifact.~~ ✅ **done (v1)** — `/design` emits a REASONS Canvas (`reasons-canvas.md`) with a machine-read `Governs` list; a structure gate validates it, and the drift monitor flags Canvas↔code drift. Full bidirectional regeneration (`/sync`) deferred by choice — detection + "fix-prompt-first" discipline shipped.
- ~~**G5 (P1)** — sensor messages are generic, not per-rule LLM-optimised.~~ ✅ **done** — `lib/sensor-guidance` enriches `verify-on-save` lint/type blocks with a per-rule fix line + the threshold-bump-with-justification valve.
- ~~**G6 (P1)** — no inferential modularity review on top of the coupling report.~~ ✅ **done** — `modularity-pack.js` grounds a `modularity-reviewer` agent (pre-classifying legit hubs so it doesn't flag factories/schemas); runs in `/brownfield --full` and, scoped to the touched components, in `/design --delta` Step D3.5.
- ~~**G7 (P1)** — `mutation-smoke` exists but isn't a `/auto` ratchet gate.~~ ✅ **done** — diff-scoped mutation gate enforced by pre-commit during `/auto`; survivors below threshold BLOCK with file:line + the exact flip. "tests pass" now implies "tests bite."
- ~~**G8 (P2)**~~ ✅ **done** — vertical bounded-context rules (`contexts.js`) + import-cycle ratchet (`cycle-gate.js`).
- ~~**G9**~~ ✅ **done** (both halves) — the guide scaffolds /metrics into generated apps; the `runtime-slo` sensor reads it and FAILs on 5xx error-rate over SLO.
- ~~**G10**~~ ✅ **done** — `/scaffold` resolves a named topology (web-app / api-service / cli-or-library) and presets the manifest-knob bundle via `topologies.js` (Ashby's-Law variety reduction). ~~**G11**~~ ✅ **done** — `harness-coverage.js` reports per-axis coverage from the sensors' `scope` field (`npm run harness-coverage`).
- ~~**G12 (P2)**~~ ✅ **done** (all 4 slices) — API contract-drift (`oasdiff`), default-on axe/WCAG, approved-fixtures (snapshot-oracle lock), and flake detection (N× re-run). **G1–G12 are now all closed.** (The approved-fixtures review minors — `.approved.*` matcher coverage, build-dir walk ignores, friendlier missing-file error — have since been fixed; the only forward item is a P3 flake-history trend.)
- ~~**G13–G14** *(Anthropic long-running-agent principles)*~~ ✅ **done** — distinct first-context-window initialization (`first-window-init` guide) and a session-start **resume smoke check** (`resume-smoke` sensor) in `/auto` SECTION 2. Sourced from Anthropic's *Effective harnesses for long-running agents* + autonomous-coding quickstart (the multi-context-window split and the "run a basic test on the dev server at session start" failure-mode fix), not the Fowler/SPDD roadmap.
- ~~**G15** — no cross-feature regression sensor.~~ ✅ **done** — `/evaluate` and `/gate` only verified the CURRENT story-group's sprint contract, and `/change`/`/vibe` only re-ran the unit suite; nothing re-ran the accumulated `e2e/` Playwright suite or PRIOR groups' sprint contracts, so a change could pass its own tests while silently breaking an earlier feature. `regression-gate.js` re-runs every accumulated Playwright spec and re-executes every prior group's sprint-contract `api_checks` live against the running app (reusing the `validate-contract.js` schema machinery), excluding tests already quarantined in `flake-history.jsonl`. As of gap G16, this full unabridged sweep is wired as a hard block into `/gate` and `/auto`'s pre-merge-to-`WAVE_BASE` step only (reserved for merge time — too expensive for every local iteration); `/change` Step S5 and `/vibe` Step 6 now run G16's faster impact-scoped check instead. Degrades loudly when there is no `e2e/` or `sprint-contracts/` baseline yet. **Known limitation, now closed by G28** (below): e2e-spec auto-quarantine only matches if `flake-history.jsonl`'s `name` field carries a Playwright-shaped title, and `flake-detector.js` used to only ever record node:test TAP names — see G28 for the `--e2e` mode that closes it.
- ~~**G16** — no LOCAL/fast regression signal before commit, only the full merge-time sweep.~~ ✅ **done** — G15's `regression-gate.js` proves itself only at `/gate`/`/auto` pre-merge; running the whole accumulated suite on every `/change`/`/vibe` was too expensive, and a single non-peer-reviewed preprint (TDAD, arXiv:2603.17973 — one finding, not its own proposed fix, survived adversarial verification) found telling an agent to just "run more tests" without targeted context made regressions *worse*, not better — directional evidence, not a settled result. `impact-scope.js` computes a deterministic Test Impact Analysis — changed files → reverse-dependency blast radius over `code-graph.json`'s imports/calls edges → owning story-group(s) (`verification-matrix.json` primary, `component-map.md` + `features.json` fallback) → that group's e2e spec(s) + sprint-contract — and `local-regression-gate.js` runs only that subset (unioned with any human-curated `project-manifest.json#verification.golden_paths` safety net), reusing G15's `runE2eSuite`/`extractPlaywrightFailures`/`regressPriorContract` primitives rather than duplicating them. Wired as a hard block into `/change` Step S5 and `/vibe` Step 6, every iteration; G15's full sweep remains the mandatory merge-time backstop, unchanged.
- ~~**G17** — three of the four legacy-preservation discipline skills had no mechanical proof they ran.~~ ✅ **done** — `checking-coverage-before-change`, `pinning-down-behavior`, and `sprouting-instead-of-editing` were prompt-level instructions only; only `keeping-refactors-pure` had a real pre-commit gate. An agent could skip straight to editing an uncovered legacy symbol and nothing would block it — directional evidence (the same TDAD preprint as G16) that procedural-only discipline without a mechanical, presence-checkable gate can make under-pressure outcomes *worse*, not better. `record-coverage-verdict.js` wraps `checking-coverage-before-change` Step 2's (unmodified) `coverage_map.py` call, appending every symbol verdict to `specs/reviews/coverage-verdicts.jsonl` as a receipt while passing the JSON through unchanged so the skill's read-and-route flow is undisturbed. `legacy-discipline-gate.js` then re-checks every staged MODIFIED (not newly-added) production source file at commit time: no receipt at all BLOCKs, and a latest-recorded UNCOVERED verdict BLOCKs unless a new/modified test-shaped file is also staged as evidence a pin-down or sprout happened — composing with, not duplicating, `mutation-gate.js` (G7)'s independent proof that new test code bites. Wired as a hard block into `.claude/git-hooks/pre-commit`, default-on (`HARNESS_LEGACY_DISCIPLINE_GATE=off` acknowledges a local skip; a real exception belongs in `sensor-waivers.json`). Degrades loudly, never silently, when no `code-graph.json` exists yet or the graph has no per-file symbol records. **Both of the two limitations originally disclosed here are now narrowed by gap G29** (below): receipt matching is range-aware, not file-level, and the UNCOVERED-evidence check is a per-file/per-story relatedness signal, not a single commit-wide boolean. See G29 for what changed and its own, narrower disclosed limitations. The forward item this entry originally left open — mechanically verifying `sprouting-instead-of-editing`'s "legacy diff touches exactly one symbol" constraint against `code-graph.json` symbol ranges — is now closed by **gap G30** (below).
- ~~**G18** — the coupling/instability signal computed for every hub in `code-graph.json`'s `metrics.hubs` (fan_in, fan_out, `instability`) existed only as a periodic drift-only report (`coupling-report.md`, `drift-report.js`'s `newUnstableHubs`), never fed back to the agent as a commit-time self-correction signal — exactly the "good kind of prompt injection" gap the Fowler *Maintainability Sensors for Coding Agents* article describes: a team that never wired `npm run drift` to `/schedule` or CI saw this signal not at all, and even a team that did saw it outside the normal `/change`/`/gate` review loop.~~ ✅ **done** — `coupling-gate.js` mirrors the import-cycle ratchet (G8, `cycle-gate.js`) exactly, reusing `drift.js`'s `unstableHubIds` threshold logic (fan_in >= 5, instability >= 0.8) and `cycle-gate.js`'s `gateDecision` ratchet math rather than reimplementing either. It compares the current unstable-hub set to a monotonic baseline (`.claude/state/coupling-baseline.txt`, which stores the hub-id set, not just a count, so a BLOCK can name the specific new hub(s)). A new unstable hub **BLOCKs** at `/auto` Gate 4 and `/gate` (also `npm run coupling-gate`), naming the file, its fan-in/instability numbers, and concrete remediation (extract a narrower interface, or split responsibilities to lower fan-in) — an agent-actionable message, not just a fact statement. Removing unstable hubs ratchets the baseline down. No code-graph yet → skipped loudly, the same degrade pattern G8 uses. `coupling-report.md`'s OTHER data (the full fan-in/fan-out tables, dead-code candidates) remains discovery-only; only the unstable-hub signal is now enforced.
- ~~**G19** — `modularity-review`'s manifest entry claimed `cadence: "drift"`, but the sensor has no periodic trigger at all: `drift-report.js` (the harness's actual drift runner) has zero references to modularity anywhere in it or in `hooks/lib/drift.js` — a manifest-honesty violation of this file's own registered invariant that every active entry point at a real `wired_at` path with an accurate cadence. The review's only two real triggers, `/brownfield --full` Step 3.6 and `/design --delta` Step D3.5, are both manually/human-triggered, never scheduled.~~ ✅ **done** — `modularity-review`'s cadence is now honestly `"planning"` (matching `modularity-pack`/`coupling-report`'s existing convention for the same trigger shape), and its description states plainly that it has no periodic component of its own. A genuinely periodic signal was added to fill the resulting hole: `record-modularity-review.js` writes `.claude/state/modularity-review-marker.json` (`{timestamp, unstableHubIds}`) after a real review completes (wired into both trigger points' skill steps); `hooks/lib/drift.js`'s new `withModularityStaleness` (reusing the existing `unstableHubIds`/`newItems` primitives, no reimplementation) diffs the live code-graph's current unstable hubs against that marker inside `drift-report.js`, threaded through `diffSnapshots`/`hasRegressed`/`renderDriftReport` exactly like `canvasDrift` — a newly-stale hub now trips `hasRegressed` (exit 1) and the rendered section names the hub and tells the agent to run `/brownfield --full` or `/design --delta`, not just state a count. No marker at all (never run) means every currently unstable hub counts as stale, degrading loudly via a stderr warning rather than silently passing. Registered as the new `modularity-review-staleness` sensor (`cadence: "drift"`, `gap_ref: "G19"`), a cheap deterministic proxy for "the expensive inferential review is overdue" — not a substitute for the review itself.

- ~~**G20** — this harness's only acceptance-criteria verification layer was Playwright E2E (`/test` Step 5)~~ ✅ **done** — full-stack, browser-driven, generated per story. That was structurally Matteo Vaccari's named antipattern **E2E-as-AT-implementation**: slow, flaky, unreliable, a failing test requires log-spelunking, and it often breaks on external systems outside your control. Vaccari's fix is not to delete E2E — it stays as a final confirming layer — but to make the *primary* acceptance-criteria loop a fast, business-readable Acceptance Test (AT) layer built on Ports-and-Adapters: business rules on the inside, I/O on the outside via a swappable adapter, so the AT calls business logic directly through an in-process port with a test-double adapter, bypassing the UI/transport layer entirely. This pass shipped the DISCIPLINE half: `writing-acceptance-tests-first` (guide) writes the AT for a story BEFORE implementation begins, from a human-provided template (or flags the first AT on the codebase for human confirmation before treating it as the pattern), reusing `/seam-finder`'s existing observable/funnel/asymmetry scoring to locate the port rather than inventing a new seam concept, and requires the AT to fail for the right reason before implementation proceeds. Wired into `/test` Step 4.6 (part of the plan-phase deliverable, alongside the existing grounding gate) and `/change` Step S4 (as a `REQUIRED SUB-SKILL`, ahead of the existing red-test step); Step S6 review also asks `code-reviewer` to judge a changed AT file's readability specifically. `/test` Step 5's Playwright E2E generation is untouched — ATs are additive, E2E remains the final full-stack layer. **The mechanical enforcement gate this entry originally disclosed as missing now exists**: gap G23 (below) ships `at-first-gate.js`, the same commit-time proof pattern G17 uses for its own discipline — see G23 for what it proves and its disclosed limitation.

- ~~**G21**~~ ✅ **done** — this registry answers *what governs what*, but not the higher-level question a team hands a codebase to a heavy-autonomy agent needs answered first: *is this codebase actually ready for that?* `agent-readiness.js` (loosely inspired by Factory.ai's "agent readiness" framing — 8 pillars × maturity levels) aggregates state this harness's OWN prior sensors and gates already produce into one synthesis dashboard, computing nothing new: Style & Validation (lint/type config presence + real provisioning, reusing `toolchain.js`'s run/skipped primitives rather than a bespoke `which` check), Architecture Fitness (`cycle-baseline.txt` + `coupling-baseline.txt`, G8/G18), Testing (coverage ratchet, G7 mutation-gate, G15/G16 regression gates, G20 acceptance-test artifacts as an informational note), Code Quality / Modularity Freshness (reuses `drift.js`'s `withModularityStaleness`, G19), Documentation / Navigation (reuses `stale-stamp.js`'s `STALE_MARK` convention), Observability (`project-manifest.json#observability.enabled`, G9), Security & Governance (whether `security-scan.js`'s enhanced tools are actually provisioned, G3), and Dev Environment (`init.sh` vs `verification.mode`). Each pillar reports `active`/`partial`/`planned` — the exact vocabulary `harness-manifest.json#model.statuses` already defines — with a one-line concrete remediation when not `active`. Report-only (exit 0 always, like `harness-coverage.js`); writes `specs/reviews/agent-readiness.md`/`.json`; runnable via `npm run agent-readiness` or `/agent-readiness`. Same registry placement as G11 below: a standalone section, not a `harness-manifest.json` guides/sensors entry, since it inspects/reports on existing sensor and codebase state rather than governing code directly.

- **G22** ✅ **done** — `scaffold-copy.js` copies `.claude/scripts/*.js` and `.claude/skills/*/SKILL.md` into scaffolded projects via explicit named lists (`CORE_SCRIPTS`, `CORE_SKILLS`; `BROWNFIELD_SCRIPTS`/`BROWNFIELD_SKILLS` just spread them), not a directory glob. When a skill is edited to call `node .claude/scripts/X.js` or invoke a `REQUIRED SUB-SKILL: name` discipline without adding that name to the matching list, the reference silently breaks — but only in projects the scaffold produces, never in this repo's own `npm test`, since this repo already has every script and skill on disk regardless of the copy lists. The gap surfaced twice: once for this session's own additions (`writing-acceptance-tests-first` missing from `CORE_SKILLS`, several new scripts missing from `CORE_SCRIPTS`), then again as a much larger pre-existing set unrelated to this session's own work — 16 scripts (`approved-fixtures-gate.js`, `auto-merge.js`, `contract-accessibility-default.js`, `contract-drift-gate.js`, `feature-lane.js`, `modularity-pack.js`, `mutation-gate.js`, `naming-clusters.js`, `seam-confidence.js`, `security-scan.js`, `slo-check.js`, `validate-canvas.js`, `vertical-glossary-pack.js`, `vocabulary-check.js`, `wave-plan.js`, `wave-pr.js`) already referenced by skills but missing from `CORE_SCRIPTS`. Both are now fixed. The permanent fix is `test/scaffold-copy-completeness.test.js`: it scans every `.claude/skills/*/SKILL.md` and `.claude/git-hooks/pre-commit` for `node .claude/scripts/X.js` references and `REQUIRED SUB-SKILL: name` references (covering both backtick conventions already in use — around the name only, or around the whole phrase), and asserts each referenced name is present in `scaffold-copy.js`'s own `CORE_SCRIPTS`/`CORE_SKILLS` arrays, parsed straight from source rather than hand-duplicated. A future reference added without its copy-list entry now fails loudly in THIS repo's own `npm test`, not silently three steps downstream in a scaffolded project — the same class of guard `test/hook-requires-tracked.test.js` already applies to require()-target tracking. **Registry placement**: like `hook-requires-tracked.test.js`, this is *not* a `harness-manifest.json` guides/sensors entry and gets no dedicated section below. Guides/sensors in that manifest govern code *inside scaffolded target projects* across the four quality axes; this test instead guards the harness repo's OWN build-time copy wiring, which is meaningless outside this repo (`scaffold-copy.js` is a harness-repo-only build script, never itself copied to a target project). That also makes it a different placement than G11/G21's standalone-section treatment, which document report-only tools a human runs on demand (`npm run harness-coverage`, `/agent-readiness`) — G22 is a plain enforcement test wired into `npm test`, not a tool with its own usage surface.

- **G23** ✅ **done** — `writing-acceptance-tests-first` (G20) shipped only the DISCIPLINE half of the AT-first guide: nothing proved at commit time that a story's AT was actually written and observed red before its implementation shipped, the exact disclosed forward item G20's own entry named. This closes it with the same mechanical-proof pattern G17 already proved out for `checking-coverage-before-change`: `record-at-red.js` runs a story's AT test command and, only when it FAILS (the AT is red), appends a receipt `{storyId, atPath, observedRedAt, testCmd}` to `specs/reviews/at-red-receipts.jsonl` (append-only, gitignored `**/specs/reviews/`, same convention as `coverage-verdicts.jsonl`); a PASSING run records nothing and exits loudly instead, quoting the skill's own Red Flag ("An AT that passes against unmodified/unimplemented code proves nothing") so the non-receipt is visible in the calling skill step's output. `at-first-gate.js` then re-checks at commit time: for every story with a NEW (added, not modified) staged production source file, it requires BOTH `specs/test_artefacts/acceptance/{story-id}.<ext>` on disk and a matching receipt naming that `{storyId, atPath}` pair, or BLOCKs, naming the story and the exact remediation. "Which story owns this new file" reuses `parseComponentMapStoryFiles` from `hooks/lib/impact-scope.js` (G16) — the codebase's existing story→files `component-map.md` parser — rather than `ownership-check.js`'s own `parseComponentMap`, which deliberately flattens every row into one owned-path Set with no story association; a brand-new file with no story owner at all remains `ownership-check.js`'s own, already-covered problem, not this gate's concern. Wired into `.claude/git-hooks/pre-commit` as a default-on hard block (`HARNESS_AT_FIRST_GATE=off` acknowledges a local skip, mirroring `HARNESS_LEGACY_DISCIPLINE_GATE=off`'s shape); `writing-acceptance-tests-first` Process step 5 now runs the AT through `record-at-red.js` as a small, natural addition to the existing "confirm it fails for the right reason" step. Degrades loudly, never silently, when no `component-map.md` exists yet or no staged NEW production file resolves to a story owner. **Known limitation, disclosed rather than silently accepted** (the same precedent G17 sets for its own two limitations): a receipt's timestamp mechanically proves the AT was confirmed red BY THE TIME OF THIS COMMIT — it cannot prove strict chronological ordering relative to a specific later implementation commit without fragile git-history archaeology. The value this gate proves is "the AT existed and was proven red by commit time," not "provably happened strictly before every line of the implementation diff" — the achievable proof, stated as such, not oversold.

- **G24** ✅ **done** — `contexts.js` (G8) implements real, wired bounded-context enforcement, but `/scaffold`'s interview never mentioned it: a team scaffolding a project had no way to discover the capability existed short of reading `contexts.js`'s source, so `architecture.contexts` stayed silently opt-in and effectively undiscoverable. Fixed with a two-tier surfacing, proportionate to how rarely most projects need it: (1) Step 2's `architecture` documentation in `.claude/commands/scaffold.md` now lists `contexts` as a documented sibling to `layers`/`layer_roots`/`"enabled": false`, with the same config shape `contexts.js`'s own header comment defines; (2) the Step 10 report adds a one-line, non-blocking nudge — "Bounded-context enforcement is available but not configured…" — for any multi-module project (backend/+frontend/, the same signal Step 5.B already uses) that didn't opt in; and (3) the full configuration wizard (Step 1.E) gained a new Q9 asking whether the project has bounded contexts to enforce, with the `allow` list defaulting to `[]` (strictest) rather than a guessed permissive default. The fast inferred-and-confirmed path (Step 1.C/1.D options A/B, most scaffolds) stays question-free — only the Step 10 nudge reaches it — so this closes the discoverability gap without adding interview fatigue to the common case. `contexts.js` itself is unchanged: still opt-in, still off by default, only now impossible to scaffold a project without at least seeing that the option exists. **Registry placement**: like G22, this is a prompt/documentation change to `.claude/commands/scaffold.md`, not a new computational sensor — `bounded-context-rules` (G8) already covers the enforcement mechanism in `harness-manifest.json`'s `sensors[]`; G24 gets no separate manifest entry of its own.

- **G25** ✅ **done** — `deep-mutation.js` (the optional Stryker/mutmut release tier) is correctly implemented but was permanently `unprovisioned` by default: `detectTool()` only activates when a Stryker/mutmut config or dependency is already on disk, and nothing in `/scaffold` ever put one there, so a team that wanted the deeper mutation tier got zero onboarding help and had to hand-write a config from scratch. Fixed the same way G24 fixed bounded-context discoverability — a prompt/documentation change to `.claude/commands/scaffold.md`, not a new computational sensor: (1) a new "Mutation Testing Config" subsection infers Stryker (JS/TS stacks) or mutmut (Python backend stacks) from the stack chosen in Step 1 and records a `project-manifest.json#quality.mutation` block (`tool`, `config_file`, and an empty-by-default `critical_globs` — the same "human judgment call, not an auto-guess" reasoning `verification.golden_paths` (G16) already established); (2) a "Provision the Mutation Tool" subsection writes an actual starter config at the project root — `stryker.conf.json` (from the new `.claude/templates/stryker.conf.json.template`, with `testRunner` mapped from the JS/TS side's `test_runner` to the matching Stryker runner plugin: `vitest`→`@stryker-mutator/vitest-runner`, `jest`→`@stryker-mutator/jest-runner`) or a `[tool.mutmut]` `pyproject.toml` (from `.claude/templates/mutmut-pyproject.template.toml`) — placed at project root specifically because `detectTool()` only ever checks `process.cwd()`, never a `backend/`/`frontend/` subdirectory; (3) for a brownfield rescaffold where `package.json`/`pyproject.toml` already exists, the corresponding dependency (`@stryker-mutator/core` + runner plugin, or `mutmut`) is merged into it directly — real provisioning, not just a hint — while a greenfield project (the common case, since `/scaffold` never generates application source) gets the starter config plus a Step 10 report line naming the install command to run once the JS/TS or Python project exists; (4) Step 7 gained a one-line init.sh confirmation echo (not a full LSP-style health-check block — mutation tools have no meaningful `--version` probe) when a tool was configured. `deep-mutation.js` itself is unchanged and correct; older pre-G25 projects and non-JS/Python stacks (Go, Rust, Java, C#) are unaffected and still exit `unprovisioned` until a team configures a tool by hand. **Registry placement**: unlike G24, this gap's sensor already existed in `harness-manifest.json` (`deep-mutation`, present since before this registry's G1–G24 pass) — its entry is updated in place (new `gap_ref: "G25"`, description now mentioning scaffold-time provisioning) rather than getting a new entry, the same "update, don't duplicate" treatment G19 gave `modularity-review`.

- **G26** ✅ **done** — `graph_metrics.py`'s `_hubs()` truncates `code-graph.json`'s `metrics.hubs` to the top 25 by fan-in, a reasonable cap for the human-facing coupling-report markdown table on its own terms. The bug: every downstream consumer that runs a threshold-based unstable-hub CHECK (fan_in >= 5, instability >= 0.8) — `coupling-gate.js`'s ratchet (G18), `drift.js`'s staleness tracking (G2), `agent-readiness-project.js`'s modularity-freshness pillar (G21), `record-modularity-review.js`'s marker (G19) — was reusing that same display-truncated list for its own enforcement decision, so a real unstable hub ranked 26th+ by fan-in was structurally invisible to all of them, silently understating the ratchet's baseline and never surfacing in any of the four sensors it feeds. Fixed additively, without touching any of the four gaps' existing behavior or fixtures: `graph_metrics.py`'s `compute()` now also writes a sibling `metrics.unstable_hubs` field — the FULL, uncapped hub list already filtered by the identical threshold (`UNSTABLE_FAN_IN`/`UNSTABLE_INSTABILITY`, kept in lockstep with `hooks/lib/drift.js`'s own constants of the same name); `metrics.hubs` itself and its top-25 cap are completely unchanged. `hooks/lib/drift.js` gained a new `hubsForStabilityCheck(graph)` helper (exported alongside the untouched `unstableHubIds`) that prefers `metrics.unstable_hubs` when present — its entries are already the unstable set, no further filtering needed — and falls back to the pre-G26 `unstableHubIds(metrics.hubs)` path when absent (an older graph, or a non-Python producer that hasn't gained the field). All four production call sites that read `unstableHubIds` off a live-read graph now go through this helper instead: `drift.js`'s own `extractMetrics`, `coupling-gate.js`'s `unstableHubKeys` (a fourth call site found by grep beyond the three named in the fix's design, since it feeds G18's ratchet directly), `agent-readiness-project.js`'s `modularityFreshnessPillar`, and `record-modularity-review.js`'s `buildMarker`. Every one of G18/G19/G21/G23's existing fixture-based tests — which hand-construct graphs as `{ metrics: { hubs: [...] } }` with no `unstable_hubs` field — kept passing completely unmodified via the fallback path (127/127 across the affected suites, zero regressions). New coverage (`test/unstable-hub-truncation.test.js`): a hand-built fixture proves a 26th-ranked unstable hub absent from a capped `hubs` list is caught via `unstable_hubs`; a second proves the no-`unstable_hubs` fallback matches the pre-G26 behavior exactly; a third is a REAL round-trip — a generated fixture repo with 30 files at fan_in=5/fan_out=20 (instability exactly 0.8) run through the actual `code_index.py` — proving the real Python output emits `metrics.unstable_hubs` with all 30 entries while `metrics.hubs` stays capped at 25, and that at least one unstable hub is excluded from the capped list. **Known, disclosed limitation**: `.claude/scripts/coupling-gate.js`'s `hubDetail()` (the CLI's BLOCK-message hub lookup, used only for display) still reads the capped `metrics.hubs` array to print a hub's fan-in/instability numbers; a BLOCK on a 26th+-ranked hub still fires correctly (the ratchet decision itself now sees the full set via `hubsForStabilityCheck`) but the message falls back to naming the hub with no stats attached, since `hubDetail` was out of this fix's additive scope (it never called `unstableHubIds`). A richer message for that one case is a low-priority cosmetic follow-on, not a correctness gap.

- **G27** ✅ **done** — `modularity-reviewer`'s inferential pass had 4 assessment categories (semantic duplication, misplaced responsibility, argument clumps, cycles) grounded only in a looser "duplication/responsibility/argument-clumps/cycles" framing, not in any formal coupling model — a hub or duplication candidate that was intentionally, healthily coupled (e.g. a narrow contract used across a service boundary) had no principled way to be told apart from one that was genuinely risky, beyond ad hoc judgment. Fixed by grounding the review in Vlad Khononov's actual published Balanced Coupling rubric (the real `modularity` Claude Code plugin at github.com/vladikk/modularity, coupling.dev): a new 5th assessment category, "Coupling balance (Balanced Coupling model)", classifies each hub/duplication relationship already surfaced by categories 1-4 (no new deterministic input) on three dimensions — Integration Strength (`intrusive` > `functional` > `model` > `contract`, from the call sites), Distance (`near`/`medium`/`far`, from the module/package boundary), and Volatility (`high`/`low`/`medium` core/generic-subdomain judgment, sourced from `specs/design/CONTEXT.md` or `naming-clusters.md`/`.json` if either has a usable subdomain grouping, else explicitly disclosed as absent and inferred from git commit-frequency or story/BRD context instead of guessed) — then applies the formal rule `BALANCE = (STRENGTH XOR DISTANCE) OR NOT VOLATILITY`, flagging a new `coupling-imbalance` finding category only for the high-strength + far-distance + high-volatility combination; every other combination is healthy by the rule and is explicitly not flagged, matching the file's existing "never flag on fan-in alone" discipline. `modularity-verdict.json`'s finding shape gained optional `integration_strength`/`distance`/`volatility` fields, populated only for `coupling-imbalance` findings. `harness-manifest.json`'s `modularity-review` entry and this file's Maintainability matrix row now name the rubric explicitly instead of a general "Khononov-style" gesture. Prompt-only change (`.claude/agents/modularity-reviewer.md`); no new script, no manifest `gap_ref` added to `modularity-review` (`test/modularity-wiring-contract.test.js` asserts it stays gap-free, matching the G19 "update, don't duplicate" precedent) — the change is documented here and in the manifest description instead.

- **G28** ✅ **done** — closes G15's own disclosed known limitation: `regression-gate.js`'s e2e-spec auto-quarantine matches `flake-history.jsonl`'s `name` field against a Playwright spec's joined `describe > test` title, but `flake-detector.js` only ever ran/recorded node:test TAP names, so that match path never actually fired — an operator would have had to hand-write a Playwright-shaped title into `flake-history.jsonl` for quarantine to work at all. Fixed additively, without touching `flake-detector.js`'s existing TAP mode or `flake-history.js` (which already just passes through whatever `name` value it receives, unmodified — confirmed, not changed). `hooks/lib/regression-gate.js`'s `extractPlaywrightFailures` (G15) had its internal tree-walk factored into a shared `walkPlaywrightSpecs` helper, alongside a new exported `extractPlaywrightResults(report)` that returns EVERY spec's `{file, line, title, ok}`, pass or fail — `extractPlaywrightFailures`'s own exported behavior and output shape are completely unchanged (its existing callers, `regression-gate.js` and `local-regression-gate.js`, and their test suites needed zero changes), the same purely-additive "refactor the shared internals, don't touch the existing external contract" discipline G26 already used for `unstableHubIds`/`hubsForStabilityCheck`. `flake-detector.js` gained a new `--e2e` flag: instead of `parseTap`, it runs the given `--test-cmd` (default `npx playwright test --reporter=json`) and parses the JSON output via `extractPlaywrightResults` (imported from `hooks/lib/regression-gate.js`, reused rather than reimplemented), building the same `{title: 'ok'|'not ok'}` map shape `parseTap` already produces — `aggregateFlakes` needed zero changes, since it was already format-agnostic (it only needs consistent keys and 'ok'/'not ok' values across runs, confirmed by reading it rather than assumed). An unparseable or errored `--e2e` run is excluded from aggregation via the same `null` sentinel and downstream handling a TAP run's spawn-error/timeout case already used — no separate error semantics for the two modes. New coverage: `test/regression-gate-results.test.js` proves the refactor returns every spec (not just failures) while leaving `extractPlaywrightFailures`'s own output byte-for-byte unchanged (verified by re-running `test/regression-gate.test.js` and `test/local-regression-gate.test.js` with zero changes needed to either); `test/flake-detector.test.js` adds a real round-trip — a fake Playwright-JSON-emitting CLI that alternates `ok`/`false` by run, proving a spec that fails in one run and passes in another is correctly flagged as a flake via `--e2e`, plus a stable-pass case and an unparseable-output case. `.claude/skills/test/SKILL.md` was checked and does not reference `flake-detector.js`/TAP-only framing, so it needed no update. **Opt-in, like flake detection always was**: `npm run flakes` still only runs the TAP path; e2e flake capture requires explicitly invoking `npm run flakes:e2e` (or `flake-detector.js --e2e`) — this closes the mechanism, it does not make e2e flake detection run automatically on any existing cadence.

- **G29** ✅ **done** — closes both of G17's own disclosed known limitations. **Gap A (file-level, not symbol/line-range, receipt matching)**: `checkLegacyDiscipline` used to ask only "does ANY receipt exist for this FILE", so a COVERED verdict for symbol A (lines 1-5) satisfied the gate for an edit to a never-checked symbol B (lines 40-50) in the same file — coarser than the Iron Law's own symbol-scoped wording. A new pure parser, `hooks/lib/diff-hunks.js`'s `parseUnifiedDiffRanges`, reads a real `git diff --cached -U0` and returns each changed file's new-side line ranges (no existing hunk parser was found to reuse in this repo — `coverage-diff.js` only needs changed FILES, not ranges, and `hooks/lib/coverage-preflight.js`'s `editedRanges` works from an Edit tool's `old_string` against in-memory content, not a git diff, so both were read and rejected as fits before writing this new, minimal parser). `legacy-discipline-gate.js`'s `run()` and `.claude/git-hooks/pre-commit`'s `checkLegacyDisciplineGate` both now call the gate's new `gitDiffRanges(exec, 'MR')` and pass the result into `checkLegacyDiscipline`, which only counts a receipt as covering an edit when the receipt's `[start,end]` overlaps the changed range — an unrelated-range receipt is folded into the existing `noVerdict` BLOCK case, extended to be range-aware rather than merely file-presence-aware. `--files` CLI mode has no git diff plumbing, so it passes `changedRanges: null` (a disclosed, narrower-scope permissive fallback to the pre-G29 whole-file assumption, not a silent regression) — `--staged` mode (the git-hook's actual production path) always has real range data. **Gap B (commit-wide, not per-file/per-story, test-evidence boolean, with no bite-check backstop outside `/auto`)**: `hasTestEvidence` used to be a single boolean — staging ANY test-shaped file ANYWHERE in the commit satisfied the UNCOVERED-evidence requirement for EVERY uncovered file in that commit. A new module, `hooks/lib/legacy-discipline-relatedness.js`'s `hasRelatedEvidence`, replaces it with a three-tier per-file signal: (1) `component-map.md` story ownership — reusing `parseComponentMapStoryFiles` from `hooks/lib/impact-scope.js` (G16), the same parser `at-first-gate.js` (G23) already reuses for exactly this "which story owns this file" question — requires the uncovered production file and the staged test file to share a story; (2) when the map has no opinion on the production file (or doesn't exist), a naming-convention heuristic (matching basenames with test/spec markers stripped, e.g. `src/b.py` ↔ `tests/test_b.py`); (3) when neither resolves, the old commit-wide boolean as a last-resort fallback, surfaced as a `relatednessNotes` entry so "relatedness could not be verified" is visible in the gate's own output rather than silently indistinguishable from a verified pass — the explicit design choice named in this gap's own brief, not silently blocking everything (no map, no story) or silently passing everything (the pre-G29 bug). Separately, `mutation-gate.js` (G7)'s independent bite-check backstop only ran `inAutoBuild`, so a manual commit's UNCOVERED-evidence path had no proof the related test actually bites. A new `hooks/lib/legacy-bite-check.js` (`biteCheckFiles`, dependency-injected `runMutationOnFiles` so it stays unit-testable without spawning real subprocesses) reuses G7's existing `runMutationOnFiles` primitive unchanged, but with a deliberately narrower budget (3 mutants, a 10s timeout each, vs G7's 12 × 30s) scoped ONLY to the specific file(s) that just passed via evidence — a full G7-scale run was judged impractical to add unconditionally to every commit's synchronous hook (the same reason G7's own gate already skips manual commits entirely), so this is the narrowest version judged to still add real signal, not a silent skip of the design goal. Wired into `.claude/git-hooks/pre-commit`'s `checkLegacyDisciplineGate` as `checkLegacyBiteBackstop`, which itself skips when `inAutoBuild` (G7 already re-verifies there) or `HARNESS_LEGACY_BITE_CHECK=off`. New coverage: `test/diff-hunks.test.js`, `test/legacy-discipline-relatedness.test.js`, and `test/legacy-bite-check.test.js` unit-test the three new pure/dependency-injected modules directly; `test/legacy-discipline-gate.test.js` adds Gap A and Gap B cases at the `checkLegacyDiscipline` and `run()` levels; `test/pre-commit-git-hook-legacy-discipline.test.js` adds real-git-diff, real-`component-map.md` integration cases for both gaps plus the bite-check escape hatch. Several pre-G29 tests that encoded the OLD, looser behavior (e.g., "any staged test file satisfies UNCOVERED evidence regardless of relatedness") were deliberately updated, with comments explaining why the old assertion no longer reflects correct behavior, rather than left to enforce the bug. **Known limitations, disclosed rather than silently accepted**: (1) a pure-deletion hunk (0 added lines) has no real "+" range to report, so `parseUnifiedDiffRanges` records a single-point range at the hunk's new-side anchor line — a conservative approximation of "something changed immediately adjacent to here", not an exact line-level proof for pure deletions; (2) the naming-heuristic and commit-wide-fallback relatedness tiers are real but weaker signals than component-map.md story ownership — a project with no component-map.md and an oddly-named pin-down test could still pass on the fallback tier (surfaced via `relatednessNotes`, not hidden); (3) the bite-check backstop's 3-mutant/10s budget is real signal but is intentionally narrower than G7's own 12-mutant/30s budget, and only fires for a manual (non-`/auto`) commit — an `/auto` commit still relies on G7's own, more thorough check.

- **G30** ✅ **done** — closes G17's own disclosed forward item: mechanically verifying `sprouting-instead-of-editing`'s Iron Law ("touch the legacy file at exactly one call line, or the rename pair for wrap") was never built — the skill's Process step 2 stated the constraint in prose only, with nothing checking a legacy diff's actual symbol span. `sprout-diff-gate.js` fires ONLY when both are true for a staged commit: (a) `legacy-discipline-gate.js` (G17/G29) already classifies a legacy file as UNCOVERED-with-evidence, and (b) that evidence is a **sprout**, not a **pin-down** — a genuinely NEW production file (staged, added — `diff-filter=A`, source, non-test) alongside the legacy touch, as opposed to a pin-down commit that only adds/modifies test files and has no one-symbol constraint of its own. A new pure classifier, `hooks/lib/sprout-classify.js`'s `classifySprout`, tells the two apart, reusing `hooks/lib/legacy-discipline-relatedness.js`'s `storyOwnersFor` (G29) for the same component-map.md-story-ownership-then-commit-wide-fallback tiering that file already established, rather than reinventing a second relatedness scheme. For each sprout-classified legacy file, `hooks/lib/sprout-symbol-check.js`'s `symbolsTouchedByRanges` checks the file's actually-changed line ranges (`legacy-discipline-gate.js`'s already-exported `gitDiffRanges`/`diff-hunks.js`, gap G29 — reused, not reimplemented) against `code-graph.json`'s per-file `symbols` records, flattening a class's methods (`children`) to leaf units exactly like `coverage_map.py`'s `symbol_rows` already does for the receipt check this composes with — a change inside one method counts once, against that method, never doubled against the method AND its enclosing class (a disclosed scoping choice: a class-level edit that falls between methods, outside every child's range, is invisible to this check — judged the better trade-off than the much more common false double-count it avoids). More than 2 distinct symbols touched BLOCKs, naming the extras — the cap of 2, not 1, is deliberate: `sprouting-instead-of-editing`'s own Process step 2 names a legitimate 2-symbol shape (the wrap-rename pair), and this gate does not attempt to mechanically distinguish a 1-symbol sprout call-line from a 2-symbol wrap pair (judged too heuristic to build with confidence from a diff alone); a 2-symbol touch passes but is annotated "assumed wrap-rename pair, not independently verified" so the uncertainty is visible, not hidden. Wired into `.claude/git-hooks/pre-commit` as `checkSproutDiffGate`, right after `checkLegacyDisciplineGate`, default-on (`HARNESS_SPROUT_DIFF_GATE=off` acknowledges a local skip, mirroring `HARNESS_LEGACY_DISCIPLINE_GATE=off`'s shape); a real exception belongs in `specs/reviews/sensor-waivers.json` per `docs/sensor-arbitration.md`. Degrades loudly, never silently, exactly mirroring `legacy-discipline-gate.js`'s own `noGraphVerdict`/`hasSymbolRecords` conventions: no `code-graph.json`, a regex-fallback graph with no per-file symbol records, no per-file symbol record for a specific candidate file, or unavailable changed-range data (`--files` CLI mode) all SKIP with a note rather than block; no UNCOVERED-with-evidence candidate at all, or one present but pin-down-shaped (no new production file), also SKIPs with a distinguishing note. New coverage: `test/sprout-symbol-check.test.js` and `test/sprout-classify.test.js` unit-test the two new pure modules directly; `test/sprout-diff-gate.test.js` covers the orchestrating `checkSproutDiff` core and `run()`/CLI wiring (`--staged` and `--files` modes); `test/pre-commit-git-hook-sprout-diff.test.js` is a real-git-diff, real-git-hook integration suite covering the no-candidate skip, pin-down skip, clean one-symbol pass, three-symbol BLOCK, two-symbol assumed-wrap-pair pass, the `HARNESS_SPROUT_DIFF_GATE=off` escape hatch, and the regex-fallback-graph degrade. `legacy-discipline-gate.js` gained two small additive exports (`resolveFileSets`, `readMapText`) so `sprout-diff-gate.js`'s own standalone `run()` can reuse its CLI-argument and component-map-reading logic rather than duplicating it; both functions' existing behavior and all pre-G30 tests are unchanged. **Known limitations, disclosed rather than silently accepted**: (1) this gate reads `code-graph.json` as-is, the same staleness assumption `legacy-discipline-gate.js`'s own receipt matching already accepts — if the graph was not regenerated after the sprout/wrap edit, its symbol ranges may not reflect the diff's true post-edit shape; (2) the sprout-vs-pin-down classification's "is the new production file actually THE sprout for THIS legacy edit" check is a commit-wide, not a per-line-attributed, signal at its weakest (commit-wide-fallback) tier — a project with **no `component-map.md` at all, or one that never mentions the legacy file itself**, could, in principle, have an unrelated new file staged in the same commit as a coincidental multi-symbol legacy edit and read as sprout-classified; this is the same class of weaker-tier trade-off G29 already discloses for its own relatedness fallback, not a new kind of risk. (A post-ship review fixed a symmetry bug in `sprout-classify.js`'s `componentMapSproutVerdict`: when the map DOES have an opinion on the legacy file but assigns the added file to a different story — or doesn't mention the added file at all — that is now a definitive non-sprout, component-map-tier verdict, mirroring `legacy-discipline-relatedness.js`'s own `componentMapVerdict` symmetry, rather than incorrectly falling through to this weaker commit-wide-fallback tier.) (3) the 2-symbol wrap-pair allowance is a cap, not a verified pattern match — this gate cannot mechanically confirm a 2-symbol touch is genuinely a rename pair (versus two unrelated small edits), and says so in its own PASS-path note rather than asserting more confidence than the check can support.

- **G31** ✅ **done** — G7 (mutation-gate), G17 (legacy-discipline), and G30 (sprout-diff) all prove a change didn't silently regress *production* code; none of them catch the adjacent failure mode a refactor or dependency bump can reach for under pressure instead — making its own suite pass by deleting, or newly skipping, the test that would have caught the regression, rather than fixing the code. (Prompted directly by an external case study, [Bun's Zig→Rust rewrite](https://bun.com/blog/bun-in-rust), which names exactly this discipline — "zero tests skipped or deleted" through a 1,448-file, LLM-assisted port — as load-bearing.) `hooks/lib/test-deletion-gate.js`'s `classifyTestFileChange` compares each staged test-shaped file's (`hooks/lib/tdd.js` `isTestFile`) old (`git show HEAD:<file>`) and new (`git show :<file>`, the staged index content) bodies with heuristic regex counts — the same tier `secrets.js`'s baseline scan and `tdd.js`'s own `isTestFile` already use, not an AST parse: a live-test-marker count (`it(`/`test(`/`it.each(`/`test.each(`/`def test_...`) that drops, or the deletion of a file that had live markers, is a `deleted`/`count-decreased` finding; an unchanged-or-higher count with a newly added skip/todo marker (`.skip(`/`.todo(`/`xit(`/`xdescribe(`/`xtest(`/`@pytest.mark.skip`/`@unittest.skip`) is an independent `new-skip` finding — a JS `it.skip(...)` unmatches the test-marker regex entirely (so it reads as `count-decreased`), while a Python `@pytest.mark.skip` decorator sits above an unchanged `def test_...` line (so it reads as `new-skip`); both are real, distinct signals, not a gap in the heuristic. `scripts/test-deletion-gate.js` does the git plumbing (`collectStagedChanges`, the same `--diff-filter=M`/`=D` dependency-injected-`exec` pattern `legacy-discipline-gate.js` and `sprout-diff-gate.js` already use) and exposes both a `--staged` CLI (`npm run test-deletion-gate`) and a `checkStaged(exec)` entry point the pre-commit hook calls directly. Wired into `.claude/git-hooks/pre-commit` as `checkTestDeletionGate`, deliberately called *before* the docs-only early exit: `stagedFiles()` filters `--diff-filter=ACMR`, which excludes pure deletions, so a commit that *only* deletes a test file would otherwise never reach a check gated behind that filter — the same reason `checkSecrets` and `checkAmendmentProvenance` already run there. Default-on hard block (`HARNESS_TEST_DELETION_GATE=off` acknowledges a local skip, mirroring `HARNESS_LEGACY_DISCIPLINE_GATE=off`'s shape); a real exception (a test for removed functionality, a flaky test being quarantined) belongs in `specs/reviews/sensor-waivers.json` (`sensor_id: test-deletion-guard`) per `docs/sensor-arbitration.md`. Degrades loudly, never silently, when the sensor script is missing/unloadable. New coverage: `test/test-deletion-gate.test.js` (pure classification), `test/test-deletion-gate-cli.test.js` (git-plumbing layer, dependency-injected `exec`), and `test/pre-commit-git-hook-test-deletion.test.js` (real git repo, real hook process — deletion, count-decrease, new-skip, clean-pass, the `HARNESS_TEST_DELETION_GATE=off` escape hatch, and the missing-script degrade). **Known limitations, disclosed rather than silently accepted**: (1) heuristic regex counting, not an AST parse, so unusual formatting or dynamically generated test cases (a loop emitting `it(...)` calls) can mis-count; (2) a test rewritten to cover different or broader behavior at the same or higher count is invisible to this gate — it proves the COUNT and SKIP STATE didn't regress, not that behavioral coverage is equivalent, which stays a `code-reviewer` / human judgment; (3) composes with, does not duplicate, `regression-suite-full` (G15) and `impact-scoped-regression` (G16), which prove a still-*present* test's outcome didn't regress, not that the test itself wasn't quietly removed or skipped.

- **G32** ✅ **done** — a prompt-level guide, also prompted by the Bun Zig→Rust case study above: their port was proved on 3 files before being run on all 1,448, catching the mechanical pattern's failure modes cheaply before paying for them at scale. `/refactor` Step 5 and `upgrading-dependencies` Step 6 previously applied a large mechanical fix (a `--sweep --auto-fix` batch, or a major dependency bump's call-site rewrite) across every affected file in one pass — a bad mechanical pattern was only discovered after touching all N files, forcing a bisect after the fact. Both skills now instruct: when the affected-file count is more than ~10, apply the fix to a small trial batch (3 files, or the smallest representative sample) first, run tests/lint/typecheck on that batch, and only then extend the same edit to the rest — skip the canary for smaller batches, where the overhead isn't worth it. Prompt-only, no computational sensor (the file-count trigger is judgment applied mid-task, not a mechanically checkable commit-time property); locked by `test/canary-rollout-wiring.test.js` asserting both skill files describe the canary step and its threshold, the same skill-text-assertion pattern `test/gate-reverify-wiring.test.js` already uses for a prompt-only feature with no runtime script (the /gate 3-instance re-verification pass, item 1 of the 2026-07-09 Devin-parity hardening).

- **G33** ✅ **done** — closes the last cheaply-portable idea from the Bun Zig→Rust case study above: Bun ran a *separate* workflow to resolve cyclic dependencies before mass-fixing their 16k compiler errors, because a cross-package import cycle re-opens errors in one shard by fixing another, and `fix-from-diagnostics` sharded straight by package with no cycle check — a cluster of errors sitting on a real cycle could burn all of Step 6's 3 full-queue passes without ever converging. `fix-from-diagnostics/SKILL.md` gained a new Step 2 "Cyclic-dependency pre-pass", inserted between Capture (Step 1) and Build-the-work-queue (now Step 3, with Parallel safety/Fix each shard/Close the queue/Process learning renumbered 4-7 accordingly): when the raw capture spans ≥3 packages, check whether the error-dense packages sit together in a known import cycle (`specs/brownfield/modularity-pack.md`, falling back to `code-graph.json`'s `cycles` field); if so, run a structural pass to break the cycle first — smallest cut, validated by a targeted build/typecheck, not the full suite — then re-capture diagnostics and re-shard before resuming normal per-shard fixing. Prompt-only, no computational sensor, same shape as G32 (the "≥3 packages" / "error-dense" thresholds are judgment applied mid-task, not a mechanically checkable commit-time property); locked by `test/cyclic-prepass-wiring.test.js` asserting the skill describes the pre-pass step, its package-count threshold, both cycle-data sources, and the re-capture/re-shard sequence, the same skill-text-assertion pattern `test/canary-rollout-wiring.test.js` and `test/gate-reverify-wiring.test.js` already use for prompt-only features with no runtime script.

- **G34** ✅ **done** — prompted by Boris Cherny's "convert domain knowledge to infrastructure" post: the harness had deep *structure* sensors (G1–G33) but no external-boundary test-double infrastructure, so isolating a DB/HTTP/LLM call in a test was hand-rolled by the agent every time (the "fix it each time it happens" anti-pattern), and the regression gates (G15/G16) ran the app with *live* externals and exact-match assertions — structurally flaky for LLM-backed apps, with a name-based, no-expiry quarantine as the only escape. G34 ships the **boundary-test-doubles kit** as real, registered infrastructure under `.claude/templates/boundary-doubles/`: an HTTP `ReplayTransport` (record once → replay a golden fixture at the wrapper's `_call` seam), a deterministic `FakeLLMClient` (golden structured responses keyed by a stable request hash — the sharpest previously-missing piece, since code-gen had strong LLM *runtime* guidance but zero LLM *test-stubbing* guidance), a transactional-rollback `db_session` fixture (keeps a real engine per `test-strategy.md` but makes it deterministic — resolving that file's contradiction with `tests-python.md`), a `conftest.py` binding them under `HARNESS_TEST_REPLAY=1`, and a Ports-and-Adapters `at-template.py` so `writing-acceptance-tests-first` stops delegating the first AT to the human from scratch. `/test` Step 4.7 generates `tests/integration/` against the kit; a real python record→replay round-trip test proves determinism. Registered as the `boundary-test-doubles` guide in `harness-manifest.json`. **Python/FastAPI + TypeScript/React both ship** (the `.ts` siblings — `replay-transport.ts`, `fake-llm.ts`, `db-fixture.ts`, `msw-handlers.ts`, `vitest.setup.ts`, `at-template.test.ts` — mirror the Python kit, proven by a real `tsx` record→replay round-trip; G36 now also scans co-located `*.test.tsx?` and honors a per-line `harness:live-ok` marker). Go and full integration-suite auto-synthesis remain a later gap. **Scaffold-encoding extension** (Boris-thesis follow-through): `/scaffold` now emits *encoded* project artifacts — `scaffold-encoding.js` injects a project-specific "This Project" block (import hierarchy, bounded contexts, sensor tier, topology, SLO, domain vertical) into the generated `CLAUDE.md`, and generates a `REVIEW.md` review policy that the `code-reviewer` agent reads and enforces — so a new contributor's change is reviewed against project conventions with zero extra prompting.

- **G36** ✅ **done** — the enforcement half that makes G34 stick (a shipped kit nobody is forced to use rots). Hybrid `live-externals` sensor. **Commit half** (`live-externals-gate.js`, hard-block, standard+): heuristically scans staged `tests/integration`/`e2e` files for a non-localhost http(s) URL, a real DB DSN, or a directly-constructed SDK client (`Anthropic(`/`OpenAI(`) and BLOCKs with an agent-actionable fix line pointing at the kit. Wired via a dedicated `hooks/lib/gates-live-externals.js` wrapper (kept out of `gates-early.js`, which carries pre-existing grandfathered >30-line functions that would otherwise force unrelated edits — flagged as standing harness debt), `runsWithoutSource:true` so it fires on a test-only commit like `test-deletion-guard`. **Runtime half**: the regression gates force `HARNESS_TEST_REPLAY=1` and treat a missing fixture (`MissingFixtureError`/`GoldenNotFoundError`) as a live-external reach — a hard fail. This is a portable "deterministic by construction" check needing no OS-level network jail; an OS-level outbound-network block is a documented later hardening. **Known limitations (disclosed):** the lint is heuristic regex, not an AST parse (dynamically constructed URLs/clients can be missed); the wrapper-honors-the-flag check is enforced at runtime, not at lint time; v1 is Python-backend-only. (G35 — the `/test` integration-generation step — was folded into G34's v1 rather than minted as a separate gap.)

- **G39** ✅ **done** — **evidence-integrity gate.** Prompted by Cognition's write-up of Devin's own computer-use testing ([Verifying Agentic Development at Scale](https://cognition.com/blog/testing-development)), which names *"models lean too heavily on executing JavaScript … instead of clicking through the UI"* as a live open problem. The audit found the same hole here, unfenced: the `evaluator` agent holds `browser_evaluate` (it needs it to inject axe-core), and the tool appeared nowhere else in the prompt surface — so a functional `playwright_checks` assertion could be satisfied by reading state or firing a handler in JS, silently voiding the evaluator's own black-box premise ("the running app produces the evidence; you never read source to decide it looks right"). Emission first: the evaluator now writes `specs/reviews/evaluator-evidence.json` — one entry per browser check with the contract's check `id`, `layer` (`playwright`/`accessibility`), `verdict` (`pass`/`fail`/`untested`), the `interactions` actually used, `matrix_ids`, and `artifacts` (G40). `hooks/lib/evidence-integrity.js` then cross-checks that ledger against the **read-only** sprint contract; `scripts/evidence-integrity-gate.js` does the fs plumbing and writes `evidence-integrity-verdict.json`. Five BLOCK findings — `js-bypass`, `no-interaction-pass`, `missing-evidence`, `undeclared-check`, `unbacked-axe` — plus `missing-ledger`/`group-mismatch`/`missing-contract` as fail-loud input errors (a gate with nothing to read is never a pass). Wired as a `verification`-pack registry check (`exists:sprint-contracts`, blocking), folded into `/evaluate` as `failure_layer: "evidence"`, and aggregated by `quality-card.js`. Recording an unexecutable check as `untested` instead of omitting it is what lets the gate tell *not attempted* from *verified*; the untested list is reported but never a finding, because "never report a layer as passed that you could not execute" is already the evaluator's own rule (one owner per invariant). **Known limitation (disclosed):** `missing-evidence`, `undeclared-check` and `no-interaction-pass` are arithmetic against the pre-committed contract and hold however honestly the evaluator describes its method; `js-bypass` and `unbacked-axe` read a self-declared interaction list. That is deliberate parity with Cognition's own control (commit to the expectation before acting, so an unexpected result is harder to rationalise as a pass) — it catches the rationalising evaluator, not a determined one. Tamper-proof enforcement needs a PostToolUse tap on the MCP tool, which means a `.claude/settings.json` matcher — inter-session work behind `HARNESS_PREFIX_EDIT=1`, deliberately not applied mid-session:
  ```json
  { "matcher": "mcp__plugin_playwright_playwright__browser_evaluate",
    "hooks": [{ "type": "command", "command": "node \"$CLAUDE_PROJECT_DIR/.claude/hooks/record-run.js\"", "timeout": 5000 }] }
  ```

- **G40** ✅ **done** — **runtime evidence artifacts.** The other half of the same Cognition finding: their real insight is that verification output is a *human-review artifact* (chaptered video, chronological assertion list, dead time compressed), because a verdict nobody can cheaply re-check gets rubber-stamped. The harness's text receipts (`walkthrough.md`, `quality-card.md`) were already Devin-Review-class, but the *runtime* layer left only prose plus a few design-critic screenshots — and the generated `playwright.config.template.ts` set `trace: 'on-first-retry'` with `retries: 0` locally, so it captured nothing on exactly the runs an engineer debugs. The template now retains `trace`/`video` on failure with `screenshot: 'only-on-failure'` into `test-results/`, and the evaluator cites those paths in the `artifacts` field of its structured failure report and its evidence-ledger entries. No new control (this is emission, not a gate) — it feeds G39's ledger and the comprehension-debt sensor.

## Harness coverage (G11)

`harness-coverage.js` (`npm run harness-coverage`) makes this registry measurable: it maps each source file in a project's `code-graph.json` against the active sensors' `scope` field and reports per-axis coverage % + the ungoverned holes (files with no scoped sensor on an axis). Runtime / dependency / artifact / repo-wide sensors are reported separately. Report-only; run it on a cadence via `/schedule`.

## Agent readiness (G21)

`agent-readiness.js` (`npm run agent-readiness`, or `/agent-readiness`) answers a different question than the coverage report above: not *what governs what*, but *is this codebase actually ready to hand to a heavy-autonomy agent?* — loosely inspired by Factory.ai's "agent readiness" framing (8 pillars × maturity levels), adapted to what this harness tracks. It reads state its OWN prior sensors and gates already produce — Style & Validation, Architecture Fitness (G8/G18 ratchet baselines), Testing (coverage ratchet + G7/G15/G16 gates + G20 acceptance-test adoption), Code Quality / Modularity Freshness (G19), Documentation / Navigation, Observability (G9), Security & Governance (G3), and Dev Environment — and reports each pillar as `active`/`partial`/`planned` (the same vocabulary `harness-manifest.json#model.statuses` defines) with a one-line remediation when not `active`. It computes no new measurements: every pillar reuses an existing detection primitive (`hooks/lib/toolchain.js`'s run/skipped, `hooks/lib/drift.js`'s `withModularityStaleness`, `hooks/lib/stale-stamp.js`'s `STALE_MARK`) rather than reinventing one. Writes `specs/reviews/agent-readiness.md`/`.json`; report-only, exit 0 always, same convention as `harness-coverage.js` above — and, like it, not registered in `harness-manifest.json`'s `guides[]`/`sensors[]` arrays, since it inspects/reports on existing sensor and codebase state rather than governing code directly. Run it on a cadence via `/schedule`, or on demand before starting heavy autonomous work on a codebase.

## Complexity dial (`sensor_tier`)

Commit-time (and, as the registry lands, related) sensors are filtered by **`project-manifest.json#quality.sensor_tier`**: `minimal` · `standard` (default) · `strict`. This is the operability dial so teams do not memorize a dozen `HARNESS_*_GATE=off` env vars. Env vars remain local escape hatches only.

Product install boundaries (what you copy/install) are separate: **harness-lite / harness-core / harness-full** — see [`docs/product-skus-and-tiers.md`](docs/product-skus-and-tiers.md). This monorepo dogfoods itself as Project Zero via the root `project-manifest.json` and `.claude/state/agent-readiness-baseline.json`, with `quality.agent_readiness.mode: "ratchet"` enforced in CI (`npm run agent-readiness:assert`).

## How to extend the harness

When you add a guide or sensor, register it here so it joins the loop instead of rotting:

1. **Build the control** — a hook check, a script, a skill, or a reviewer agent.
2. **Wire it to a cadence** — session (a `verify-on-save`/`pre-write-gate` check), commit (`git-hooks/pre-commit` or `/gate`), integration (the evaluator), or drift (a scheduled job).
3. **Register it** — add an entry to `harness-manifest.json` (`guides[]` or `sensors[]`) with a real `wired_at` path, its `axis`, `cadence`, `type`, and `status`. If it closes a gap, set the `gap_ref`. Declare which **sensor_tier** values include it (see [`docs/product-skus-and-tiers.md`](docs/product-skus-and-tiers.md)).
4. **Declare the blocking level** — classify the sensor as `hard-block`, `self-correct`, `review-focus`, or `advisory` using [`docs/sensor-arbitration.md`](docs/sensor-arbitration.md). If it can be waived, state the evidence required in `specs/reviews/sensor-waivers.json` (schema: `.claude/templates/sensor-waivers.schema.json`) and the expiry rule.
5. **Make the signal LLM-legible** — emit a message that tells the agent how to self-correct, not just that it failed (the highest-leverage sensor technique; see G5).
6. **Update the matrix above** so the human view stays in sync with the manifest.

> Keep `harness-manifest.json` honest: every `active`/`partial` entry must point at a file that exists. A drift between this registry and reality is itself a harness failure.
