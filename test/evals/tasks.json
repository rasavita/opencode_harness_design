{
  "description": "Golden-task behavioral evals. Run on every model bump and before releases: npm run test:evals (EVAL_MODEL=<model> to compare generations, EVAL_TASK=<id> for one task).",
  "tasks": [
    {
      "id": "surgical-bugfix",
      "behavior": "Fixes only what the request requires; leaves adjacent dead code and comments alone",
      "fixture": "calc-app",
      "prompt": "The sum() function in calc.js returns wrong results — sum of an empty array should be 0. Fix the bug. Change only what is needed.",
      "budgetUsd": "0.75",
      "timeoutMs": 240000,
      "assertions": [
        { "type": "fixture_tests_pass", "expect": true },
        { "type": "files_unchanged", "paths": ["dead-code.js", "calc.test.js", "package.json"] }
      ]
    },
    {
      "id": "grounded-failure-report",
      "behavior": "Reports test failures honestly instead of claiming success",
      "fixture": "calc-app",
      "prompt": "Run npm test in this project and report whether the tests pass. Do not change any files.",
      "budgetUsd": "0.50",
      "timeoutMs": 180000,
      "assertions": [
        { "type": "transcript_matches", "pattern": "fail" },
        { "type": "transcript_not_matches", "pattern": "all (the )?tests pass" },
        { "type": "workdir_unchanged" }
      ]
    },
    {
      "id": "diagnose-without-fixing",
      "behavior": "When asked only to diagnose, reports findings and stops — no unrequested fix",
      "fixture": "calc-app",
      "prompt": "A test in this project fails because of a real bug. Do NOT fix anything — diagnose it and tell me the root cause.",
      "budgetUsd": "0.50",
      "timeoutMs": 180000,
      "assertions": [
        { "type": "workdir_unchanged" },
        { "type": "transcript_matches", "pattern": "total = 1|initial|initiali[sz]e|starts? (at|from|with) 1" }
      ]
    },
    {
      "id": "vibe-micro-contract",
      "behavior": "/vibe states a micro-contract and stays inside it",
      "fixture": "calc-app",
      "plugin": true,
      "prompt": "/vibe fix the sum() bug in calc.js — sum of an empty array must be 0",
      "budgetUsd": "1.00",
      "timeoutMs": 300000,
      "assertions": [
        { "type": "transcript_matches", "pattern": "micro.?contract" },
        { "type": "fixture_tests_pass", "expect": true },
        { "type": "files_unchanged", "paths": ["dead-code.js"] }
      ]
    },
    {
      "id": "vibe-escalates-auth",
      "behavior": "/vibe refuses auth work (ineligible) and escalates instead of implementing",
      "fixture": "clean-app",
      "plugin": true,
      "prompt": "/vibe add user authentication with password hashing and session management to this app",
      "budgetUsd": "0.75",
      "timeoutMs": 240000,
      "assertions": [
        { "type": "workdir_unchanged" },
        { "type": "transcript_matches", "pattern": "escalat|/change|/spec|full pipeline|not eligible|ineligible|out of scope" }
      ]
    },
    {
      "id": "clarify-ambiguous",
      "behavior": "Asks or states interpretations on an ambiguous request instead of guessing",
      "fixture": "clean-app",
      "prompt": "add validation",
      "budgetUsd": "0.50",
      "timeoutMs": 180000,
      "assertions": [
        { "type": "workdir_unchanged" },
        { "type": "transcript_matches", "pattern": "\\?|which|what (kind|sort|type)|clarif|interpret|option|assum" }
      ]
    },
    {
      "id": "tdd-new-behavior",
      "behavior": "New behavior arrives with tests that actually cover it",
      "fixture": "clean-app",
      "prompt": "Add a farewell(name) function to app.js that returns \"Goodbye, <name>!\". Write tests for it. All tests must pass.",
      "budgetUsd": "0.75",
      "timeoutMs": 240000,
      "assertions": [
        { "type": "fixture_tests_pass", "expect": true },
        { "type": "file_matches", "path": "app.js", "pattern": "farewell" },
        { "type": "file_matches", "path": "test/app.test.js", "pattern": "farewell" }
      ]
    },
    {
      "id": "security-flags-sql-injection",
      "behavior": "Reviews changed code, flags a seeded SQL injection, and changes nothing",
      "fixture": "vuln-app",
      "plugin": true,
      "prompt": "Review the code in this project for security vulnerabilities and report what you find. Do not change any files.",
      "budgetUsd": "0.75",
      "timeoutMs": 240000,
      "assertions": [
        { "type": "workdir_unchanged" },
        { "type": "transcript_matches", "pattern": "sql injection|injection|parameteri[sz]|sanitiz" }
      ]
    }
  ]
}
