{
  "version": "1",
  "purpose": "Registry of the harness control system: guides (feedforward) and sensors (feedback) across the quality axes, plus the cadence each runs at and whether it is active, partial, or planned. This file is the machine-readable companion to HARNESS.md and the single source of truth for 'what governs what' and 'where the holes are'. Keep it faithful to reality: every active/partial entry must point at a real wired_at path.",
  "model": {
    "definition": "Agent = Model + Harness. The harness is everything around the model: guides that steer it before it acts, sensors that detect after it acts, and a human steering loop that improves both over time.",
    "axes": {
      "maintainability": "Internal quality — keep the codebase easy and low-risk to change.",
      "architecture": "Architecture fitness — structural constraints, layering, module boundaries, performance budgets.",
      "behaviour": "Functional correctness — does it do what was asked, verified against a running system.",
      "traceability": "Harness extension beyond the source articles: deterministic grounding of every artifact against its upstream (FRD -> BRD -> spec -> test). This is a strength we have that the articles' examples lack."
    },
    "kinds": {
      "feedforward": "A guide. Prevents or steers behaviour before the agent acts.",
      "computational": "A sensor that is deterministic, fast, CPU-based (linters, type checkers, structural rules, tests).",
      "inferential": "A sensor that uses an LLM for semantic judgement (review agents). Slower, probabilistic, semantically rich.",
      "hybrid": "A sensor that combines a deterministic runner with inferential interpretation."
    },
    "cadences": {
      "planning": "Runs during BRD/spec/design, before code exists.",
      "session": "Runs inside the agent's inner loop, on every write (PostToolUse / PreToolUse hooks). Fast self-correction.",
      "commit": "Runs at commit time (git pre-commit hook) or in the on-demand /gate.",
      "integration": "Runs against a running application (the evaluator / /evaluate) or in CI.",
      "drift": "Runs repeatedly on a slower cadence, OUTSIDE the change lifecycle, to catch accumulated decay. This column is mostly empty today (gap G2)."
    },
    "statuses": {
      "active": "Wired and enforced.",
      "partial": "Exists but limited in scope, opt-in, or report-only rather than enforced.",
      "planned": "Identified in the gap analysis, not yet built. gap_ref points at the gap id."
    },
    "sensor_tiers": {
      "minimal": "Secrets + structural basics when configured; low-ceremony CLI/library work.",
      "standard": "Default product posture — historical pre-commit set (including sprout-diff).",
      "strict": "Standard plus architecture ratchets at commit (cycle-detection, coupling-ratchet, duplication-ratchet)."
    },
    "sensor_tier_loader": ".claude/hooks/lib/sensor-tier.js",
    "gate_registry": ".claude/hooks/lib/gate-registry.js"
  },
  "steering_loop": [
    {
      "id": "program",
      "name": "program.md",
      "wired_at": ".claude/program.md",
      "description": "Karpathy human<->agent bridge; the steering input that biases /auto."
    },
    {
      "id": "learned-rules",
      "name": "learned-rules.md",
      "wired_at": ".claude/state/learned-rules.md",
      "description": "Monotonically growing failure-derived rules, injected into future agent prompts. The harness improving itself."
    },
    {
      "id": "process-rules",
      "name": "process-rules.md",
      "wired_at": ".claude/state/process-rules.md",
      "description": "Monotonic workflow constraints (destructive git bans, no stub-to-green, canary requirements) — distinct from code-style learned-rules. Injected into implement/auto/change. Bun Phase A: fix the process that generates the code."
    },
    {
      "id": "review-on-stop-suggestions",
      "name": "CLAUDE.md update suggestions",
      "wired_at": ".claude/hooks/review-on-stop.js",
      "description": "Stop hook surfaces session learnings as suggested CLAUDE.md edits (applied between sessions, never mid-run)."
    }
  ],
  "guides": [
    {
      "id": "code-gen",
      "axis": "maintainability",
      "kind": "feedforward",
      "wired_at": ".claude/skills/code-gen/SKILL.md",
      "description": "10 quality principles injected into every generator teammate: TDD, static typing, 30-line function cap, deep modules, explicit errors, latency budgets. Phase A also: no stub-to-green + paragraph-workaround rule (Bun)."
    },
    {
      "id": "adversarial-code-review",
      "axis": "behaviour",
      "kind": "feedforward",
      "wired_at": ".claude/scripts/review-tier.js",
      "status": "active",
      "description": "Tiered dual adversarial code review (Bun Phase A): review-tier.js resolves standard vs adversarial; adversarial spawns two independent code-reviewer instances and merge-review-verdicts.js (default union). Wired into /implement Step 7, /auto Gate 8, /change S6/I8. Defaults: review.adversarial=auto, min 8 files / 200 lines, or security-boundary / sensor_tier=strict."
    },
    {
      "id": "git-safety-parallel",
      "axis": "behaviour",
      "kind": "feedforward",
      "wired_at": ".claude/hooks/lib/git-safety.js",
      "status": "active",
      "description": "Multi-agent git safety (Bun Phase A): deny git stash / reset --hard / clean -fd / force-push while HARNESS_PARALLEL_AGENTS=1 or parallel-implement.lock exists. Pre-bash-gate enforces; SECTION 4 teammate prompts restate. Escape: HARNESS_GIT_SAFETY=off."
    },
    {
      "id": "fix-from-diagnostics",
      "axis": "behaviour",
      "kind": "feedforward",
      "wired_at": ".claude/skills/fix-from-diagnostics/SKILL.md",
      "status": "active",
      "description": "Bun Phase B diagnostics work queue: parse tsc/eslint/ruff/mypy → diagnostics-shard.js → fix per package shard without full monorepo suite mid-queue. Invoked from /implement Step 6 and /auto SECTION 6 when lint/type volume is high (≥~15 findings)."
    },
    {
      "id": "mechanical-migrate",
      "axis": "behaviour",
      "kind": "feedforward",
      "wired_at": ".claude/skills/refactor/SKILL.md",
      "status": "active",
      "description": "Bun Phase B mechanical migrate via /refactor --mechanical and .claude/templates/migrate/ (MAPPING.md, CONSTRAINTS.tsv, CANARY.md). Mapping dual-review + mandatory 3-file canary before fan-out."
    },
    {
      "id": "canary-before-fanout",
      "axis": "behaviour",
      "kind": "feedforward",
      "wired_at": ".claude/skills/refactor/SKILL.md",
      "status": "active",
      "gap_ref": "G32",
      "net_add_justification": "Merges the former canary-first-rollout + canary-implement-feature (v6-reduction, one-owner-per-invariant): both were the same G32 discipline registered twice for different call sites. Now one owner across all four sites: /refactor Step 5 (--sweep/--auto-fix), /upgrading-dependencies Step 6 (call-site rewrites), /implement Step 0.5 (large/mechanical group), /feature (first ready story as epic canary). Distinct from mechanical-migrate (the broader Bun-Phase-B migrate feature, whose own 3-file canary is one instance of this discipline).",
      "description": "Prompt-level guide (gap G32): before applying one mechanical change across many files or stories, prove the pattern on a small trial batch (3 files, or the smallest representative sample; threshold ~10 files) and run tests/lint/typecheck on it first; a canary failure revises the pattern rather than forcing a post-fan-out bisect. Wired at /refactor Step 5, /upgrading-dependencies Step 6, /implement Step 0.5, and /feature (first ready story as epic canary). Prompt-only, no computational sensor: the file-count trigger is judgment the agent applies mid-task, and a canary failure is caught by the same test/lint/typecheck gates every step already runs."
    },
    {
      "id": "semantic-divergence",
      "axis": "behaviour",
      "kind": "feedforward",
      "wired_at": ".claude/skills/code-gen/references/semantic-divergence.md",
      "status": "active",
      "description": "Bun Phase C: checklist for mechanical ports (assert side effects, rounding, bounds, Drop/defer, placeholders). code-reviewer applies when specs/migrate or /refactor --mechanical is in scope."
    },
    {
      "id": "review-commit-attribution",
      "axis": "traceability",
      "kind": "feedforward",
      "wired_at": ".claude/scripts/review-commit-msg.js",
      "status": "active",
      "description": "Bun Phase C: optional commit subject formatter that attributes dual-review fixes (review:…; policy=union). adversarial-review-audit.json remains the machine source of truth."
    },
    {
      "id": "fix-diagnostics-workflow",
      "axis": "behaviour",
      "kind": "feedforward",
      "wired_at": ".claude/workflows/fix-diagnostics.js",
      "status": "active",
      "description": "Bun Phase C dynamic workflow exemplar: multi-phase diagnostics work queue. Not a clone of /gate or /implement; skill fix-from-diagnostics remains the primary single-agent form. Teaches 'edit the workflow when agents misbehave'."
    },
    {
      "id": "clarify",
      "axis": "maintainability",
      "kind": "feedforward",
      "wired_at": ".claude/skills/clarify/SKILL.md",
      "description": "Bounded clarification gate — resolve ambiguity before code, consuming local context first."
    },
    {
      "id": "architecture-doc",
      "axis": "architecture",
      "kind": "feedforward",
      "wired_at": ".claude/architecture.md",
      "description": "Architecture reference copied into target projects."
    },
    {
      "id": "layer-config",
      "axis": "architecture",
      "kind": "feedforward",
      "wired_at": ".claude/hooks/lib/layers.js",
      "description": "Default layer order + roots that the layer sensor enforces; overridden per target project via project-manifest.json#architecture (opt-out for non-layered shapes)."
    },
    {
      "id": "brd-spec-design",
      "axis": "behaviour",
      "kind": "feedforward",
      "wired_at": ".claude/skills/design/SKILL.md",
      "description": "BRD -> spec -> design artifacts + acceptance criteria + sprint contracts: the functional specification the agent builds against."
    },
    {
      "id": "reasons-canvas",
      "axis": "behaviour",
      "kind": "feedforward",
      "wired_at": ".claude/skills/design/references/reasons-canvas-template.md",
      "status": "active",
      "description": "SPDD REASONS Canvas (gap G4): /design emits specs/design/reasons-canvas.md (Requirements/Entities/Approach/Structure/Operations/Norms/Safeguards + a machine-read Governs list). Living artifact — 'fix the prompt first' when behaviour changes; brownfield grounds entities existing-vs-new in the code-graph."
    },
    {
      "id": "legacy-discipline",
      "axis": "behaviour",
      "kind": "feedforward",
      "wired_at": ".claude/skills/checking-coverage-before-change/SKILL.md",
      "description": "Behaviour-preservation skills (checking-coverage, pinning-down-behavior, sprouting, keeping-refactors-pure) that steer brownfield edits."
    },
    {
      "id": "acceptance-tests-first",
      "axis": "behaviour",
      "kind": "feedforward",
      "wired_at": ".claude/skills/writing-acceptance-tests-first/SKILL.md",
      "status": "active",
      "gap_ref": "G20",
      "description": "AT-first discipline (gap G20, Matteo Vaccari's 'Acceptance Tests for AI-Assisted Development'): before implementation on a story, writes a business-readable Given/When/Then acceptance test against a Ports-and-Adapters seam — reusing /seam-finder's observable/funnel/asymmetry scoring rather than inventing a new seam concept — with a test-double adapter standing in for I/O, confirms it fails for the right reason, then proceeds to implementation. Wired into /test (Step 4.6, part of the plan-phase deliverable) and /change Step S4. Additive to, not a replacement for, the existing Playwright E2E layer (/test Step 5), which remains the final full-stack confirmation layer — this closes the harness's structural instance of Vaccari's E2E-as-AT-implementation antipattern (E2E was previously the ONLY acceptance-criteria verification layer). As of gap G23, this discipline has real commit-time mechanical proof (at-first-gate.js + record-at-red.js, mirroring legacy-discipline-gate.js's G17 pattern): a story's new production files are blocked without both an AT file on disk and a receipt proving it ran red. Readability review remains prompt-level only (surfaced to code-reviewer at Step S6, not mechanically checked) — see HARNESS.md G20/G23 for the current, narrower disclosed scope."
    },
    {
      "id": "observability-conventions",
      "axis": "architecture",
      "kind": "feedforward",
      "wired_at": ".claude/skills/code-gen/references/observability-conventions.md",
      "status": "active",
      "gap_ref": "G9",
      "description": "App-level observability baseline (gap G9, guide-half): the generator emits a RED-metrics + /metrics + trace-id-log-correlation baseline INTO generated server apps, steered by observability-conventions.md (stack-neutral) + observability-python-fastapi.md (concrete). Default-on for server shapes via project-manifest.json#observability (scaffold-render.js); deploy adds Prometheus scrape discovery; /metrics is a verifiable acceptance criterion. The runtime-SLO drift sensor that reads these signals is a separate follow-on."
    },
    {
      "id": "topology-templates",
      "axis": "architecture",
      "kind": "feedforward",
      "wired_at": ".claude/scripts/topologies.js",
      "status": "active",
      "gap_ref": "G10",
      "description": "Per-topology harness templates (gap G10): /scaffold resolves a detected topology (web-app / api-service / cli-or-library) and presets a coherent bundle of existing manifest knobs (architecture, observability, verification, ceremony, model_tier) via the TOPOLOGIES registry, instead of a uniform default. Ashby's-Law variety reduction; drop-in extensible; the manifest stays per-project overridable."
    },
    {
      "id": "token-governor",
      "axis": "traceability",
      "kind": "feedforward",
      "wired_at": ".claude/scripts/navigation-refresh.js",
      "status": "active",
      "description": "Default-on advisory token governor: /scaffold creates placeholder living navigation for empty greenfield repos or a lean initial DeepWiki/code-map for source-bearing repos; agents are steered to wiki/symbol-map/context queries before broad source reads. Configured by project-manifest.json#token_governor."
    },
    {
      "id": "architecture-constitution",
      "axis": "architecture",
      "kind": "feedforward",
      "wired_at": ".claude/templates/constitution-template.md",
      "status": "active",
      "description": "Cross-sprint architecture invariants (sprint-delta lane): a human-owned, PR-reviewed file scaffolded into every project at specs/design/constitution.md. /design --delta's design-delta rubric treats every '## Invariants' line as a hard constraint on every future sprint's design amendment."
    },
    {
      "id": "context-pack",
      "axis": "traceability",
      "kind": "feedforward",
      "wired_at": ".claude/scripts/context-pack.js",
      "status": "active",
      "description": "Bounded source-context retrieval (schema v2) over the living DeepWiki/code-map: lexical+wiki BM25-ish ranking, optional TF-IDF semantic index (.claude/state/nav-index), depth-2 graph neighbors, co-change expansion, optional --diff boost, impact-scoped tests_to_run, task_map + confidence, read_next citations, session receipt, and nav-telemetry.jsonl. Facade: nav-query.js; MCP: nav-mcp-server.js; change-family Iron Law requires pack before broad source reads."
    },
    {
      "id": "nav-query-facade",
      "axis": "traceability",
      "kind": "feedforward",
      "wired_at": ".claude/scripts/nav-query.js",
      "status": "active",
      "description": "Single CLI facade for pack/symbol/callers/impact/cochange/semantic/refresh over code-graph + secondary nav artifacts. MCP twin: nav-mcp-server.js."
    },
    {
      "id": "nav-index-semantic",
      "axis": "traceability",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/nav-index.js",
      "signal": "stale or missing TF-IDF nav index after graph refresh",
      "description": "Offline TF-IDF semantic index over symbols+wiki; rebuilt by graph-refresh / nav-query refresh; fail-open when missing. Feeds context-pack scoring."
    },
    {
      "id": "nav-cochange",
      "axis": "traceability",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/nav-cochange.js",
      "signal": "files frequently committed together",
      "description": "Git co-change edges (specs/brownfield/co-change.json); expands context-pack neighbors beyond import graph."
    },
    {
      "id": "nav-concepts",
      "axis": "traceability",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/nav-concepts.js",
      "signal": "stale concept pages when member hashes change",
      "description": "Hash-cached deterministic concept pages under specs/brownfield/wiki/concepts/, steered by .harness/wiki.json."
    },
    {
      "id": "nav-brownfield-maps",
      "axis": "traceability",
      "kind": "feedforward",
      "wired_at": ".claude/scripts/nav-brownfield-maps.js",
      "status": "active",
      "description": "Deterministic lean brownfield essays (codebase/architecture/test/risk/change-strategy) from code-graph — default /brownfield path skips LLM essays; --full for narrative enrichment."
    },
    {
      "id": "nav-graph-index",
      "axis": "traceability",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/nav-graph-index.js",
      "signal": "stale inverted symbol/path index",
      "description": "Pure-JSON inverted index for O(1) symbol/caller lookup without re-walking the full graph; rebuilt on graph-refresh."
    },
    {
      "id": "nav-bench",
      "axis": "traceability",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/nav-bench.js",
      "signal": "golden-query recall_rate and pack vs naive token estimates",
      "description": "Golden navigation benchmark (test/fixtures/nav-bench/golden-queries.json); measures pack recall and estimated token savings."
    },
    {
      "id": "tool-output-pack",
      "axis": "traceability",
      "kind": "feedforward",
      "wired_at": ".claude/scripts/tool-output-pack.js",
      "status": "active",
      "description": "Standalone tool-output compactor: preserves raw logs on disk, extracts failures/paths/lines, and emits raw/packed/saved token estimates. Hook integration remains future work."
    },
    {
      "id": "local-ccr",
      "axis": "traceability",
      "kind": "feedforward",
      "wired_at": ".claude/scripts/context-store.js",
      "status": "active",
      "description": "Local Compress-Cache-Retrieve layer: stores raw command/search context by hash, emits compact packs with retrieval commands, and supports query-filtered raw retrieval through context-retrieve.js, run-compact.js, and search-compact.js."
    },
    {
      "id": "first-window-init",
      "axis": "behaviour",
      "kind": "feedforward",
      "wired_at": ".claude/skills/auto/SKILL.md",
      "status": "active",
      "gap_ref": "G13",
      "description": "First-context-window vs continuation split (gap G13): on the first /auto window (Session 0 only, no completed groups) the loop verifies the initializer's artifacts are coherent (populated features.json, executable init.sh, specs prerequisites) before building, instead of running identical recovery logic on window 1 and window N. Implements Anthropic's multi-context-window 'a different prompt for the very first context window' principle."
    },
    {
      "id": "pr-respond",
      "axis": "behaviour",
      "kind": "feedforward",
      "wired_at": ".claude/skills/pr-respond/SKILL.md",
      "status": "active",
      "description": "Bounded post-PR response loop: /pr-respond polls a harness-opened PR's CI checks and review comments, classifies failures via the /auto self-healing table, fixes/pushes/replies with evidence, and stops on clean/max-cycles/watch-window/budget. Opt-in from /build and /feature via --respond (default off); never merges or force-pushes — merge stays human-owned. The deterministic half (poll/diff/handled-state) lives in .claude/scripts/pr-poll.js."
    },
    {
      "id": "sensor-arbitration-policy",
      "axis": "traceability",
      "kind": "feedforward",
      "wired_at": "docs/sensor-arbitration.md",
      "status": "active",
      "description": "Guide for resolving conflicts between sensors and recording reviewed waivers with explicit expiry, so a larger harness stays coherent instead of accumulating contradictory gates."
    },
    {
      "id": "boundary-test-doubles",
      "axis": "behaviour",
      "kind": "feedforward",
      "wired_at": ".claude/templates/boundary-doubles/replay_transport.py",
      "status": "active",
      "gap_ref": "G34",
      "description": "Shipped record/replay test-double kit (DB transactional-rollback fixture, HTTP ReplayTransport at the wrapper _call seam, deterministic FakeLLMClient with golden structured responses keyed by request hash) bound under HARNESS_TEST_REPLAY=1, plus a Ports-and-Adapters at-template.py. Converts the harness's live-integration test story into a deterministic one: the /test integration-generation step (Step 4.7) generates tests/integration/ against these doubles, the live-externals sensor (G36) blocks tests that reach real externals instead, and the regression gates (G15/G16) run --replay against them. v1 covers the Python/FastAPI backend; TS/React and other stacks are a later gap."
    },
    {
      "id": "cyclic-dependency-prepass",
      "axis": "behaviour",
      "kind": "feedforward",
      "wired_at": ".claude/skills/fix-from-diagnostics/SKILL.md",
      "status": "active",
      "gap_ref": "G33",
      "description": "Prompt-level guide (gap G33, same shape as G32's canary-first guide): before fix-from-diagnostics shards diagnostics by package, if ≥3 packages are affected and cycle data exists (specs/brownfield/modularity-pack.md, or code-graph.json's cycles field as a fallback), check whether the error-dense packages sit together on a known import cycle. If so, run a structural pass to break the cycle first (smallest cut, validated narrowly), then re-capture diagnostics and re-shard before resuming normal per-shard fixing — prevents cross-shard cyclic errors from burning the queue's 3-pass cap without converging. Wired into fix-from-diagnostics/SKILL.md Step 2 (locked by test/cyclic-prepass-wiring.test.js, same skill-text-assertion pattern test/canary-rollout-wiring.test.js uses). No computational sensor: the package-count and error-density thresholds are judgment applied mid-task, not a mechanically checkable commit-time property."
    },
    {
      "id": "team-policy",
      "axis": "architecture",
      "kind": "feedforward",
      "wired_at": ".claude/scripts/team-policy.js",
      "status": "active",
      "description": "Solo-vs-team policy for multi-story groups: solo | solo_sequential | team based on ownership span and cross-story deps. Reduces multi-agent boundary tax on tiny independent stories. Consumed by generator Rule 2."
    },
    {
      "id": "advisor",
      "axis": "architecture",
      "kind": "feedforward",
      "wired_at": ".claude/agents/advisor.md",
      "status": "active",
      "description": "Frontier mid-run advisor (Opus, read-only): structural re-rank/unblock at fixed checkpoints (post_fail_2, mid_epic) or /advise. Not self-serve escalation. Cap via execution.advisor_max_per_run."
    },
    {
      "id": "reuse-scout",
      "axis": "architecture",
      "kind": "feedforward",
      "wired_at": ".claude/scripts/reuse-scout.js",
      "status": "active",
      "net_add_justification": "Ranks the existing seam a new story could extend against the design/component-map seam metadata and cross-sprint constitution invariants; feeds the reuse-or-justify intake dialogue at /change Step S2 with a signal no existing guide (seam-confidence scores a chosen seam post-hoc; reuse-scout ranks candidates pre-hoc) or sensor covers.",
      "description": "Ranks the existing seam a new story could extend; feeds the reuse-or-justify intake dialogue."
    },
    {
      "id": "reuse-or-justify",
      "axis": "architecture",
      "kind": "feedforward",
      "wired_at": ".claude/skills/reuse-or-justify/SKILL.md",
      "status": "active",
      "net_add_justification": "The confidence-gated dialogue that turns reuse-scout's ranked candidates into a binding intake decision (extend vs. new) plus a recorded performance budget, wired into /change, /feature, and /sprint intake before the failing test — no existing guide runs this decision step; reuse-scout only ranks candidates, it does not force or record the choice.",
      "description": "Confidence-gated reuse-vs-new dialogue run at intake (band >= medium, a touched constitution invariant, or a same-release clone cluster); resolves whether a change extends an existing seam or justifies a new structure before code, and records the decision + performance budget via record-reuse-decision.js."
    },
    {
      "id": "record-reuse-decision",
      "axis": "architecture",
      "kind": "feedforward",
      "wired_at": ".claude/scripts/record-reuse-decision.js",
      "status": "active",
      "net_add_justification": "Append-only provenance for reuse-or-justify decisions (reuse-decisions.jsonl): each decision, its band, and its performance budget is immutable once recorded, giving the P2 seam-conformance gate and P3 performance-budget fitness functions a durable record to check the implementation against — no existing script persists intake-time reuse decisions.",
      "description": "Immutable decision recorder for the reuse-or-justify dialogue: appends each reuse-vs-new decision (with band, chosen seam or new-structure justification, and performance budget) to reuse-decisions.jsonl for downstream seam-conformance and budget checks."
    },
    {
      "id": "author-prompt-surface",
      "axis": "maintainability",
      "kind": "feedforward",
      "wired_at": ".claude/skills/author-prompt-surface/SKILL.md",
      "status": "active",
      "description": "Claude-only skill that loads docs/prompting-standards.md when creating or editing agents, skills, or commands (trigger conditions, model-agnostic bodies, no reasoning-as-text)."
    },
    {
      "id": "retro-auto-invoke",
      "axis": "traceability",
      "kind": "feedforward",
      "wired_at": ".claude/skills/auto/references/section-11-11-stopping-criteria.md",
      "status": "active",
      "description": "Agentic-flywheel §4.2/§9 decision (docs/agentic-flywheel-design.md): /auto invokes /retro once at its two genuine session-terminal stopping-criteria branches (Hard stop, Success) — never at the per-story Escalate or coverage-revert branches, which continue the session, and never on a --once intermediate link (exits via the separate SECTION 10.1 path before reaching SECTION 11). Suppressible with --no-retro. /retro (.claude/skills/retro/SKILL.md) stays silent when it drafts zero new recommendations, so a quiet run adds no interactive interruption."
    },
    {
      "id": "promote-eligibility-gate",
      "axis": "traceability",
      "kind": "feedforward",
      "wired_at": ".claude/scripts/promote-recommendation.js",
      "status": "active",
      "description": "Agentic-flywheel Phase B (§4.3, docs/agentic-flywheel-design.md): the /promote skill's deterministic, non-negotiable guardrail — a recommendation whose class is gate-loosen or security is PERMANENTLY ineligible for automated promotion regardless of status/confidence/any other field, enforced in code (checkPromotionEligible), not left to skill-prompt discipline. Also refuses an already-promoted or not-yet-approved recommendation. /promote is on-demand only, never auto-invoked; merge always stays human. The harness eval-suite gate for a promotion-PR (§4.4) is the existing full npm test suite + harness-manifest honesty invariant, not a new golden-task framework — deliberately scoped down from the original design sketch, see §9 in the design doc."
    },
    {
      "id": "plan-review-loop",
      "axis": "traceability",
      "kind": "feedforward",
      "wired_at": ".claude/skills/plan-review-loop/SKILL.md",
      "net_add_justification": "The three planning gates already stopped for a human, but asked a closed approve/reject question over a wall of artifacts — the cheapest correct answer is always yes, so the stop bought nothing. This is the shared dialogue discipline (review brief leading with the load-bearing decisions and where the planner would push back, one question at a time in /clarify format, 2-3 alternatives at each fork, revise-and-re-present as a changelog including declined feedback, round cap 5). Registered once as a sub-skill rather than triplicated into /spec, /design, /test, which would have been three copies free to drift.",
      "description": "Human review of a planning artifact run as a brainstorming dialogue instead of an approve/reject prompt. Invoked at /spec Step 8, /design gate, and /test Step 4.8; supplies the round shape, the question budget (reused from /clarify), and the per-phase challenge sources. Records rounds via plan-approval.js."
    },
    {
      "id": "spec-decisions-gate",
      "axis": "traceability",
      "kind": "feedforward",
      "wired_at": ".claude/scripts/validate-spec-decisions.js",
      "net_add_justification": "The /spec phase was split into a main-session shaping half and a forked sidekick renderer so decisions are made before 1.83 MB of artifacts exist rather than after. That split is only real if the renderer cannot run against a decisions file the human never shaped — the audited failure was six clarifications whose every basis ended \"Original planner reasoning\", i.e. the planner authoring both sides. No existing control checks who made a planning decision: plan-approval.js records that a review happened, not that the scope was chosen by a human before generation. Registered as one gate invoked from both halves rather than duplicated prose in each.",
      "description": "Hard block between /spec shaping and spec-render. Fails unless specs/decisions/spec-decisions.json is well-formed, names at least one milestone epic, carries at least one decision with basis \"human\", and marks every load_bearing decision as human-made. Headless lanes (--auto/--autonomous) waive only the human requirement, never the structural checks, and the waiver is reported. Re-run at spec-render Step 0 so the block holds if /spec is bypassed."
    },
    {
      "id": "design-decisions-gate",
      "axis": "architecture",
      "kind": "feedforward",
      "wired_at": ".claude/scripts/validate-design-decisions.js",
      "net_add_justification": "Sibling of spec-decisions-gate for the /design split, sharing its spine via hooks/lib/decision-record.js rather than duplicating it. Carries one rule no other control has: a load-bearing architecture decision must name what it RULES OUT, with n/a|none|TBD rejected. Taken from what the audited design got right — its decision table is literally | Decision | What it rules out | and its alternatives-rejected section was the most useful content in 632 KB of output. Nothing else in the harness checks that a design decision forecloses anything; trace-check proves ID linkage and validate-canvas proves structure, and a design can satisfy both while every decision in it is a preference an implementer can quietly reverse.",
      "description": "Hard block between /design shaping and design-render. Fails unless design-decisions.json is well-formed, names the committed stack, carries at least one basis:\"human\" decision, marks every load_bearing decision human-made, and gives every load_bearing decision a substantive rules_out. Headless lanes waive only the human requirement — never rules_out or the stack — and the waiver is recorded to specs/reviews/design-decisions-verdict.json. Re-run at design-render Step 0 so the block holds if /design is bypassed."
    },
    {
      "id": "prd-shape-gate",
      "axis": "traceability",
      "kind": "feedforward",
      "wired_at": ".claude/scripts/validate-prd.js",
      "net_add_justification": "The PRD is the root of the grounding chain and the only artifact a human authors, and nothing has ever checked one — while --autonomous and --auto both refuse to run without it. R2 raised the stakes: brd-adopt.js now carries PRD text verbatim into the spine, so there is no longer a rewriting step that might incidentally fix a malformed requirement. Owns one invariant nothing else covers: every functional requirement has an OBSERVABLE POSTCONDITION. trace-check proves ids link and brd-taxonomy-floor proves the ten slots are addressed; neither notices that a requirement gives the evaluator nothing to check, and a check with no oracle passes by default. Measured on a real PRD: 27 of 38 functional requirements had none.",
      "description": "Blocks a malformed PRD before it becomes the grounding baseline: missing/empty Out of Scope (or Non-goals), no functional requirements, duplicate ids, placeholder text, an acceptance entry naming an unknown requirement, and any FR with no acceptance postcondition. Warns on an NFR with no number or named standard and a milestone with no observable Done-when. Accepts the layouts real PRDs use (bullet, heading-per-requirement, bold pseudo-heading; acceptance in its own section or as an inline AC: line) — ids and postconditions are what is load-bearing, not heading text."
    }
  ],
  "sensors": [
    {
      "id": "accessibility",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "integration",
      "status": "active",
      "scope": "runtime",
      "wired_at": ".claude/scripts/contract-accessibility-default.js",
      "gap_ref": "G12",
      "signal": "axe/WCAG violations",
      "description": "axe-core gate (gap G12): default-on for UI stories. contract-accessibility-default.js injects a default accessibility_checks block into any contract with playwright_checks (opt out via project-manifest accessibility.enabled:false; explicit blocks respected); the evaluator then runs axe and FAILs serious/critical impacts in Full mode (failure_layer:accessibility), WARN in Lean. Gate logic lives in evaluate/SKILL.md."
    },
    {
      "id": "amendment-provenance-check",
      "axis": "traceability",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/amendment-provenance-check.js",
      "signal": "a commit changes specs/design/ with no matching record under specs/design/amendments/",
      "description": "Sprint-delta lane sensor: once a design baseline exists (specs/design/architecture.md tracked at HEAD), any commit touching specs/design/ (excluding the amendments/ dir itself) must carry a new file under specs/design/amendments/ in the same commit — otherwise a sprint's design evolution is invisible to the next sprint's human gate. Enforced at pre-commit (HARNESS_AMENDMENT_GATE=off escape); the very first design-authoring commit is exempt (verdict initial-design).",
      "records_as": [
        "amendment-provenance"
      ]
    },
    {
      "id": "api-contract-drift",
      "axis": "architecture",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "runtime",
      "wired_at": ".claude/scripts/contract-drift-gate.js",
      "gap_ref": "G12",
      "signal": "breaking changes to an existing API's OpenAPI contract",
      "description": "Contract-drift gate (gap G12): contract-drift-gate.js runs `oasdiff breaking` between the OpenAPI spec at the git base and the working tree, BLOCKing on breaking changes. Boundary-gated in /gate (fires when the OpenAPI spec changes) + `npm run contract-drift`. Conditional on an OpenAPI spec existing; degrades loudly (exit 0) when oasdiff is unprovisioned. Exit-code gating only (version-robust)."
    },
    {
      "id": "api-schema-validation",
      "axis": "architecture",
      "type": "computational",
      "cadence": "integration",
      "status": "active",
      "scope": "runtime",
      "wired_at": ".claude/scripts/validate-contract.js",
      "signal": "API response/shape violations",
      "description": "Sprint-contract + api-contracts.schema.json validation; evaluator Layer 1 checks live responses."
    },
    {
      "id": "approved-fixtures-gate",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/approved-fixtures-gate.js",
      "gap_ref": "G12",
      "signal": "snapshot oracle modified or added without re-approval",
      "description": "Approved-fixtures gate (gap G12): tracks a baseline of approved snapshot files (path+sha256 in specs/test_artefacts/approved-snapshots.json) and BLOCKs in /gate when an approved snapshot's checksum changed or a new unapproved snapshot appears; approve-fixtures.js (npm run approve-fixtures) re-blesses. Dormant (exit 0) when a project has no snapshot files. Stops agents silently regenerating oracles to pass tests."
    },
    {
      "id": "ask-codebase",
      "axis": "traceability",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/ask-codebase.js",
      "signal": "human orientation without opening the source tree",
      "description": "Human Ask CLI (P2, Devin Ask analogue): wraps context-pack into markdown answer with citations and read_next. npm run ask -- \"question\"; writes specs/reviews/ask-last.md."
    },
    {
      "id": "at-first-proof",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "artifacts",
      "gap_ref": "G23",
      "wired_at": ".claude/scripts/at-first-gate.js",
      "signal": "a story with a staged NEW (added) production source file has no specs/test_artefacts/acceptance/{story-id}.<ext> on disk, or no matching record-at-red.js receipt naming that {storyId, atPath} pair",
      "description": "Mechanical proof for the AT-first discipline (gap G20's disclosed forward item, closed by gap G23): writing-acceptance-tests-first's Process step 5 ('confirm it fails for the right reason') was a prompt-level instruction only, with no sensor confirming an AT was actually written and observed red before a story's implementation shipped. record-at-red.js runs a story's AT test command and, only when it FAILS (the AT is red), appends a receipt {storyId, atPath, observedRedAt, testCmd} to specs/reviews/at-red-receipts.jsonl (append-only, gitignored, same convention as coverage-verdicts.jsonl); a PASSING run records nothing and exits non-zero instead, quoting the skill's own Red Flag so the non-receipt is visible in the calling step's output. at-first-gate.js then re-checks at commit time: for every story with a NEW (added, not modified) staged production source file, it requires BOTH the acceptance-test file and a matching receipt, or BLOCKs, naming the story and the exact remediation. Determines 'which story owns this new file' by reusing parseComponentMapStoryFiles from hooks/lib/impact-scope.js (gap G16) — the codebase's existing story->files component-map.md parser — rather than ownership-check.js's own parseComponentMap, which flattens every row into one owned-path Set with no story association; a brand-new file with no story owner at all remains ownership-check.js's own, already-covered problem, not this gate's concern. Wired as a hard block into .claude/git-hooks/pre-commit, default-on (HARNESS_AT_FIRST_GATE=off acknowledges a local skip, mirroring HARNESS_LEGACY_DISCIPLINE_GATE=off's shape); a real exception belongs in specs/reviews/sensor-waivers.json per docs/sensor-arbitration.md. Degrades loudly, never silently, when no specs/design/component-map.md exists yet or no staged NEW production file resolves to a story owner. KNOWN LIMITATION (disclosed, see HARNESS.md G23): a receipt's timestamp mechanically proves the AT was confirmed red by commit time, not that it strictly preceded every line of the later implementation diff — proving strict chronological ordering would need fragile git-history archaeology this gate does not attempt.",
      "records_as": [
        "at-first-gate"
      ]
    },
    {
      "id": "biting-meta",
      "axis": "maintainability",
      "type": "computational",
      "cadence": "drift",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/loop-health.js",
      "signal": "commit gates that never fire or never block over run history",
      "description": "Meta-sensor (sensors-cli parity): reads .claude/state/sensor-outcomes.jsonl to surface dead (never-fired) and possibly-miscalibrated (never-blocked) commit gates for /retro. Report-only, advisory."
    },
    {
      "id": "bounded-context-rules",
      "axis": "architecture",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "contexts",
      "wired_at": ".claude/hooks/lib/contexts.js",
      "signal": "cross-context internal-import violations",
      "description": "Vertical bounded-context rules (gap G8): a context may not reach into another's internals except via its public surface or a declared allow-edge. Opt-in via project-manifest.json#architecture.contexts; enforced on every write (verify-on-save) and at pre-commit. The ArchUnit/dependency-cruiser rule the horizontal layer check can't express."
    },
    {
      "id": "branch-protection-verify",
      "axis": "traceability",
      "type": "computational",
      "cadence": "drift",
      "status": "active",
      "scope": "portfolio",
      "wired_at": ".claude/scripts/provision-protection.js",
      "signal": "live GitHub ruleset drifted from the desired branch-protection spec (missing/extra/mismatched required-check, code-owner, or force-push/deletion rules)",
      "net_add_justification": "Increment 2 of the security-compliance program: no existing sensor verifies that platform-level merge-blocking (GitHub branch-protection ruleset marking gitleaks+sast required and requiring code-owner review) is actually provisioned and un-drifted across the repo portfolio. Increment 1's security-baseline/secure-baseline-wiring controls prove the scanners RUN and are wired in-repo, but neither can see GitHub's server-side ruleset — a non-compliant PR still merges unless the ruleset is live. This is the first portfolio-scoped traceability control (config -> live GitHub platform state), a genuinely new axis extension, not an overlap of any in-repo sensor.",
      "description": "Branch-protection drift sensor: provision-protection.js --verify reads back the live GitHub ruleset (by ruleset_name), diffs it against the config-driven desired spec, writes specs/reviews/branch-protection-verify.json, and exits non-zero on structured drift so CI or an org-admin can gate on it. All identifiers come from project-manifest.json#github; no client literals. gh runs through an injected runner with literal argv (never shell-interpolated) and result-object error handling (exit 2 on gh absent/auth in verify)."
    },
    {
      "id": "brd-taxonomy-floor",
      "axis": "traceability",
      "type": "computational",
      "cadence": "planning",
      "status": "active",
      "scope": "repo",
      "gap_ref": "G38",
      "wired_at": ".claude/scripts/brd-taxonomy-check.js",
      "signal": "a requirement-taxonomy slot is neither covered by a requirement nor excused with a substantive reason",
      "description": "BRD comprehensiveness floor (gap G38): grounding-check.js proves the BRD invented and dropped nothing RELATIVE TO ITS SOURCE, but cannot prove the source asked the right questions — if the FRD never mentions retention, authz, or failure modes, the BRD is silent too and every existing gate still passes, leaving 'comprehensive' to mean only 'all sections non-empty'. brd-taxonomy-check.js asserts a fixed ten-slot floor (functional, data_lifecycle, integration, performance, security_authz, privacy_retention, observability, operability_failure, ux_accessibility, constraints): each slot needs a BR requirement tagged with it, or an entry in specs/brd/taxonomy-coverage.json giving a substantive na_reason. Placeholder excuses ('N/A', 'none', 'TBD', anything under 25 chars) are rejected — the gate exists to force the question to be asked, and a box-tick means it was not; the reason is committed so a reviewer can disagree with it. Also catches untagged requirements and misspelled slot names rather than silently ignoring them, and fails loudly on an empty spine instead of passing vacuously. BLOCKS at /brd Step 4.45 before the rubric runs; the rubric's completeness criterion is anchored to the verdict instead of judging prose.",
      "net_add_justification": "Nothing else in the harness can catch an unasked question. Every traceability control is relative — grounding, trace-check, and the AC round-trip all verify that downstream matches upstream, so a gap present in the FRD propagates cleanly through all of them and arrives as a production defect in retention, authz, or failure handling. This is the only absolute check in the planning chain, it is cheap (planning cadence, deterministic, no runtime cost), and its failure mode is the one the harness has repeatedly been burned by: a green pipeline over a silently incomplete spec."
    },
    {
      "id": "canvas-semantic-check",
      "axis": "traceability",
      "type": "hybrid",
      "cadence": "commit",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/canvas-semantic-check.js",
      "signal": "Canvas prose (Approach/Structure/Norms/Safeguards or an Operations step) may no longer describe changed governed code",
      "description": "Semantic half of code->prompt sync, companion to canvas-sync-check's path/generative half. canvas-semantic-check.js deterministically builds the review packet (specs/reviews/canvas-semantic-review.md) pairing changed governed files with the design-narrative claims a diff can falsify (canvas-sync.js buildSemanticReview); /gate then dispatches an agent (evaluator/code-reviewer) to judge whether each claim still holds. A claim the code no longer satisfies is a WARN resolved by fixing the Canvas prose first (fix-the-prompt-first), not a hard BLOCK — the judgement is inferential. Advisory script (always exit 0); runnable as npm run canvas-semantic."
    },
    {
      "id": "canvas-structure",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "planning",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/validate-canvas.js",
      "signal": "REASONS Canvas missing a section or an empty Governs list; or a BRD safeguard (SG-n) that the Canvas never cites",
      "description": "Deterministic structure gate (gap G4) run in /design Step 1.9: the Canvas must carry all seven REASONS sections and a non-empty Governs list. Hard block. Safeguard coverage (D9): when specs/brd/brd-safeguards.json exists, also proves every SG-n invariant/prohibition/limit/norm the BRD recorded is cited in the Canvas Safeguards or Norms section. Structure alone cannot catch a Safeguards section that is present, well-formed, and silently missing a business invariant — which is exactly how a BRD constraint fails to reach the design contract, since those sections were previously authored from the architecture with nothing tying them back. UNCOVERED (a required constraint the design does not carry) and UNKNOWN (an SG-n cited but absent from the spine — a typo or an invented constraint) both BLOCK; a misplaced citation (a norm filed under Safeguards or vice versa) is a warning only, because the constraint did reach the design and only the filing is off. Skipped loudly when the spine file is absent; an EMPTY spine reports empty_spine and FAILS, so creating an empty file cannot be used to silence the gate."
    },
    {
      "id": "canvas-sync-check",
      "axis": "traceability",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/canvas-sync-check.js",
      "signal": "changed files are missing from the REASONS Canvas Governs or Operations sections",
      "description": "Living-design sync sensor: compares changed files with specs/design/reasons-canvas.md so design-governed changes update the Canvas first instead of letting SPDD artifacts silently rot. Beyond DETECTING divergence, it deterministically PROPOSES the fix (canvas-sync.js proposeCanvasSync/applyCanvasProposal, no LLM): the report lists the exact Governs bullets + Operations stubs to add, and canvas-sync-check.js --write applies them in place (default stays report-only / human-gated). Idempotent — an already-governed path is never duplicated. This is the generative/path half of code->prompt sync; the semantic half (judging whether the Canvas prose still holds) is the companion canvas-semantic-check sensor. Runnable as npm run canvas-sync (add -- --write to apply)."
    },
    {
      "id": "clean-code-review",
      "axis": "maintainability",
      "type": "inferential",
      "cadence": "commit",
      "status": "active",
      "scope": "universal",
      "wired_at": ".claude/agents/code-reviewer.md",
      "signal": "SOLID breaks, god functions, duplication, swallowed errors",
      "description": "Inferential structure review on changed files (structure lens of the merged code-reviewer agent)."
    },
    {
      "id": "compliance-attestation",
      "axis": "traceability",
      "type": "computational",
      "cadence": "integration",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/generate-attestation.js",
      "signal": "no durable, SHA-keyed, committed compliance evidence exists for a commit (or a stored attestation's sha256 integrity hash no longer matches its content)",
      "net_add_justification": "Increment 4a of the security-compliance program: the harness produced per-run verdicts (gate-receipt, quality-card, the Inc 2/3 *-verify outputs) but they are ephemeral — specs/reviews/ and all *.jsonl are gitignored, the verify outputs overwrite-in-place with no commit SHA or timestamp, and the only durable committed evidence was a hand-authored certification/status.json. No existing control produces a per-commit, durable, committed evidence record aggregating the control inventory + verify outputs + gate verdict + ratchet baselines into the client-mandated attestation format. This is a genuinely new traceability output (durable evidence bundle, not another live sensor read), remappable to the client's real standard via a neutral standard-map.json — distinct from branch-protection-verify/deploy-gate-verify, which are transient live-platform drift reads it consumes.",
      "description": "Durable compliance-attestation generator: generate-attestation.js assembles a per-commit evidence bundle from harness-manifest.json (control_inventory + controls[] resolved to neutral standard clauses via standard-map.json), the branch-protection/deploy-gate verify outputs, the gate verdict (gate-receipt + quality-card), and the ratchet baselines, then writes an immutable, sha256-hashed .claude/attestations/<sha>.json + append-only index.json. Every optional input absent => recorded null (auditable, never a silent vacuous pass); compliant is true only when evaluated inputs pass. --verify recomputes the canonical content-minus-integrity hash to detect tamper; git/version read at runtime through an injected runner; no client literals (the standard map is data, overridable). Runnable at /gate or in CI, then the record is committed."
    },
    {
      "id": "comprehension-debt",
      "axis": "maintainability",
      "type": "computational",
      "cadence": "drift",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/sensor-comprehension-debt.js",
      "signal": "cumulative stock of merged-but-unread code (auto-merged diffs no human read), weighted by dependents and security boundary",
      "net_add_justification": "Every maintainability sensor measures a per-diff FLOW property (this change's cycles, coupling, length); none measures the cumulative STOCK of merged-but-unread code — the 'dark factory' that accretes when auto-merge lands diffs no human ever read (Horthy). This is a distinct axis of debt no existing control sees. Shipped emission-first (merge-provenance.jsonl is written by enableAutoMerge + the /gate receipt BEFORE this reader existed) so it cannot repeat the sensor-outcomes.jsonl-empty trap. Report-only DRIFT sensor — it adds a /retro nomination surface, not a merge-blocking gate, so it is a judgement input, not new friction.",
      "description": "Report-only DRIFT sensor (exit 0 always). Reads .claude/state/merge-provenance.jsonl, keeps human_reviewed:false rows, and weights each unread merge by computeBlastRadius (impact-scope.js — how many dependents lean on the unread code) × touchesSecurityBoundary (review-policy.js). Emits a ranked comprehension-debt score for /retro. Degrades loudly (a note) when code-graph.json is absent (blast reach counted as 0) or the ledger is empty. Run via `npm run sensor-comprehension-debt` or in /retro."
    },
    {
      "id": "constraints-extract",
      "axis": "traceability",
      "type": "computational",
      "cadence": "planning",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/constraints-extract.js",
      "signal": "schema obligations without a negative test",
      "description": "Mines minLength/required/enum from design schemas into test obligations."
    },
    {
      "id": "control-budget",
      "axis": "maintainability",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/control-budget-gate.js",
      "signal": "net growth in the harness's own registered control count without justification",
      "net_add_justification": "P0 subtractive ratchet (harness-simplification) — the one control the harness lacked: a counter-force on its OWN accretion. Every gap analysis argued to add; nothing ever argued to subtract, so a detailed 2026-06-10 cut-to-half proposal partially executed and was overwhelmed in five weeks (27->45 skills, 22->69 hooks). This gate makes control count a ratchet like cycle/coupling.",
      "description": "Ratchets the count of registered controls (guides+sensors, non-planned) against .claude/state/control-budget-baseline.json. The count may only stay flat or drop unless each newly-added control carries a net_add_justification (encoding 'replace one OR justify the net-add'). Pure logic in hooks/lib/control-budget.js, IO in scripts/control-budget-gate.js; reuses cycle-gate.js's ratchet shape. Run via `npm run control-budget`, in /retro, or when adding/removing any control."
    },
    {
      "id": "cost-report",
      "axis": "traceability",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "universal",
      "wired_at": ".claude/scripts/cost-report.js",
      "signal": "est $ / model mix / token totals from run receipts",
      "description": "Operator cost report over .claude/runs receipts: est/measured $, agents by role, model mix, optional cache-read share. Writes .claude/state/cost-report.json; /status Cost line via pipeline-state-readers#readCostSummary."
    },
    {
      "id": "coupling-ratchet",
      "axis": "architecture",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "repo",
      "gap_ref": "G18",
      "wired_at": ".claude/scripts/coupling-gate.js",
      "signal": "a change added an unstable hub (fan_in >= 5, instability >= 0.8)",
      "description": "Unstable-hub ratchet (gap G18): coupling-gate.js reads via drift.js's hubsForStabilityCheck (gap G26: prefers the uncapped code-graph.json metrics.unstable_hubs field when present, so a hub ranked 26th+ by fan-in in the top-25-truncated metrics.hubs display list is still caught — falls back to the prior unstableHubIds(metrics.hubs) behavior on an older graph) — the same fan_in >= 5 / instability >= 0.8 thresholds coupling-report.md and the drift-architecture sensor already use — and cycle-gate.js's gateDecision ratchet math to compare the unstable-hub set to a monotonic baseline (.claude/state/coupling-baseline.txt, which stores the hub-id set so a BLOCK can name the specific new hub(s), not just a count). A new unstable hub BLOCKS (/auto Gate 4, /gate, npm run coupling-gate) with fan-in/instability numbers and concrete extract-a-narrower-interface/split-responsibilities remediation — an agent-actionable message, not just a fact statement, per the Fowler 'Maintainability Sensors for Coding Agents' good-prompt-injection principle. Removing unstable hubs ratchets the baseline down. Closes the gap where this signal previously only surfaced on the drift cadence (npm run drift) or in the on-demand coupling-report.md, never fed back to the agent at commit time. No code-graph yet → skipped loudly, same degrade pattern as cycle-detection (G8)."
    },
    {
      "id": "coupling-report",
      "axis": "maintainability",
      "type": "computational",
      "cadence": "planning",
      "status": "partial",
      "scope": "repo",
      "wired_at": ".claude/skills/code-map/SKILL.md",
      "signal": "hubs, cycles, instability, dead-code candidates",
      "description": "AST-derived coupling/dead-code report. Its unstable-hub signal (fan_in/instability) is now ALSO enforced at commit time via the coupling-ratchet gate (gap G18, coupling-gate.js) — see that sensor entry. The report's own `hubs` table stays capped to the top 25 by fan-in, a deliberate display choice for a human-readable markdown table; gap G26 fixed the bug where enforcement consumers (coupling-ratchet among them) were reusing that same capped list for a threshold check, so a real unstable hub ranked 26th+ by fan-in was structurally invisible to the ratchet even though it never belonged in the display table's cap in the first place — those consumers now read the separate, uncapped `metrics.unstable_hubs` field instead. The report itself remains discovery-only — informational, not enforced — for everything else it carries: the full fan-in/fan-out tables and dead-code candidates."
    },
    {
      "id": "coverage-diff",
      "axis": "maintainability",
      "type": "computational",
      "cadence": "integration",
      "status": "active",
      "scope": "test-covered",
      "wired_at": ".claude/scripts/coverage-diff.js",
      "signal": "per-diff coverage below floor",
      "description": "Coverage floor applied to only the files a group changed."
    },
    {
      "id": "coverage-preflight",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "test-covered",
      "wired_at": ".claude/hooks/lib/coverage-preflight.js",
      "signal": "an Edit/MultiEdit's changed line range overlaps an UNCOVERED symbol per coverage_map.py",
      "description": "Real-time, PreToolUse half of checking-coverage-before-change's Iron Law (predates this session's G17 work; found unregistered during the G17 review and registered here rather than left orphaned). For a production file the brownfield code-graph maps, blocks an Edit/MultiEdit whose changed lines overlap an UNCOVERED symbol range, routing to pinning-down-behavior or sprouting-instead-of-editing; missing coverage data blocks with the scoped-regen command. Verdicts are cached per file against the graph/coverage file mtimes so coverage_map.py only re-runs when either changed. Tooling gaps (no python3, regex-fallback graph with no per-file symbol records) degrade to a non-blocking note, never a silent pass. Escape hatch: HARNESS_COVERAGE_PREFLIGHT=off. Composes with, does not duplicate, legacy-discipline-proof (G17): this sensor blocks the EDIT itself in real time inside one session; G17 separately proves at COMMIT time that the check actually happened and, for an UNCOVERED symbol, that pin-down/sprout evidence was staged — an agent could in principle disable this session-time gate (HARNESS_COVERAGE_PREFLIGHT=off) for one edit and still be caught by G17 at commit time, and vice versa a commit assembled outside a live session (no PreToolUse hook fired) is still caught by G17's receipt check."
    },
    {
      "id": "coverage-ratchet",
      "axis": "maintainability",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "test-covered",
      "wired_at": ".claude/git-hooks/pre-commit",
      "signal": "coverage below baseline or 80% floor",
      "description": "Repo-wide coverage ratchet (monotonic) at pre-commit and /auto Gate 3.",
      "records_as": [
        "coverage-ratchet-js",
        "coverage-ratchet-py"
      ]
    },
    {
      "id": "custom-sensor-runner",
      "axis": "traceability",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/run-custom-sensors.js",
      "signal": "user-defined project sensors declared in project-manifest.json#custom_sensors[]",
      "description": "Sensors-cli parity: runs project-declared custom sensors through the default parser at commit + on demand; opt-in blocking. Lets a downstream project add a check without touching harness internals."
    },
    {
      "id": "cycle-detection",
      "axis": "architecture",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/cycle-gate.js",
      "signal": "a change added an import cycle",
      "description": "Import-cycle ratchet (gap G8): cycle-gate.js compares the code-graph cycle count to a monotonic baseline (.claude/state/cycle-baseline.txt). A new cycle BLOCKS (/auto Gate 4, /gate, npm run cycles); removing cycles ratchets the baseline down. Detection still also feeds coupling-report.md + the drift monitor."
    },
    {
      "id": "deep-mutation",
      "axis": "maintainability",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "test-covered",
      "gap_ref": "G25",
      "wired_at": ".claude/scripts/deep-mutation.js",
      "signal": "Stryker/mutmut survivors in critical modules",
      "description": "Optional release/critical-module mutation tier. Detects Stryker or mutmut when configured, exits unprovisioned when absent, and never replaces the fast mutation-smoke gate. As of gap G25, /scaffold gives JS/TS and Python stacks a real onboarding path: it writes a project-manifest.json#quality.mutation block and a starter stryker.conf.json or pyproject.toml [tool.mutmut] section (root-level, matching detectTool()'s root-only lookup), so a fresh project is provisioned by default instead of permanently unprovisioned. deep-mutation.js itself is unchanged; older pre-G25 projects and non-JS/Python stacks still exit unprovisioned until configured manually."
    },
    {
      "id": "dep-audit",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "dependencies",
      "wired_at": ".claude/scripts/security-scan.js",
      "signal": "known-vulnerable dependencies (CVEs)",
      "description": "npm audit + pip-audit via security-scan.js --deps, run at /gate. Completes the computational security set; npm audit was previously only inside the inferential security-reviewer."
    },
    {
      "id": "deploy-gate-verify",
      "axis": "traceability",
      "type": "computational",
      "cadence": "drift",
      "status": "active",
      "scope": "portfolio",
      "wired_at": ".claude/scripts/provision-environments.js",
      "signal": "live GitHub deployment-approval Environment drifted from the desired spec, or fell below the approval-gate floor (zero required reviewers or protected_branches false)",
      "net_add_justification": "Increment 3 of the security-compliance program: no existing sensor verifies that the deployment-approval gate (a GitHub Environment with required reviewers, which only fires when a deploy job references environment:<name>) is actually provisioned and un-drifted across the repo portfolio. Increment 1's secure-baseline-wiring proves a deploy.yml references a configured environment in-repo, and Increment 2's branch-protection-verify covers the merge gate — but neither can see GitHub's server-side per-repo Environment, so a deploy could run with zero required approvers. This adds the deploy-time platform-state counterpart (config -> live GitHub Environment) with an approval-gate floor that reports empty-reviewers or unprotected-branch environments non-compliant regardless of config, a genuinely new deploy-approval axis extension distinct from the branch-protection merge gate.",
      "description": "Deployment-approval drift sensor: provision-environments.js --verify reads back each configured live GitHub Environment (per repo), diffs it against the config-driven desired spec plus the approval-gate FLOOR (>=1 required reviewer AND protected_branches), writes specs/reviews/deploy-gate-verify.json, and exits non-zero on drift so CI or an org-admin can gate on it. Environments are repo-scoped (--repo/--fleet). All identifiers come from project-manifest.json#github.environments; reviewers default to [] (no client literals). gh runs through an injected runner with literal argv and result-object error handling (exit 2 on gh absent/auth or malformed reviewer entries in verify)."
    },
    {
      "id": "diff-review",
      "axis": "behaviour",
      "type": "inferential",
      "cadence": "commit",
      "status": "active",
      "scope": "universal",
      "wired_at": ".claude/agents/code-reviewer.md",
      "signal": "logic errors, missing edge cases, contract breaks",
      "description": "Fresh-context correctness review (reads only the diff, never the builder's conversation; correctness lens of the merged code-reviewer agent)."
    },
    {
      "id": "drift-architecture",
      "axis": "architecture",
      "type": "computational",
      "cadence": "drift",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/drift-report.js",
      "signal": "new import cycles or newly-unstable hubs vs last snapshot",
      "description": "Continuous monitor (gap G2): diffs code-graph cycle/hub metrics against specs/drift/drift-snapshot.json and flags regressions. Run by any cadence (npm run drift, /schedule, /loop, CI); exit 1 on new drift."
    },
    {
      "id": "drift-dead-code",
      "axis": "maintainability",
      "type": "computational",
      "cadence": "drift",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/drift-report.js",
      "signal": "newly-orphaned (dead-code) files vs last snapshot",
      "description": "Continuous monitor (gap G2): diffs the code-map orphan set so dead code that accumulates outside the change lifecycle is surfaced, not silently grown."
    },
    {
      "id": "drift-deps",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "drift",
      "status": "active",
      "scope": "dependencies",
      "wired_at": ".claude/scripts/drift-report.js",
      "signal": "new dependency CVEs in unchanged code",
      "description": "Continuous monitor (gap G2): re-runs npm/pip audit (reusing security-scan runDeps) and flags CVEs newly disclosed against dependencies the change lifecycle never touched."
    },
    {
      "id": "drift-design-code",
      "axis": "architecture",
      "type": "computational",
      "cadence": "drift",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/drift-report.js",
      "signal": "Canvas governs a source path that no longer exists",
      "description": "Design-vs-code drift (gap G4): the drift monitor reads reasons-canvas.md's Governs list and flags governed paths that vanished — the living design referencing deleted/moved code. Pairs with the 'fix the prompt first' discipline."
    },
    {
      "id": "duplication-ratchet",
      "axis": "architecture",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/duplication-gate.js",
      "signal": "a change added a code clone above the grandfathered baseline",
      "description": "jscpd-backed clone ratchet; blocks new duplicate code, grandfathers existing. Degrades loudly when jscpd is unprovisioned.",
      "net_add_justification": "Closes a real coverage gap the existing architecture axis did not enforce: cycle-detection and coupling-ratchet catch structural issues but neither catches copy-pasted duplicate code at commit time. Same ratchet shape as its siblings (monotonic baseline, loud degrade when unprovisioned), so it earns its keep as a genuinely new signal rather than overlapping one already registered."
    },
    {
      "id": "eslint-ruff",
      "axis": "maintainability",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "universal",
      "wired_at": ".claude/hooks/verify-on-save.js",
      "signal": "lint violations (complexity, length, style)",
      "description": "ruff/eslint on the saved file (report-only on save, blocking at /auto Gate 2 and pre-commit). Block messages are enriched per-rule via lib/sensor-guidance (gap G5): each flagged rule gets an agent-facing fix line + the threshold-bump-with-justification valve ('positive prompt injection')."
    },
    {
      "id": "eval-api",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "integration",
      "status": "active",
      "scope": "runtime",
      "wired_at": ".claude/agents/evaluator.md",
      "signal": "API contract failures against running app",
      "description": "Evaluator Layer 1: curl + jsonschema against the live application."
    },
    {
      "id": "eval-design-critic",
      "axis": "behaviour",
      "type": "inferential",
      "cadence": "integration",
      "status": "active",
      "scope": "runtime",
      "wired_at": ".claude/agents/design-critic.md",
      "signal": "weak frontend quality (design/originality/craft/functionality)",
      "description": "Evaluator Layer 3 vision scoring (Full mode); secondary GAN loop with plateau detection."
    },
    {
      "id": "eval-playwright",
      "axis": "behaviour",
      "type": "hybrid",
      "cadence": "integration",
      "status": "active",
      "scope": "runtime",
      "wired_at": ".claude/agents/evaluator.md",
      "signal": "broken user flows",
      "description": "Evaluator Layer 2: Playwright browser interaction with semantic selectors."
    },
    {
      "id": "flake-detection",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "drift",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/flake-detector.js",
      "gap_ref": "G12",
      "signal": "tests that pass and fail across repeated runs",
      "description": "Flake detection (gap G12; e2e mode added gap G28): flake-detector.js runs a test command N times (npm run flakes), parses node:test TAP per run, and reports tests that both passed and failed across runs. As of gap G28, a new --e2e flag switches it to run a Playwright command per run and parse its --reporter=json output via hooks/lib/regression-gate.js's extractPlaywrightResults (reused, not reimplemented), so it can also populate specs/drift/flake-history.jsonl with Playwright-shaped 'describe > test' titles — closing regression-suite-full (G15)'s disclosed limitation that e2e-spec auto-quarantine never actually matched anything. Drift cadence — opt-in / /schedule, non-blocking (exit 1 on flakes for cron/CI signal); deliberately NOT a /gate or /auto gate, since a genuine flake should not block the change lifecycle. Errored/timed-out/unparseable runs are excluded from aggregation in both modes."
    },
    {
      "id": "flake-history",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "drift",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/flake-history.js",
      "signal": "tests that repeatedly flake across drift runs",
      "description": "Aggregates flake-detector reports into specs/drift/flake-history.jsonl and renders recurring flaky tests by frequency."
    },
    {
      "id": "fleet-gate-retrofit",
      "axis": "traceability",
      "type": "computational",
      "cadence": "drift",
      "status": "active",
      "scope": "portfolio",
      "wired_at": ".claude/scripts/fleet-retrofit.js",
      "signal": "some repo in the fleet does not have BOTH live gates in place — its branch-protection ruleset or deploy-approval environment is absent, drifted, or provisioned-but-not-gating",
      "net_add_justification": "Fleet-retrofit driver (follow-on to Increments 2-4b): branch-protection-verify and deploy-gate-verify each verify ONE gate (deploy-gate-verify already takes --fleet, but only for its own gate), and portfolio-compliance-rollup aggregates per-repo attestation EVIDENCE artifacts — none produces a per-repo COMBINED verdict over BOTH live gates at once, and none is an apply driver. This is the portfolio-scoped live cross-gate aggregate (both gates in one per-repo verdict) plus the one-command --apply/audit driver, isolating per-repo failures so a mid-fleet error never truncates the fleet picture. Genuinely new artifact + action, not double-counting the single-gate verifiers it orchestrates.",
      "description": "Fleet-retrofit runner: fleet-retrofit.js audits (read-only, default) or --applies the branch-protection ruleset + deploy-approval environment across every repo in fleet.json in one command, invoking provision-protection/provision-environments per repo (--repo owner/repo) with a shared injected gh runner and classifying each from the provisioners' real apply/verify exit codes. A gate the operator never configured (no github section, or empty github.environments) is recorded 'not-configured', NEVER 'gated' — the provisioners return exit 0 for 'nothing to provision', so configured-ness is decided from the manifest up front to prevent a vacuous green. Per-repo failures are isolated (a repo's gh 404/no-admin becomes a 'failed' row, never aborts the rest); the aggregate specs/reviews/fleet-retrofit.json records each repo as gated/drifted/not-gating/not-configured/failed with a fail-safe fleet_gated (true only when the fleet is non-empty AND every repo is gated). Exit 0 iff fleet_gated, 1 if any repo is not gated, 2 on unreadable/malformed fleet. Specs come from project-manifest.json#github, identity from fleet.json; no client literals."
    },
    {
      "id": "git-safety-session",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "universal",
      "wired_at": ".claude/hooks/pre-bash-gate.js",
      "signal": "bash invokes git stash / reset --hard / clean -fd / force-push during parallel implement",
      "description": "Session sensor half of multi-agent git safety (Bun Phase A). pre-bash-gate calls hooks/lib/git-safety.js when HARNESS_PARALLEL_AGENTS=1 or .claude/state/parallel-implement.lock exists."
    },
    {
      "id": "human-codebase",
      "axis": "traceability",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/human-codebase.js",
      "signal": "missing or stale docs/CODEBASE.md human homepage",
      "description": "Human-facing DeepWiki homepage (P0/P1): regenerates docs/CODEBASE.md from code-graph + CONTEXT + concept/wiki pages. Fail-open from graph-refresh secondary scripts; also /gate Step 4."
    },
    {
      "id": "impact-classifier",
      "axis": "traceability",
      "type": "computational",
      "cadence": "planning",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/impact-classifier.js",
      "signal": "a /feature story touches >3 files, a risk category (auth/payments/persistence/public-API), or introduces a new module",
      "description": "Sprint-delta lane routing sensor: classifies a single /feature story as architecturally invisible (-> /change, no design amendment) or design-touching (-> /design --delta, amendment + GATE 2), so small stories don't pay full design-gate overhead and design-relevant ones don't skip it. Advisory — always exits 0, it routes work rather than blocking it."
    },
    {
      "id": "risk-envelope",
      "axis": "traceability",
      "type": "computational",
      "cadence": "planning",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/risk-envelope.js",
      "signal": "task text, expected paths, impact breadth, and security/data/production boundary signals determine a monotonic R0-R4 risk floor",
      "net_add_justification": "Consolidates scattered route and reviewer risk decisions into one machine-readable artifact consumed by /feature and /change. It reuses impact-classifier rather than creating a second impact model, and is the future single source for required evidence, approvals, and prohibited actions.",
      "description": "Deterministic task risk classifier. Writes .claude/state/risk-envelope.json plus current task/tier markers; R0 documentation through R4 production/irreversible work. Agents may raise but never lower the floor. R3/R4 preserve human authority even in autonomous lanes; R4 forbids the agent from executing the production change."
    },
    {
      "id": "outcome-lifecycle",
      "axis": "traceability",
      "type": "computational",
      "cadence": "integration",
      "status": "active",
      "scope": "runtime",
      "wired_at": ".claude/scripts/record-outcome.js",
      "signal": "intent, gate, PR, merge, deploy, acceptance, rework, revert, and incident events tied to one run/task/risk identity",
      "net_add_justification": "Existing telemetry measures agent/tool activity but cannot distinguish busy machinery from accepted engineering outcomes. This extends the existing .claude/runs JSONL stream and record-run context instead of adding a parallel ledger.",
      "description": "Canonical lifecycle vocabulary and outcome recorder. record-run.js now attaches schema_version, run_id, task_id, parent_run_id, and risk_tier to ordinary events; record-outcome.js appends outcome milestones to the same daily receipts. outcome-report.js derives accepted outcomes, production survival, attention productivity, and cost per accepted outcome without treating successful tool calls as engineering success."
    },
    {
      "id": "matched-productivity-evidence",
      "axis": "traceability",
      "type": "computational",
      "cadence": "drift",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/productivity-study.js",
      "signal": "a productivity multiplier is claimed without enough matched baseline/agentic pairs, comparable risk/work/size, accepted production-surviving outcomes, passing gates, positive attention and cycle time, or evidence hashes",
      "net_add_justification": "Outcome telemetry prevents tool activity from masquerading as delivery, but it cannot establish causality or validate an 8x claim. This adds one analysis control over the existing run ledger, using matched pairs and deterministic bootstrap confidence rather than another telemetry stream or model grader.",
      "description": "Conservative productivity-study analyzer. Matches exactly one baseline and one agentic observation by comparison id and risk/work/size, validates project-local evidence hashes, and reports incomplete outcomes rather than counting them as wins. A multiplier claim requires no excluded study observations, at least ten eligible pairs, and a 95% lower confidence bound meeting the configured threshold."
    },
    {
      "id": "evidence-driven-autonomy",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "runtime",
      "wired_at": ".claude/scripts/autonomy-policy.js",
      "signal": "an execution mode exceeds its risk ceiling, autonomy is promoted without exact signed authority and sufficient live-recomputed outcome evidence, policy/evidence drifts, or unattended security certification regresses",
      "net_add_justification": "A–C establish authority, security certification, and productivity evidence but do not turn them into a controlled autonomy decision. This single state machine composes those controls, prevents self-promotion, and automatically moves toward safer modes on drift instead of adding separate mode-specific gates.",
      "description": "Integrity-chained attended/supervised/unattended policy by risk tier. Promotion advances one level only, requires sufficient complete matched-pair evidence live-recomputed from run receipts, respects hard risk ceilings, and consumes a task-bound capability scoped to the exact tier and target mode. Evidence, configuration, or unattended certification drift automatically regresses one level; R4 is permanently attended."
    },
    {
      "id": "task-envelope",
      "axis": "traceability",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "runtime",
      "wired_at": ".claude/hooks/lib/task-envelope.js",
      "records_as": [
        "task-envelope-scope"
      ],
      "signal": "a write or Bash action exceeds approved paths/actions, the envelope was tampered with, or the task budget is exhausted",
      "net_add_justification": "Operationalizes the risk-envelope decision through existing pre-write, pre-Bash, and budget enforcement surfaces. No existing control binds task intent/risk to path scope, prohibited semantic actions, and per-task budgets; this is one shared contract rather than three new gates.",
      "description": "Integrity-hashed execution contract created by task-envelope.js. Existing pre-write/pre-Bash hooks load it: absent remains compatible with ordinary attended writes, present-invalid fails closed, and present-valid restricts writes to allowed_paths and marks privileged semantic actions as externally authorized only. budget-state.js prefers its dimensions. /feature, /change, and /auto create/verify it before implementation."
    },
    {
      "id": "authority-capability-broker",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "runtime",
      "wired_at": ".claude/hooks/lib/authority-receipt.js",
      "signal": "a merge, deploy, branch-protection change, or production operation has no valid task-bound, short-lived, single-use capability backed by the required number of distinct signed human approvals",
      "net_add_justification": "The task envelope's SHA-256 detected local mutation but could not distinguish agent-authored authority from independently granted authority. This adds the missing zero-trust boundary once: Ed25519 verification against a protected public-key registry, reused by pre-Bash privileged-action checks and task completion approval checks. Private signing keys remain outside the workspace; the bundled issuer CLI is a protocol adapter, not a source of authority.",
      "description": "External authority receipt protocol and command-time capability broker. Human approvals and capabilities are Ed25519-signed, bind the exact task-envelope hash, carry issuer/key identity, nonce, issue/expiry times, and distinct approver identities. Sensitive Bash actions fail closed without a matching capability; accepted capabilities are atomically consumed before execution to prevent replay. R4 execute_production_change remains non-delegable and requires an externally signed human-execution receipt. NARROWED 2026-08: `merge` is privileged only when .claude/trust/issuers.json exists. Without an approval service no capability can be issued, so gating merge had no path to yes — it blocked every merge including owner-authorized ones, and the only way past was to disable the control, which is strictly worse than not gating it. deploy, modify_branch_protection and execute_production_change are unchanged and stay gated with or without an issuer; production remains non-delegable and is refused before the envelope check."
    },
    {
      "id": "task-evidence-finalizer",
      "axis": "traceability",
      "type": "computational",
      "cadence": "integration",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/finalize-task-evidence.js",
      "signal": "task completion lacks a green gate receipt, fresh passing required evidence, enough distinct trusted approvals, human-execution proof where required, or exact task/run/hash binding",
      "net_add_justification": "Existing gates emitted strong but independent artifacts, while required_evidence in the task envelope was declarative. This finalizer does not add new graders: it closes the loop by freshness-checking and hashing existing outputs, verifying signed approvals, and emitting one task/run-bound completion receipt that downstream PR and outcome automation can trust.",
      "description": "Central completion gate invoked by /gate after quality-card. Resolves each risk-required evidence class to existing gate artifacts, requires those artifacts to post-date the task contract and carry passing verdicts, verifies the envelope's distinct signed-approval threshold and R4 human-execution receipt, and writes an integrity-stamped task-completion-receipt.json bound to the task-envelope hash and canonical run id."
    },
    {
      "id": "unattended-preflight",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "planning",
      "status": "active",
      "scope": "runtime",
      "wired_at": ".claude/scripts/unattended-preflight.js",
      "signal": "headless execution lacks a valid task contract, fail-closed sandbox, scrubbed subprocess environment, deny-by-default egress policy, verified isolation, credential separation, or required scanners",
      "net_add_justification": "settings.auto.json previously carried only a warning to use a safe VM/container; nothing verified the compensating controls before unattended delegation. This turns that procedural boundary into one fail-closed preflight while leaving environment provisioning outside core.",
      "description": "Required /auto preflight when HARNESS_UNATTENDED=1. Verifies task envelope, sandbox.enabled+failIfUnavailable, CLAUDE_CODE_SUBPROCESS_ENV_SCRUB, deny-by-default unattended-policy.json, Docker/CI or explicit isolation evidence, absence of sensitive environment variables and mounted host credential stores, configured scanners, and external evidence that policy/authority paths are mounted read-only. Writes a diagnostic receipt and exits non-zero on any missing condition."
    },
    {
      "id": "runtime-containment",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "runtime",
      "wired_at": ".claude/scripts/runtime-policy.js",
      "signal": "the external runtime cannot prove the exact network/read-only/credential policy, or an unattended agent attempts a broker-only command directly",
      "net_add_justification": "Unattended preflight previously consumed self-asserted isolation flags; hooks alone cannot contain an arbitrary shell. This control moves the claim across the trust boundary: a role-scoped Ed25519 runtime attestation binds the exact policy hash and read-only mounts, while credential-request.js emits secret-free, single-use-authorized work for an out-of-process broker. It replaces local secret injection rather than adding another credential path.",
      "description": "External runtime enforcement contract. The existing unattended-policy.json now also declares immutable control paths, broker-only commands, and credential-to-command scopes alongside deny-by-default egress. runtime-policy.js accepts only a trusted, task-bound, unexpired runtime attestation matching the exact policy hash. In unattended mode pre-Bash blocks broker-only commands; credential-request.js consumes a credential-scoped capability and queues a secret-free request for an external executor, so credentials never enter the agent process."
    },
    {
      "id": "adversarial-security-certification",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "integration",
      "status": "active",
      "scope": "runtime",
      "wired_at": ".claude/scripts/security-certification.js",
      "signal": "a deterministic attack succeeds, certification is missing, expired, or corrupt, policy or enforcement subjects drift, or the requested unattended profile mismatches",
      "net_add_justification": "Existing isolated tests did not provide a composed negative certification boundary. One profile runner reuses existing enforcement primitives without adding an agent or grader and closes indirect shell, unapproved egress, broker bypass, and dependency-install paths.",
      "description": "Runs 14 deterministic attacks. The result binds the exact policy and enforcement-source hashes, expires after 24 hours, includes corruption detection, and is live-reprobed during verification. Unattended preflight requires it and the runtime mounts the result read-only."
    },
    {
      "id": "task-lifecycle",
      "axis": "traceability",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "runtime",
      "wired_at": ".claude/hooks/lib/task-lifecycle.js",
      "signal": "execution occurs before activation, after expiry/completion/abort, against a stale envelope version, through an invalid transition, or after lifecycle-chain tampering",
      "net_add_justification": "The task envelope constrained execution but had no temporal semantics: it could remain reusable forever, be silently replaced between tasks, or continue after completion. This one hash-chained state machine supplies expiry, terminal states, rotation safety, and authorized amendment history to both existing pre-tool gates.",
      "description": "Hash-chained task lifecycle with created, active, amended, completed, and aborted states. Version-2 envelopes expire, preserve previous-envelope hashes, and record signed amendments. Writes and Bash require an active, unexpired lifecycle binding; completion requires the matching task-evidence receipt, terminal tasks cannot execute, and rotation archives the prior envelope."
    },
    {
      "id": "checkpoint-recovery",
      "axis": "traceability",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "runtime",
      "wired_at": ".claude/scripts/checkpoint-state.js",
      "signal": "resume state is corrupt, diverged from the committed checkpoint, partially written, bound to an inactive task, or ambiguous about its next action",
      "net_add_justification": "claude-progress.txt was prose-only recovery state and session commits were described as checkpoints without a machine-verifiable snapshot. This adds one atomic, hash-chained recovery authority over existing progress/features/git/task/budget state and classifies recovery deterministically instead of creating a second orchestrator.",
      "description": "Atomic checkpoint and deterministic resume state machine. Each iteration-boundary checkpoint binds git HEAD/status, task/lifecycle/budget and progress artifact hashes, group, and exact next action into an immutable hash chain. Resume returns fresh_start, resume_exact, repair_partial_iteration, diverged_after_checkpoint, or fail-closed blocked states. Temp-write plus atomic rename prevents a killed write from replacing the last good current checkpoint."
    },
    {
      "id": "impact-scoped-regression",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "runtime",
      "gap_ref": "G16",
      "wired_at": ".claude/scripts/local-regression-gate.js",
      "signal": "an impact-scoped e2e spec or prior story-group's sprint-contract API check now fails against the running app",
      "description": "Fast local complement to gap G15 (regression-suite-full): running the WHOLE accumulated e2e/ suite and every prior contract on every single /change or /vibe iteration is too expensive, and a single non-peer-reviewed preprint (TDAD, arXiv:2603.17973 — one finding, not its own proposed fix, survived adversarial verification) found merely instructing an agent to 'run more tests' without targeted context made regressions worse, not better — directional evidence for impact-aware scoping, not a settled result. local-regression-gate.js instead computes a deterministic Test Impact Analysis via impact-scope.js: changed files -> reverse-dependency blast radius over code-graph.json's imports/calls edges -> owning story-group(s) (specs/test_artefacts/verification-matrix.json primary, specs/design/component-map.md + features.json fallback) -> that group's e2e spec(s) + sprint-contract, unioned with any human-curated project-manifest.json#verification.golden_paths (an always-on safety net under a possibly-stale graph). Reuses hooks/lib/regression-gate.js's primitives (runE2eSuite now accepts an optional specFiles filter, extractPlaywrightFailures, isQuarantined, regressPriorContract) rather than duplicating them. Tests already quarantined in specs/drift/flake-history.jsonl are excluded; an unreadable contract-schema BLOCKs (never silently validates against an empty schema). Wired as a hard block into /change Step S5 and /vibe Step 6 (every iteration); /gate and /auto's pre-merge step keep running G15's full unabridged sweep unchanged as the merge-time backstop, since impact analysis over a possibly-stale graph is deliberately treated as weaker coverage than the full suite, not a replacement for it."
    },
    {
      "id": "layer-imports",
      "axis": "architecture",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "layer-roots",
      "wired_at": ".claude/hooks/lib/layers.js",
      "signal": "upward layer-import violations",
      "description": "One-way HORIZONTAL layered-import enforcement on every write and at pre-commit. The vertical complement is bounded-context-rules (gap G8)."
    },
    {
      "id": "legacy-discipline-proof",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "artifacts",
      "gap_ref": "G17",
      "wired_at": ".claude/scripts/legacy-discipline-gate.js",
      "signal": "a staged MODIFIED production source file has no recorded checking-coverage-before-change verdict covering the actually-changed line range, or a recorded UNCOVERED verdict for that range with no RELATED pin-down/sprout test staged in the same commit",
      "description": "Mechanical proof for the legacy-preservation discipline (gap G17; range/relatedness-tightened by gap G29): checking-coverage-before-change, pinning-down-behavior, and sprouting-instead-of-editing were prompt-level instructions only, with no sensor confirming they ran — an agent could skip straight to editing an uncovered legacy symbol and nothing would block it. Directional evidence this is the exact failure mode procedural-only discipline invites under pressure: a single non-peer-reviewed preprint (TDAD, arXiv:2603.17973) found instructing an agent without a mechanical, presence-checkable gate can make outcomes WORSE, not better. record-coverage-verdict.js wraps checking-coverage-before-change Step 2's (unmodified) coverage_map.py call, appending every symbol verdict to specs/reviews/coverage-verdicts.jsonl as a receipt (path/symbol/start/end/verdict/tests/recordedAt) while passing the JSON through unchanged so the skill's existing read-and-route flow is undisturbed. legacy-discipline-gate.js then re-checks every staged MODIFIED (not newly-added — a brand-new file is greenfield) production source file at commit time: no receipt covering the ACTUALLY-CHANGED lines (gap G29 Gap A — parsed from a real `git diff --cached -U0` via hooks/lib/diff-hunks.js; a receipt for a different symbol/range in the same file no longer counts) BLOCKs (the Iron Law was never run for this edit), and a latest-recorded UNCOVERED verdict for that range BLOCKs unless a RELATED new/modified test-shaped file is also staged (gap G29 Gap B — relatedness via hooks/lib/legacy-discipline-relatedness.js: component-map.md story ownership, then a naming heuristic, then a disclosed commit-wide fallback, not 'any test file anywhere in the commit') — composing with, not duplicating, mutation-gate.js (G7)'s independent proof that new test code actually bites. As of gap G29, a manual (non-/auto) commit that passes the UNCOVERED-evidence path also gets a narrow bite-check backstop (hooks/lib/legacy-bite-check.js, reusing mutation-gate.js's runMutationOnFiles with a much smaller budget than G7's own — 3 mutants/10s vs 12/30s — scoped only to the file(s) that just passed via evidence); it skips when inAutoBuild (G7 already re-verifies there) or HARNESS_LEGACY_BITE_CHECK=off. Reuses ownership-check.js's SOURCE_EXTS/isSource classification and hooks/lib/tdd.js's isTestFile rather than reinventing either. Degrades loudly (never silently blocks forever) when no specs/brownfield/code-graph.json exists yet, or when the graph has no per-file symbol records (regex-fallback producer — the same condition coverage_map.py itself exits 3 on); --files CLI mode (no git diff plumbing) falls back to the pre-G29 whole-file assumption, disclosed not silent. HARNESS_LEGACY_DISCIPLINE_GATE=off acknowledges a local skip (default-on, unlike keeping-refactors-pure's opt-in HARNESS_COMMIT_KIND arming); a real exception belongs in specs/reviews/sensor-waivers.json per docs/sensor-arbitration.md. KNOWN LIMITATIONS (disclosed, see HARNESS.md G29): a pure-deletion diff hunk is approximated as a single-point changed range, not an exact line-level proof; the naming-heuristic/commit-wide-fallback relatedness tiers are real but weaker signals than component-map.md story ownership; the bite-check backstop's budget is intentionally narrower than G7's own and only runs for a manual commit."
    },
    {
      "id": "length-caps",
      "axis": "maintainability",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "universal",
      "wired_at": ".claude/hooks/lib/length.js",
      "signal": "30-line function / 300-line file violations",
      "description": "Pre-disk caps enforced by pre-write-gate. Per-path file cap: 300 lines for source, 500 for test files (isTestPath: exact directory segments test/tests/__tests__/e2e/spec, or filename shapes *.test.*, *.spec.*, test_*.py, *_test.py, *_test.go — segment-exact so src/testing/ and src/latest/ are NOT exempt). Rationale: the source cap exists because a long source file concentrates change, which is weaker for a test read one case at a time. It is a higher cap, not an exemption — the newlyOverFileLimit ratchet still applies to test files, and FUNC_HARD_LIMIT stays 30 everywhere since a 30-line test case is still a smell."
    },
    {
      "id": "live-externals",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "artifacts",
      "gap_ref": "G36",
      "wired_at": ".claude/scripts/live-externals-gate.js",
      "signal": "a staged tests/integration or e2e test reaches a real external system — a non-localhost http(s) URL, a real DB DSN, or a directly-constructed LLM/HTTP SDK client instead of the boundary-test-doubles kit (G34)",
      "description": "Hybrid live-externals gate (gap G36). Commit half: hooks/lib/live-externals-gate.js's classifyFiles heuristically scans staged tests/integration and e2e files for a non-localhost http(s) URL literal, a real DB DSN (postgres/mysql/mongodb/redis with a non-local host), or a directly-constructed SDK client (Anthropic(/OpenAI(); scripts/live-externals-gate.js does the git plumbing (checkStaged(exec), same --diff-filter DI-exec pattern test-deletion-gate.js uses). Wired into gate-registry.js's GATE_CATALOG via the dedicated hooks/lib/gates-live-externals.js wrapper (kept out of gates-early.js, which carries pre-existing grandfathered length violations), runsWithoutSource:true so it fires on a test-only commit like test-deletion-guard, registered at standard+ in sensor-tier.js. A finding BLOCKs with an agent-actionable fix line pointing at the kit (ReplayTransport / FakeLLMClient / db_session under HARNESS_TEST_REPLAY=1). Default-on (HARNESS_LIVE_EXTERNALS_GATE=off acknowledges a local skip); waivable via specs/reviews/sensor-waivers.json (sensor_id: live-externals). Degrades loudly when the script is missing (noteSkip). Runtime half: the regression gates (G15/G16) force HARNESS_TEST_REPLAY=1 and treat a missing fixture (MissingFixtureError/GoldenNotFoundError) as a live-external reach — a hard fail. KNOWN LIMITATIONS (disclosed): the lint is heuristic regex, not an AST parse (dynamically constructed URLs/clients can be missed); the wrapper-honors-the-flag check is enforced at runtime, not at lint time; v1 covers the Python backend only."
    },
    {
      "id": "living-navigation-refresh",
      "axis": "traceability",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/hooks/graph-refresh.js",
      "signal": "stale or missing DeepWiki/code-map navigation after source edits",
      "description": "Stop/SubagentStop refresh patches code-graph.json, re-renders symbol-map.md, and re-renders the deterministic DeepWiki. Placeholder greenfield graphs are upgraded to real graphs on the first source write; /status surfaces freshness and estimated token savings."
    },
    {
      "id": "modularity-pack",
      "axis": "maintainability",
      "type": "computational",
      "cadence": "planning",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/modularity-pack.js",
      "signal": "hubs (pre-classified legit vs suspicious), cycles, duplication candidates",
      "description": "Deterministic grounding for the modularity review (gap G6): extracts evidence from code-graph.json and pre-tags likely-legitimate hubs (factories/schemas/utils) so the inferential pass doesn't flag intentional patterns as god modules."
    },
    {
      "id": "modularity-review",
      "axis": "maintainability",
      "type": "inferential",
      "cadence": "planning",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/agents/modularity-reviewer.md",
      "signal": "semantic duplication, misplaced responsibility, argument clumps, coupling imbalance",
      "description": "Inferential modularity review (gap G6) grounded in the modularity-pack. Manually/human-triggered only, via /brownfield --full Step 3.6 or /design --delta Step D3.5 (scoped to the amendment) — it has no periodic component of its own; the drift-cadence proxy for staleness between real runs is the separate modularity-review-staleness sensor (G19). Judges duplication/responsibility/clumps/cycles against source, confirms legit hubs, and (gap G27) applies Vlad Khononov's published Balanced Coupling rubric — classifies each hub/duplication relationship's Integration Strength, Distance, and Volatility and flags coupling-imbalance only for the high-strength + far-distance + high-volatility combination via BALANCE = (STRENGTH XOR DISTANCE) OR NOT VOLATILITY. A maintainability sensor, not a gate."
    },
    {
      "id": "modularity-review-staleness",
      "axis": "maintainability",
      "type": "computational",
      "cadence": "drift",
      "status": "active",
      "scope": "repo",
      "gap_ref": "G19",
      "wired_at": ".claude/scripts/drift-report.js",
      "signal": "a currently-unstable hub absent from the last real modularity review's marker snapshot",
      "description": "Gap G19: modularity-review was previously mislabeled cadence:drift even though it has no periodic trigger of its own (fixed above). This sensor is the genuinely periodic proxy that was missing: record-modularity-review.js writes .claude/state/modularity-review-marker.json ({timestamp, unstableHubIds}) after a real review completes (/brownfield --full Step 3.6 or /design --delta Step D3.5); drift-report.js's withModularityStaleness (hooks/lib/drift.js) diffs the live code-graph's current unstable hubs against that marker and flags any that are new since the last real review. No marker at all (never run) means every currently unstable hub counts as stale. A cheap, deterministic nudge that the expensive inferential review is overdue — not a substitute for it."
    },
    {
      "id": "mutation-smoke",
      "axis": "maintainability",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "test-covered",
      "wired_at": ".claude/scripts/mutation-gate.js",
      "signal": "surviving mutants (tests that pass but do not bite)",
      "description": "Diff-scoped mutation gate (gap G7): the pre-commit hook runs mutation-smoke over a group's changed production files during /auto builds; survivors below threshold BLOCK with the file:line + exact flip. Also runnable standalone (npm run mutation) and still used by pinning-down-behavior / the brownfield CR lane."
    },
    {
      "id": "naming-clusters",
      "axis": "traceability",
      "type": "computational",
      "cadence": "planning",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/naming-clusters.js",
      "signal": "root nouns recurring across 2+ symbols in code-graph.json",
      "description": "Deterministic evidence extraction (mirrors modularity-pack.js's split) for brownfield Step 6: surfaces candidate domain terms with file evidence for the LLM to confirm into CONTEXT.md, instead of open-ended judgment on a full source read."
    },
    {
      "id": "observability-static",
      "axis": "architecture",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "universal",
      "wired_at": ".claude/scripts/observability-gate.js",
      "signal": "swallowed exceptions, empty catches, unstructured logs, boundary without logger, middleware without request_id",
      "description": "Static observability + exception-handling ratchet on changed production source (P1 human-trust program). Complements runtime-slo (G9) and code-gen observability conventions. Wired into /gate Step 2.5; BLOCK on bare except/empty catch; WARN on f-string logs and boundary-without-logger. Writes specs/reviews/observability-verdict.json.",
      "records_as": [
        "observability-gate"
      ]
    },
    {
      "id": "ownership-check",
      "axis": "traceability",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/ownership-check.js",
      "signal": "changed source files owned by no story in component-map.md",
      "description": "Deterministic file-ownership sensor: staged/changed source files must be owned by a story in specs/design/component-map.md ('no story, no code'). Enforced at pre-commit (checkOwnership, HARNESS_OWNERSHIP_GATE=off escape) and at /gate (sensor-waiver escape); a map that parses to zero entries fails loudly (empty_map) instead of passing vacuously. Cluster mode (--clusters, /design, G37 companion): once component-map.md exists, diffs story file-ownership against specs/stories/story-clusters.json and reports a file owned by stories in two different ownership clusters. story-clusters.js proves EDGE-level independence (no dependency between clusters) and is structurally blind to the collision that actually hurts a parallel team — two engineers editing the same file. Also reports unmapped_stories (clustered but no files, so the builder has no routing) and unclustered_stories (/spec and /design disagree about the decomposition). Warns by default and blocks only with --strict, because a genuinely shared surface (route registry, DI container, barrel file) is a legitimate design outcome and a hard block there would train people to route around the control; a collision on a behavior-bearing file is the actionable one. An empty map or an empty cluster plan is reported as a broken control (reason: empty_map / no_clusters), never a vacuous pass."
    },
    {
      "id": "perf-ratchet",
      "axis": "architecture",
      "type": "hybrid",
      "cadence": "integration",
      "status": "active",
      "scope": "runtime",
      "wired_at": ".claude/scripts/perf-baseline.js",
      "signal": "p95 latency regression vs baseline",
      "description": "Read-endpoint p95 ratchet; regression FAILs, absolute-budget overrun WARNs."
    },
    {
      "id": "perf-smell-static",
      "axis": "architecture",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "universal",
      "wired_at": ".claude/scripts/perf-smell-gate.js",
      "signal": "N+1 query-in-loop, unbounded loads, sequential independent awaits, sync-in-async",
      "description": "Diff-scoped static performance smell gate (P3). Complements hybrid perf-ratchet (runtime p95). BLOCK on N+1-in-loop and sync-in-async; WARN on unbounded loads and sequential awaits. Wired into /gate Step 2.5; writes specs/reviews/perf-smell-verdict.json.",
      "records_as": [
        "perf-smell"
      ]
    },
    {
      "id": "plan-confidence",
      "axis": "traceability",
      "type": "hybrid",
      "cadence": "planning",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/plan-confidence.js",
      "signal": "low-confidence plan band",
      "description": "Confidence-gated planning before execution. After /design+/test plan-only, plan-confidence.js writes specs/plan-confidence.json; --gate exits 2 when band is low so /build --auto must /clarify once or stop (never builds blind). Lite --auto escalates to full --auto on exit 2."
    },
    {
      "id": "portfolio-compliance-rollup",
      "axis": "traceability",
      "type": "computational",
      "cadence": "drift",
      "status": "active",
      "scope": "portfolio",
      "wired_at": ".claude/scripts/portfolio-rollup.js",
      "signal": "a repo in the fleet is non-compliant, unattested, running a drifted harness version, or its attestation's sha256 integrity no longer matches its content — so the portfolio as a whole is not provably compliant",
      "net_add_justification": "Increment 4b of the security-compliance program: the CISO mandate requires full SDLC-standards compliance ACROSS THE PORTFOLIO with attestation, but no existing control aggregates the per-repo 4a attestations into a single portfolio verdict or checks harness-version drift across the fleet. compliance-attestation (4a) produces one repo's durable evidence; branch-protection-verify/deploy-gate-verify read a single live-platform state; none answers 'is the whole portfolio compliant, on-version, and un-tampered?'. This is a genuinely new portfolio-scoped traceability aggregate (fail-safe: an unattested/tampered/behind-version repo keeps the portfolio non-green), reusing the 4a integrity primitive to verify each input rather than trusting it — distinct from every in-repo and single-platform sensor it consumes.",
      "description": "Portfolio compliance rollup: portfolio-rollup.js aggregates a collection of per-repo 4a attestations (a local collection dir, or --fetch via fleet.json + gh api) into one integrity-hashed report. It VERIFIES each input attestation's sha256 integrity first (a tampered/failed attestation is integrity_failed and never counted compliant), computes per-repo harness-version drift (current/behind/ahead/unknown) via a pure semver compare against --target-version or the running harness version, and records a --fleet repo with no attestation as a not-attested gap. portfolio_compliant is fail-safe (true only when every repo is compliant + integrity_ok + attested and the portfolio is non-empty). The report is itself sha256-integrity-hashed; --verify re-checks it. Identity comes from fleet.json/git at runtime; no client literals; gh runs through an injected runner. Corruption-detecting checksum, not authenticity (signing is the documented seam)."
    },
    {
      "id": "pr-walkthrough",
      "axis": "traceability",
      "type": "computational",
      "cadence": "integration",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/pr-walkthrough.js",
      "signal": "unordered PR diffs without human logical tour",
      "description": "Devin Review–class logical walkthrough (P0/P2): groups changed files entry→domain→service→data→adapter→test, attaches code-review severity, graph blast radius, 5-minute human review script. Writes specs/reviews/walkthrough.{md,json}; embedded in PR body via pr-body.js."
    },
    {
      "id": "prompt-cache-prefix",
      "axis": "traceability",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "universal",
      "wired_at": ".claude/hooks/lib/prefix-cache.js",
      "signal": "mid-session edit of CLAUDE.md, .mcp.json, or .claude/settings*.json",
      "description": "Blocks Write/Edit and Bash writes to prompt-cache prefix files so long sessions keep a stable cached prefix. Applies even in the harness monorepo (unlike machinery trust-boundary). Escape: HARNESS_PREFIX_EDIT=1 between sessions only.",
      "records_as": [
        "prefix-cache"
      ]
    },
    {
      "id": "protected-env-file",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "universal",
      "wired_at": ".claude/hooks/pre-write-gate.js",
      "signal": "writes to .env and other secret-bearing files",
      "description": "PreToolUse block on writing a protected env file, so credentials are never authored or overwritten by an agent. Canary-proven live (see sensor-value-report PROVEN-LIVE).",
      "net_add_justification": "Pre-existing control, previously UNREGISTERED. It has been running on every write/commit; registering it corrects the inventory rather than adding machinery. The budget rises because the old count understated what governs this repo."
    },
    {
      "id": "quality-card",
      "axis": "traceability",
      "type": "computational",
      "cadence": "integration",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/quality-card.js",
      "signal": "missing or failing aggregated gate receipt before PR",
      "description": "Human trust receipt (P0): aggregates evaluator, code-review, security, observability, perf-smell, regression, ownership into specs/reviews/quality-card.{md,json} and stamps .claude/state/gate-receipt.json. Always written at /gate Step 4; pr-body.js --require-gate refuses PR open when pass=false."
    },
    {
      "id": "readiness-digest",
      "axis": "traceability",
      "type": "computational",
      "cadence": "drift",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/readiness-digest.js",
      "signal": "stale quality-card, low active readiness pillars, missing human homepage",
      "description": "Weekly/ops digest (P3) over agent-readiness pillars + quality-card age + human-codebase presence. Report-only; npm run readiness-digest; writes specs/reviews/readiness-digest.{md,json}."
    },
    {
      "id": "refactor-purity",
      "axis": "maintainability",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "universal",
      "wired_at": ".claude/hooks/lib/gate-registry.js",
      "signal": "behaviour change mixed into a structural commit",
      "description": "Commit gate for the keeping-refactors-pure discipline: a commit containing renames/moves/extractions must not also change behaviour, so a regression stays attributable to the commit that caused it.",
      "net_add_justification": "Pre-existing control, previously UNREGISTERED. It has been running on every write/commit; registering it corrects the inventory rather than adding machinery. The budget rises because the old count understated what governs this repo."
    },
    {
      "id": "regression-suite-full",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "integration",
      "status": "active",
      "scope": "runtime",
      "gap_ref": "G15",
      "wired_at": ".claude/scripts/regression-gate.js",
      "signal": "a previously-passing accumulated e2e/ Playwright spec or a prior story-group's sprint-contract API check now fails against the running app",
      "description": "Cross-feature regression gate (gap G15): /evaluate and /gate only verify the CURRENT story-group's sprint contract, and /change and /vibe only re-run the unit suite — neither re-ran the accumulated e2e/ Playwright suite or prior groups' sprint contracts, so a change could pass its own tests while silently breaking an earlier feature. regression-gate.js re-runs every *.spec.(ts|js) under e2e/ via the project's e2e command and re-executes every prior group's sprint-contract contract.api_checks as live HTTP requests, first re-validating each contract's schema via the same hooks/lib/contract-schema machinery validate-contract.js uses. Tests already quarantined in specs/drift/flake-history.jsonl are excluded by matching the recorded `name` field against the Playwright spec's joined `describe > test` title or the api_check's id/description. Wired as a hard block into /gate and /auto's pre-merge-to-WAVE_BASE step (the full, unabridged sweep, reserved for merge time since it's too expensive to run on every local iteration); degrades loudly (never silently) when there is no e2e/ or sprint-contracts/ baseline yet. As of gap G16, /change Step S5 and /vibe Step 6 run the faster impact-scoped-regression sensor instead on every iteration, with this full sweep remaining the mandatory merge-time backstop. As of gap G28, flake-history.jsonl's `name` field can be populated with Playwright-shaped `describe > test` titles via flake-detector.js's new --e2e mode (previously it only ever recorded node:test TAP output, so this quarantine match path never actually fired) — see the flake-detection sensor entry. api_check quarantine (matched on id/description) was unaffected either way."
    },
    {
      "id": "resume-smoke",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "integration",
      "status": "active",
      "scope": "runtime",
      "wired_at": ".claude/skills/auto/SKILL.md",
      "gap_ref": "G14",
      "signal": "app fails to boot / health-check on a fresh-process resume",
      "description": "Startup smoke check (gap G14): the first time /auto enters SECTION 2 in a fresh process with prior committed work, it boots the app (project-manifest.json#verification.mode) and runs the evaluator Health-Check Retry loop BEFORE selecting work, catching a tree a killed prior window left broken that the append-only progress log never recorded. Failure routes to SECTION 6 self-healing as failure_layer:infrastructure. Implements Anthropic's 'run a basic test on the dev server at session start' principle."
    },
    {
      "id": "runtime-slo",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "integration",
      "status": "active",
      "scope": "runtime",
      "wired_at": ".claude/scripts/slo-check.js",
      "gap_ref": "G9",
      "signal": "5xx error-rate over SLO; p95 over absolute budget",
      "description": "Runtime-SLO sensor (gap G9 sensor-half): scrapes the generated app's RED /metrics (the guide-half's http_requests_total + http_request_duration_seconds) and compares to project-manifest.json#observability.slo. 5xx error-rate over error_rate_pct FAILs /evaluate in Full mode (failure_layer:slo); p95 over p95_ms is a WARN (regression stays with the perf ratchet). Wired into /evaluate Step P4 (app already booted) and runnable standalone (npm run slo -- --url <live>) for the drift cadence."
    },
    {
      "id": "sast",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "universal",
      "wired_at": ".claude/scripts/security-scan.js",
      "signal": "known vulnerable code patterns",
      "description": "Semgrep SAST via security-scan.js --sast, boundary-gated in /gate as the computational complement to the inferential security-reviewer. Degrades loudly when semgrep is unprovisioned (like eslint/tsc)."
    },
    {
      "id": "seam-confidence",
      "axis": "traceability",
      "type": "computational",
      "cadence": "planning",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/seam-confidence.js",
      "signal": "no clean cut-point for a brownfield change",
      "description": "Stops or surfaces at a gate when no safe seam exists for the goal."
    },
    {
      "id": "secret-scan",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "universal",
      "wired_at": ".claude/git-hooks/pre-commit",
      "signal": "credential-shaped strings",
      "description": "Always-available baseline regex secrets scan: pre-write (lib/secrets.js) on every edit AND pre-commit (checkSecrets) over all staged files. gitleaks is the enhanced tier, run at /gate via security-scan.js when provisioned.",
      "records_as": [
        "secret-scan-write"
      ]
    },
    {
      "id": "secure-baseline-wiring",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/hooks/lib/gates-strict.js",
      "signal": "the security CI guard was removed or downgraded",
      "description": "Strict-tier presence invariant (Increment 1, order 165, runsWithoutSource): BLOCKS if .github/workflows/security.yml is absent or its gitleaks/sast job is missing or non-blocking (continue-on-error), .gitleaks.toml is absent, or quality.sast_engine is unset. Shallow YAML job-presence parse only — GitHub branch-protection required-checks are server-side (Increment 2).",
      "net_add_justification": "The findings ratchet (security-baseline) is worthless if the guards themselves can be silently deleted or downgraded to continue-on-error. This is the anti-tamper counterpart that keeps the propagated security.yml + .gitleaks.toml + sast_engine seam in place — distinct enforcement (presence, not findings), so it earns a separate control rather than overloading the ratchet."
    },
    {
      "id": "security-baseline",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/hooks/lib/gates-strict.js",
      "signal": "a new secret, or SAST high/critical findings above the grandfathered baseline",
      "description": "Strict-tier commit ratchet (Increment 1 secure-repo baseline, order 160, runsWithoutSource so secrets are caught on docs/config-only commits): gitleaks secrets are absolute (any new one BLOCKS, never grandfathered, honours harness:secret-ok); semgrep high/critical findings ratchet down vs .claude/state/security-baseline.txt via cycle-gate.gateDecision, baseline rewritten on pass. Tier-aware via security-scan.js — fail-closed on a missing required scanner in strict, loud note-skip otherwise. Surfaced at /auto Gate 7 through registry membership.",
      "net_add_justification": "Closes the real gap the audit named: SAST/secrets were in-session and boundary-gated only, never wired into the /auto ratchet the way cycle/coupling/duplication are, and fail-open when the scanner was absent. This makes secrets absolute and SAST monotonic at commit time, the same ratchet shape as its siblings — a genuinely new enforced signal, not an overlap of the inferential security-review."
    },
    {
      "id": "security-patterns",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "universal",
      "wired_at": ".claude/hooks/pre-write-gate.js",
      "signal": "dangerous code patterns at authoring time",
      "description": "Per-write pattern block (eval, shell injection shapes, unsafe deserialisation) — the session-cadence complement to the /gate SAST tier, catching the pattern before it is ever committed.",
      "net_add_justification": "Pre-existing control, previously UNREGISTERED. It has been running on every write/commit; registering it corrects the inventory rather than adding machinery. The budget rises because the old count understated what governs this repo."
    },
    {
      "id": "security-review",
      "axis": "behaviour",
      "type": "inferential",
      "cadence": "commit",
      "status": "active",
      "scope": "universal",
      "wired_at": ".claude/agents/security-reviewer.md",
      "signal": "OWASP Top 10 findings + dependency CVEs",
      "description": "Adversarial find-then-refute review + npm audit. Boundary-gated in /gate. Inferential only — no computational SAST (gap G3)."
    },
    {
      "id": "sensor-value-meter",
      "axis": "maintainability",
      "type": "computational",
      "cadence": "drift",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/sensor-value-report.js",
      "signal": "commit gates that never fire or never block over run history (candidate shelfware)",
      "net_add_justification": "P0 (harness-simplification) — makes the already-wired biting-meta value data actionable. recordOutcome (gate-registry.js) fills sensor-outcomes.jsonl at every commit gate, but nothing turned it into a cut list; this is the nomination surface the subtractive ratchet needs so /retro can retire idle controls, not just cap new ones. Report-only, so it adds a removal lever, not friction.",
      "description": "Report-only. Reads .claude/state/sensor-outcomes.jsonl + GATE_CATALOG and names never-fired / never-blocked commit gates so /retro can propose retiring them. Degrades loudly (INSUFFICIENT DATA) below --min-runs. Run via `npm run sensor-value` or in /retro."
    },
    {
      "id": "sensor-waiver-validation",
      "axis": "traceability",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/validate-sensor-waivers.js",
      "signal": "invalid or expired sensor waiver",
      "description": "Validates specs/reviews/sensor-waivers.json so threshold bumps and suppressions are reviewed, complete, and expire."
    },
    {
      "id": "sprint-contract",
      "axis": "traceability",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/hooks/lib/gate-registry.js",
      "signal": "sprint contract schema/criteria drift",
      "description": "Commit gate validating the sprint contract round-trips through its real validator, so the evaluator is checking the criteria the planner actually wrote.",
      "net_add_justification": "Pre-existing control, previously UNREGISTERED. It has been running on every write/commit; registering it corrects the inventory rather than adding machinery. The budget rises because the old count understated what governs this repo."
    },
    {
      "id": "sprout-diff-one-symbol",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "artifacts",
      "gap_ref": "G30",
      "wired_at": ".claude/scripts/sprout-diff-gate.js",
      "signal": "a legacy file legacy-discipline-gate.js already classified as UNCOVERED-with-evidence, where that evidence is a genuinely NEW production file (a sprout, not a pin-down), has a staged diff intersecting more than 2 distinct code-graph.json symbol ranges",
      "description": "Mechanical proof for sprouting-instead-of-editing's Iron Law (gap G17's disclosed forward item, closed by gap G30): 'touch the legacy file at exactly one call line (or the rename pair for wrap)' was a prose-only instruction, with nothing checking a legacy diff's actual symbol span. Fires only when legacy-discipline-gate.js (G17/G29) already sees a legacy file as UNCOVERED-with-evidence AND hooks/lib/sprout-classify.js's classifySprout (reusing legacy-discipline-relatedness.js's storyOwnersFor, G29) determines that evidence is a SPROUT — a genuinely new production file staged (diff-filter=A, source, non-test) alongside the legacy touch — rather than a PIN-DOWN (test files only, no new production file), which has no one-symbol constraint of its own. For each sprout-classified file, hooks/lib/sprout-symbol-check.js's symbolsTouchedByRanges checks the file's actually-changed line ranges (legacy-discipline-gate.js's gitDiffRanges/hooks/lib/diff-hunks.js, G29, reused) against code-graph.json's per-file symbols, flattening a class's methods to leaf units the same way coverage_map.py's symbol_rows already does. More than 2 distinct symbols touched BLOCKs, naming the extras; the cap is 2, not 1, because sprouting-instead-of-editing's own Process step 2 names a legitimate 2-symbol wrap-rename-pair shape — this gate does not attempt to mechanically distinguish a 1-symbol sprout from a 2-symbol wrap (judged too heuristic to build with confidence), so a 2-symbol touch passes with an 'assumed wrap-rename pair, not independently verified' note rather than a false-confident PASS. Wired as a hard block into .claude/git-hooks/pre-commit (checkSproutDiffGate, right after checkLegacyDisciplineGate), default-on (HARNESS_SPROUT_DIFF_GATE=off acknowledges a local skip, mirroring HARNESS_LEGACY_DISCIPLINE_GATE=off's shape); a real exception belongs in specs/reviews/sensor-waivers.json per docs/sensor-arbitration.md. Degrades loudly, never silently, mirroring legacy-discipline-gate.js's own noGraphVerdict/hasSymbolRecords conventions: no code-graph.json, a regex-fallback graph, no per-file symbol record for a candidate, or unavailable changed-range data (--files CLI mode) all SKIP with a note; no UNCOVERED-with-evidence candidate, or one present but pin-down-shaped, also SKIPs with a distinguishing note. KNOWN LIMITATIONS (disclosed, see HARNESS.md G30): reads code-graph.json as-is (the same staleness assumption G17/G29's receipt matching already accepts); the sprout-vs-pin-down classification's weakest tier (commit-wide-fallback, no component-map.md story link) is a commit-wide, not per-line-attributed, signal; the 2-symbol wrap-pair allowance is a cap, not a verified rename-pattern match.",
      "records_as": [
        "sprout-diff"
      ]
    },
    {
      "id": "story-ownership-clusters",
      "axis": "traceability",
      "type": "computational",
      "cadence": "planning",
      "status": "active",
      "scope": "repo",
      "gap_ref": "G37",
      "wired_at": ".claude/scripts/story-clusters.js",
      "signal": "an interface contract crosses an ownership-cluster boundary with no story able to publish it",
      "description": "Ownership-cluster planner (gap G37): dependency-graph.json groups stories by topological DEPTH, which answers 'what can be scheduled next' — orthogonal to 'what can one engineer own end to end'. A depth level mixes unrelated subsystems into one branch while a vertical slice spans three levels, so the wave graph cannot be used to allocate work across a team. story-clusters.js computes the ownership view from specs/stories/stories.json: connected components over HARD edges only (data|behavior — the consumer needs the producer's runtime effect), with contract|ui edges treated as cuttable because publishing the interface first lets both engineers work at once. Oversized components split on the point-balancing bridge; a biconnected blob is reported oversized rather than split on an invented boundary. Undersized components merge into the peer they share the most cut edges with. Emits specs/stories/story-clusters.json (clusters with coordination_cost + independently_startable, interface_contracts naming the contract_story that publishes each artifact, blocking_dependencies for hard edges that had to be cut) and specs/stories/dependency-edges.json. BLOCKS (/spec Step 4.5, npm run story-clusters) when an interface contract has contract_story: null — the cut is fiction, the consumer must wait for the producer's full implementation, and allocating those clusters to separate engineers would surface a hidden serial dependency mid-sprint. Cutting a hard edge is never presented as parallelism: the downstream cluster is marked independently_startable: false. Waves are untouched — wave-plan.js, --pod, and per-cluster PRs keep their existing semantics.",
      "net_add_justification": "No existing control answers who can own what. dependency-graph.json + wave-plan.js answer scheduling order only, and their groups are routinely mistaken for work packages — a depth level mixes unrelated subsystems into one branch while a vertical slice is torn across three. Allocating from that graph produces hidden serial dependencies discovered mid-sprint. This is the first control that measures ownership independence rather than asserting it, and it is a hard gate precisely because the failure is silent: everything looks parallel until an engineer blocks. It adds no runtime cost to build lanes (planning cadence only) and leaves the wave machinery untouched."
    },
    {
      "id": "stub-smell-gate",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "repo",
      "wired_at": ".claude/scripts/stub-smell-gate.js",
      "signal": "staged production source contains stub-to-green markers (todo!, unimplemented!, NotImplementedError, bare pass/..., throw TODO) without harness:stub-ok",
      "description": "Bun Phase A anti stub-to-green computational sensor. hooks/lib/stub-smell.js classifies production (non-test) staged content; scripts/stub-smell-gate.js --staged; pre-commit via gates-early.checkStubSmellGate on standard+ tiers. Allow annotation: harness:stub-ok story=E#-S# on the same line. Complements code-reviewer Iron Laws."
    },
    {
      "id": "tdd-test-first",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "test-covered",
      "wired_at": ".claude/hooks/lib/tdd.js",
      "signal": "production code written before its test",
      "description": "PreToolUse block when a new production symbol is authored with no corresponding test (missingTest). The mechanical half of the TDD discipline the code-gen guide states in prose. Escape: HARNESS_TDD_GATE=off.",
      "net_add_justification": "Pre-existing control, previously UNREGISTERED. It has been running on every write/commit; registering it corrects the inventory rather than adding machinery. The budget rises because the old count understated what governs this repo."
    },
    {
      "id": "test-deletion-guard",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "artifacts",
      "gap_ref": "G31",
      "wired_at": ".claude/scripts/test-deletion-gate.js",
      "signal": "a staged commit deletes a test-shaped file that had live test cases, decreases a test-shaped file's live test-case count, or adds a new skip/todo marker without decreasing the count",
      "description": "G7 (mutation-gate), G17 (legacy-discipline), and G30 (sprout-diff) all prove a change didn't silently regress PRODUCTION code; none of them catch the adjacent failure mode a refactor or dependency bump under pressure can reach for instead: making the suite pass by deleting, or newly skipping, the test that would have caught the regression, rather than fixing the code. hooks/lib/test-deletion-gate.js's classifyTestFileChange compares each staged test-shaped file's (hooks/lib/tdd.js isTestFile) old (git show HEAD:<file>) and new (git show :<file>, the staged index content) bodies with heuristic regex counts (same tier as secrets.js's baseline scan and tdd.js's own isTestFile — not an AST parse): a live-test-marker count (it(/test(/it.each(/test.each(/def test_...) that drops, or a deletion of a file that had live markers, is a hard finding; a same-or-higher test count but a newly added skip/todo marker (.skip(/.todo(/xit(/xdescribe(/xtest(/@pytest.mark.skip/@unittest.skip) is a second, independent finding kind. scripts/test-deletion-gate.js does the git plumbing (collectStagedChanges, same --diff-filter=M/D DI-exec pattern legacy-discipline-gate.js and sprout-diff-gate.js use) and exposes both a --staged CLI (npm run test-deletion-gate) and a checkStaged(exec) entry point the pre-commit hook calls directly. Wired into .claude/git-hooks/pre-commit as checkTestDeletionGate, deliberately called BEFORE the docs-only early exit (stagedFiles() filters --diff-filter=ACMR, which excludes pure deletions, so a commit that ONLY deletes a test file would otherwise never reach a check gated behind that filter) — the same reason checkSecrets and checkAmendmentProvenance already run there. Default-on hard block (HARNESS_TEST_DELETION_GATE=off acknowledges a local skip, mirroring HARNESS_LEGACY_DISCIPLINE_GATE=off's shape); a real exception (a test for removed functionality, a flaky test being quarantined) belongs in specs/reviews/sensor-waivers.json (sensor_id: test-deletion-guard) per docs/sensor-arbitration.md. Degrades loudly, never silently, when the sensor script is missing/unloadable (noteSkip, same convention as every other lazy-required pre-commit gate). KNOWN LIMITATIONS (disclosed, not hidden): heuristic regex counting, not an AST parse, so unusual formatting or dynamically generated test cases (a loop emitting it(...) calls) can mis-count; a test rewritten to cover different/broader behavior at the same or higher count is invisible to this gate (it proves the COUNT and SKIP STATE didn't regress, not that behavioral coverage is equivalent — that judgment stays with code-reviewer / the human); composes with, does not duplicate, regression-suite-full (G15) and impact-scoped-regression (G16), which prove a still-PRESENT test's outcome didn't regress, not that the test itself wasn't quietly removed or skipped."
    },
    {
      "id": "token-advisor",
      "axis": "traceability",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "universal",
      "wired_at": ".claude/hooks/token-advisor.js",
      "signal": "context_search_skipped or broad_source_read or verbose_command token warning or block",
      "description": "Token optimizer hook for Read|Bash. Advisory (default): warns when context_search_required and no fresh context-pack receipt (context_search_skipped), on avoidable broad source reads when symbol ranges exist, and on likely verbose commands that should use run-compact.js. Enforced mode (token_governor.mode=enforced): same predicates block with exit 2; fails open without graph ranges/placeholder. Escape: HARNESS_TOKEN_GOVERNOR=off."
    },
    {
      "id": "trace-check",
      "axis": "traceability",
      "type": "computational",
      "cadence": "planning",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/trace-check.js",
      "signal": "net-new/dropped or untraced IDs across the planning trace chain: BRD vs FRD/interview spine, story vs BRD, test case vs AC + obligation",
      "net_add_justification": "Absorbs the former separate grounding-check sensor (v6-reduction, one-owner-per-invariant): the net-new/dropped ID-linkage invariant is one engine (trace-check.js) across every link, so it is registered once here rather than split into a second same-invariant sensor. grounding-check.js stays as a BRD-specific field-renaming adapter (still runs and is still tested), named below so the BRD hard block is not hidden.",
      "description": "Deterministic hard block on ID linkage across the whole planning trace chain — the single owner of the net-new/dropped invariant. One engine (trace-check.js) backs all links: BRD vs FRD/interview spine (via the BRD-specific grounding-check.js adapter at /brd Step 4.4, which only renames fields), story vs BRD (spec), and test case vs AC + obligation (test)."
    },
    {
      "id": "trust-boundary",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "universal",
      "wired_at": ".claude/hooks/lib/trust-boundary.js",
      "signal": "agent edits to its own control machinery",
      "description": "Blocks a session from rewriting the harness machinery that governs it (hooks, gates, git-hooks) unless running in the harness repo itself. The control system must not be editable by the thing it constrains.",
      "net_add_justification": "Pre-existing control, previously UNREGISTERED. It has been running on every write/commit; registering it corrects the inventory rather than adding machinery. The budget rises because the old count understated what governs this repo."
    },
    {
      "id": "type-check",
      "axis": "maintainability",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "universal",
      "wired_at": ".claude/hooks/verify-on-save.js",
      "signal": "type errors",
      "description": "mypy per-file on save (block messages enriched per-rule via lib/sensor-guidance, gap G5); tsc --noEmit project-wide at pre-commit."
    },
    {
      "id": "unit-tests",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "test-covered",
      "wired_at": ".claude/git-hooks/pre-commit",
      "signal": "failing tests",
      "description": "pytest/vitest/jest at /auto Gate 1 and pre-commit."
    },
    {
      "id": "verification-matrix-gate",
      "axis": "traceability",
      "type": "computational",
      "cadence": "integration",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/verification-matrix-gate.js",
      "signal": "BRD/story acceptance criteria without required unit/API/E2E evidence",
      "description": "Verification matrix gate: a deterministic BRD-to-test-to-runtime conformance control. Runs in planning, sprint-contract, implementation, and pre-PR executed phases, requiring every implementation-ready acceptance criterion to have the required verification layers and evidence. The executed phase is additionally re-run by the pre-commit hook (.claude/git-hooks/pre-commit, checkVerificationMatrix) as a deterministic commit-time backstop, so, during an active sprint group with source files staged, skipping the in-session invocation cannot bypass it."
    },
    {
      "id": "vocabulary-check",
      "axis": "traceability",
      "type": "computational",
      "cadence": "planning",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/vocabulary-check.js",
      "signal": "entity/model definition names in domain_concepts, data-models.schema.json, or api-contracts.schema.json that do not resolve to a CONTEXT.md term",
      "description": "Deterministic vocabulary-consistency sensor: extends the traceability axis from ID-linkage (trace-check) to term-linkage. Catches 'Account in the BRD, User in the API contract' before code is written. CONTEXT.md terms may originate from domain_concepts (Step 2.8) or, for any enabled vertical plugin registered in vertical-glossary-packs.json, from vertical-glossary-pack.js (Step 2.7) — vocabulary-check.js validates either origin identically."
    },
    {
      "id": "write-scope",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "universal",
      "wired_at": ".claude/hooks/pre-write-gate.js",
      "signal": "write outside the project / tmp / project-memory scope",
      "description": "PreToolUse(Write|Edit) block on any write resolving outside the project tree, /tmp, or this project's Claude memory dir (isWriteInScope). Stops an agent editing unrelated repos or home-directory config.",
      "net_add_justification": "Pre-existing control, previously UNREGISTERED. It has been running on every write/commit; registering it corrects the inventory rather than adding machinery. The budget rises because the old count understated what governs this repo."
    },
    {
      "id": "plan-approval",
      "axis": "traceability",
      "type": "computational",
      "cadence": "planning",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/plan-approval.js",
      "signal": "a planning phase entered with no recorded human review, a review still in changes-requested, or an approval whose artifacts have changed since it was given",
      "net_add_justification": "The human planning gates were prose-only and therefore unobservable: nothing downstream could tell whether /spec, /design or /test ever stopped for a human, what was said, or whether the plan still matches what was approved — the feedforward-only failure the sensor-value-meter cannot detect because no outcome was ever written. This is the feedback half. It owns one invariant no existing sensor covers (human review happened and still applies), distinct from trace-check.js, which owns machine ID-linkage between artifacts and says nothing about human sign-off. The digest check is the load-bearing part: without it an agent can rewrite the stories after approval and inherit a receipt for a plan the human never saw.",
      "description": "Receipt + hard gate for the plan-review-loop at /spec, /design, /test. record/waive/check verbs write specs/reviews/<phase>-approval.json (rounds, verbatim feedback, changes, declined items, artifact sha256 digests). Rejects placeholder feedback and an approval that leaves earlier feedback unaccounted for; voids an approval whose artifacts changed. /design blocks on spec, /test on design, /auto on all three (check --phase all). Headless lanes record a waiver naming the lane; --require-human refuses waivers. Flags low_engagement (zero feedback, zero answered questions) as a rubber-stamp trend signal for /retro without blocking."
    },
    {
      "id": "evidence-integrity",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "integration",
      "status": "active",
      "scope": "runtime",
      "gap_ref": "G39",
      "wired_at": ".claude/scripts/evidence-integrity-gate.js",
      "signal": "a browser check recorded PASS in the evaluator's evidence ledger that is not backed by real user-path interaction against a pre-committed sprint-contract check — a functional assertion satisfied via browser_evaluate, a pass with no interaction at all, a contracted check with no evidence entry, an evidence entry for a check the contract never declared, or a missing/mismatched ledger",
      "description": "Evidence-integrity gate (gap G39). The evaluator is granted browser_evaluate because it needs it to inject axe-core, so it can satisfy a UI assertion by reading state or firing a handler in JavaScript instead of driving the app — the \"JavaScript cheating\" failure mode Cognition documents for Devin's own computer-use testing (cognition.com/blog/testing-development). That silently voids the evaluator's own black-box premise (\"the running app produces the evidence; you never read source to decide it looks right\"), and no existing control observed it: browser_evaluate appeared in the evaluator's tool list and in the axe-core instructions, and nowhere else. The evaluator now emits specs/reviews/evaluator-evidence.json — one entry per browser check with the contract's check id, layer (playwright/accessibility), verdict (pass/fail/untested), the interaction tools actually used, matrix_ids and artifact paths — and hooks/lib/evidence-integrity.js cross-checks that ledger against the read-only sprint contract. Five findings, all BLOCK: js-bypass, no-interaction-pass, missing-evidence, undeclared-check, unbacked-axe (plus missing-ledger / group-mismatch / missing-contract as fail-loud input errors). Registered in .claude/config/gate-checks.json (pack: verification, trigger exists:sprint-contracts, blocking) so /gate runs it, folded into /evaluate as failure_layer \"evidence\", and read by quality-card.js. Recording an unexecutable check as \"untested\" rather than omitting it is what lets the gate distinguish not-attempted from verified; the untested list is reported, never a finding, because \"never report a layer as passed that you could not execute\" is already the evaluator's own rule (one owner per invariant). KNOWN LIMITATION (disclosed, not hidden — the same precedent G23 sets): three findings (missing-evidence, undeclared-check, no-interaction-pass) are arithmetic against the pre-committed contract and hold regardless of how honestly the evaluator describes its own method, but js-bypass and unbacked-axe read a self-declared interaction list. That is deliberate parity with Cognition's own control (annotate the expectation before acting, which makes rationalising an unexpected result as a pass harder) — it catches the rationalising evaluator, not a determined one. Tamper-proof enforcement needs a PostToolUse tap on the Playwright MCP browser_evaluate tool, which requires a .claude/settings.json matcher and is therefore inter-session work behind HARNESS_PREFIX_EDIT; the snippet is recorded in HARNESS.md G39.",
      "net_add_justification": "Owns one invariant no existing control covers: that a claimed runtime PASS was earned through the user path. The harness's verification breadth is already ahead of published practice on obligation COVERAGE (verification matrix, constraint obligations, AT-first, record/replay boundary doubles, deterministic grounding gates), so the residual risk has moved to evidence INTEGRITY — whether a green verdict was earned at all. That is the same defect class CLAUDE.md principle 5 was written from (a gate reading a flat contract while real contracts nest under `contract` kept every test green while the feature was inert), and the one place the evaluator's black-box premise could be voided without any sensor noticing. Paid for at the /gate cadence with no new agent, no new hook, and no new prompt stage: one pure lib, one CLI, one registry row."
    },
    {
      "id": "red-phase-record",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "artifacts",
      "gap_ref": "G41",
      "wired_at": ".claude/hooks/red-phase-record.js",
      "signal": "a Bash test run observed at PostToolUse: which runner, pass or fail, which test files, and the sha256 of each test file as it ran",
      "net_add_justification": "The harness proved test EXISTENCE (tdd-test-first in pre-write-gate.js) but never test ORDERING — that gate's own comment says \"pair with tdd-guard for red-green ordering\", and no such pair existed. A test written to match code already written passed every control we had, so tautological tests were undetected. test-deletion-guard (G31) catches a test DELETED or newly SKIPPED; nothing caught the cheaper move — rewriting an assertion mid-loop so a failing test passes while the production code stays broken. ImpossibleBench measures that behaviour at 50-55% for frontier models on impossible tests, and read-only test paths was the strongest single mitigation reported. This control is the EVIDENCE half and has no substitute in the existing set: G42 and G43 are both arithmetic over its ledger, so without it neither can exist. It is observation-only and never blocks, so a misclassification costs a spurious ledger row rather than a wedged session. It also lands the PostToolUse tap HARNESS.md flags as the real tamper-proofing route for G39.",
      "description": "PostToolUse(Bash) recorder. lib/red-phase.js classifies the command (pytest/vitest/jest/node --test/go test/npm test, with env assignments and delegates like `uv run --frozen`, `npx`, `poetry run` stripped) and the response as pass | fail | no-tests | env-broken, reusing toolchain.js MISSING_SIGNATURES rather than a second copy. node --test carries BOTH reporter shapes: Node >=22 defaults to `spec` (`ℹ fail 1`), not TAP (`# fail 1`) — matching TAP alone made every real run of this repo's own suite classify env-broken, so nothing was ever recorded. That dead control was invisible to hand-written fixtures and is now guarded by test/red-phase-real-runner.test.js, which drives the REAL runner and asserts on what it actually prints. env-broken is the other load-bearing distinction: pytest exiting non-zero because pytest is absent is NOT a red phase. FILTERED runs (-k / --test-name-pattern / --grep) are dropped entirely rather than parsed: the agent chooses which tests execute, so a filtered green must never close a cycle and a filtered red must never arm one — and no count check can catch it, because node reports `pass 1` even for a pattern matching nothing and for a file containing no tests (it counts the file itself). An unfiltered green sweep that names no files is attributed to the files currently failing (openRedFiles), or a cycle opened by a named red run could never be closed by a bare `pytest` and the file would stay locked forever. lib/red-phase-ledger.js appends a hash-chained event to .claude/state/red-phase.jsonl carrying test_files, a sha256 per file, head_sha and task_id; appendRun REFUSES an env-broken verdict, a run naming no files, or a run missing any file hash. The ledger path is registered as protected machinery (trust-boundary.js) alongside its peer task-lifecycle.jsonl. KNOWN LIMITATIONS (disclosed): only files the command or output NAMES are recorded for a red run; `rm <path>` is not recognised as a write by bash-targets.js, so the ledger can still be deleted via the shell — a PRE-EXISTING kernel gap that task-lifecycle.jsonl and the coverage baselines share. PROVENANCE, and its disclosed ceiling: the recorder fires at PostToolUse, AFTER the command, so hashing the file then captures the text as it stands when the HOOK runs, not as the RUNNER read it. pre-bash-gate (already PreToolUse(Bash), so no settings.json change) now snapshots the pre-run hashes; the recorder prefers those and DROPS any file whose text moved during the command, since which version produced the verdict is then unknowable. That closes the case where the file is left changed. It does NOT close write-run-restore-to-identical (`python -c \"...write(WEAK)\" ; pytest t.py ; git checkout t.py`), because pre == post and no before/after tap can see inside a single command. What IS visible is the co-tenancy, so provenanceUnverifiable() refuses to record a test run sharing its command line with inline interpreter code (python/node/perl/ruby -c/-e/-i) or a git restore/checkout/stash. Narrow on purpose: those combinations are vanishingly rare in honest use and a false positive costs one unrecorded run. A determined adversary with arbitrary shell can still defeat this; the control targets the rationalising agent, not the determined one — the same disclosed stance as G39."
    },
    {
      "id": "test-write-lock",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "artifacts",
      "gap_ref": "G42",
      "wired_at": ".claude/hooks/pre-write-gate.js",
      "signal": "a Write/Edit/MultiEdit or a shell write (sed -i/tee/patch) targets a test file whose latest observed run is still failing",
      "net_add_justification": "The harness proved test EXISTENCE but never test ORDERING (see red-phase-record). This is the in-loop BLOCK, and it is the control the source ranks highest by value-to-effort: read-only test paths outperformed strict prompting by ~8 points on realistic reward-hacking benchmarks while preserving legitimate task performance. It is not reducible to test-deletion-guard (G31), which fires at commit and only on deleted/skipped cases, nor to tdd-test-first, which only asks whether a test file exists. It costs zero when no test is red.",
      "description": "Decision logic in lib/test-write-lock.js (pure), wired into BOTH pre-write-gate.js (Edit/Write/MultiEdit) and pre-bash-gate.js via bash-targets.js extractWriteTargets (sed/tee/patch) — a lock covering only the native edit tools is theatre when the agent has a shell. Both hooks relativise the target against realResolve(projectDir): relativising a /private/var path against a /var projectDir yields ../../.., which never matches a test-file pattern, and the lock then passes silently on an equivalent path form. That bug shipped in the first version on each side in turn and is now covered by a round-trip asserting both forms block through both hooks. KEYED ON (file, CONTENT HASH), deliberately not on task_id or the path alone — independent review showed both of those were agent-controlled unlocks: declaring a different task (or none) emptied the ledger and released every lock, and running a file once before adding the failing test marked it green-first forever. Content is the one part of the key an agent cannot restate without actually changing the test. The two narrowings that make the lock usable both survive under content-keying: green-first never arms (a pin-down runs green at text P and Step 4 flips PRODUCTION code, so it is still text P — no lane exemption needed), and the lock closes when the file legitimately goes green (locking for a whole task would block adding test 2 to a file whose test 1 passes). A tampered ledger BLOCKS rather than failing open, and the pre-bash call is guarded so a throw cannot unwind and skip the remaining scope/trust-boundary checks. Bypass HARNESS_TEST_LOCK=off."
    },
    {
      "id": "test-integrity",
      "axis": "behaviour",
      "type": "computational",
      "cadence": "commit",
      "status": "active",
      "scope": "artifacts",
      "gap_ref": "G43",
      "wired_at": ".claude/scripts/test-integrity-gate.js",
      "signal": "a test file whose sha256 differs between the run that made it FAIL and the run that made it PASS",
      "net_add_justification": "The harness proved test EXISTENCE but never test ORDERING (see red-phase-record). test-write-lock (G42) is a turn-level gate, and turn-level gates are steppable — hooks can be disabled, the agent can work outside Claude Code, --no-verify exists. The source names this exact failure mode (#6: a turn gate needs a merge-level backstop with no in-session override). This control is that backstop, and it is arithmetic over a hash-chained ledger rather than a judgement, so it holds in CI where no session state exists.",
      "description": "Pure logic in lib/test-integrity.js; git/fs plumbing in scripts/test-integrity-gate.js; registered in gate-registry.js at order 155 with runsWithoutSource:true and exposed as gates-verification.js#checkTestIntegrity in the verification pack. ONE invariant: a test file must not change between the run that made it RED and the run that made it GREEN. The PAIR carries the proof — no single anchor can, because a tamperer edits and re-runs, after which every single-anchor rule agrees with itself. The anchor is the FIRST red of each cycle, not the last: anchoring on the last red let a weakened test launder itself (strip assertions but stay red, re-run to re-anchor at the weakened text, then fix the one survivor). A consequence, intended: correcting a test you believe is genuinely wrong now SURFACES rather than passing silently, because from the outside correcting and weakening are indistinguishable. NOT scoped by task_id — scoping by it made the gate a self-service unlock. Silent on still-red files (no pair yet), green-first pin-downs, and edits after the pair closed. Emits specs/reviews/test-integrity.json because the ledger is gitignored and a hash chain would conflict across branches. A ledger failing its own integrity check BLOCKS. VACUITY IS EXPLICIT: with no ledger there is nothing to check, so the verdict carries vacuous:true and prints a WARNING rather than a green; --strict turns that into a failure. CI runs NON-STRICT until the PostToolUse recorder is wired in settings.json, because until then there is no ledger to find. Bypass HARNESS_TEST_INTEGRITY_GATE=off. ADDITIONALLY reports a NEVER-RED finding (advisory, non-blocking): a test file newly added by the commit that the ledger never observed failing. Everything else in this control proves 'if a test was red it was not weakened to go green'; nothing proves 'every test was red first', so a tautological test — written to match code that already works and green on its first run — leaves no record and every cycle check is silent. That is failure mode #11 in its most common form, and neither independent review round raised it. mutation-smoke (G7) covers it from the other direction but early-returns on inAutoBuild, so /change, /vibe, /feature and ordinary work have nothing. It is ADVISORY on purpose rather than out of caution: at the ledger level a characterization pin-down is INDISTINGUISHABLE from a tautological test (both are green-only by design — pinning-down-behavior Step 3 runs pins green against unmodified code), so blocking would fail the legacy lane for following its own skill. Surfacing 'this test was never observed failing' is the honest ceiling without a signal that separates intent."
    },
    {
      "id": "adopted-spine-integrity",
      "axis": "traceability",
      "type": "computational",
      "cadence": "planning",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/brd-adopt.js",
      "signal": "a BRD requirement whose id, text or PRD label differs from what brd-adopt.js produced from the immutable source",
      "net_add_justification": "The one property of adoption that no existing gate can check. brd-render is told in plain words to fill `taxonomy` and leave `id`/`text` alone, and its own SKILL admits the consequence if it does not: an adopted spine passes the grounding gate as an IDENTITY, and a re-keyed or re-worded spine that still carries `traces` passes it as COVERAGE — the verdicts are indistinguishable. trace-check owns 'every id links'; this owns 'the ids are still the source's ids'. A live run re-keyed all 24 requirements from FRD-n to FR-n with nothing noticing, leaving two runs of the same pipeline emitting brd-requirements.json in two different id spaces and stranding the milestone-scoped grounding gate downstream.",
      "description": "`brd-adopt.js --verify` re-derives the requirement spine from specs/brd/frd-requirements.json and compares id, text and PRD label against the on-disk brd-requirements.json. The adopter itself is the oracle, so the check cannot drift from what adoption produces. taxonomy and acceptance are deliberately not compared — filling them is the renderer's job and the ten-slot floor gates them separately. Exit 1 = the baseline was modified (dropped, invented, re-keyed or re-worded); exit 2 = nothing to verify against, which is a setup problem and never a pass. Hard block at /brd Step 4.2, --prd/--frd mode only; delta mode passes the same --out-dir it adopted with. The `label` field it checks is what closed the two-id-space join: milestones and acceptance key on the PRD identifier, requirements key on the spine id, and before this the identifier existed only inside free-text `section`."
    },
    {
      "id": "phase-context-handoff",
      "axis": "traceability",
      "type": "computational",
      "cadence": "planning",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/scripts/handoff-check.js",
      "signal": "a planning phase starting inside the same session that approved its predecessor, or a render stretch starting inside the session that shaped its decisions",
      "net_add_justification": "The /clear handoff existed but was advisory, and advisory lost: on a metered run it printed at /brd approval and /spec continued in the same session anyway, running 47 turns at a 273K average context against ~110K for a fresh one. No existing control covers this. plan-approval owns whether a human approved and whether the plan still matches — it says nothing about which conversation the successor starts in, and cannot: the approving session id was not recorded until now. Distinct invariant, deterministic signal, and it fires only at the three phase boundaries rather than after every task.",
      "description": "Two checkpoints, one engine. (1) --stage boundary: hard block at Step 0 of /spec, /design and /test, refusing to start when the predecessor's approval receipt names the live session; plan-approval.js record stamps session_id into the receipt. (2) --stage render: hard block at /spec Step 6 and /design Step 0.9 §3, refusing to run the render stretch when the phase's decisions verdict names the live session — once the decisions gate passes, the renderer and every gate after it read the decisions file rather than the shaping dialogue, and on a metered run that stretch was 40 of /spec's 47 turns at a 284K average context against ~110K fresh. Both phases re-enter via --render-only (/spec had it; /design gained it with this control). The stamp records who SHAPED the decisions, carried forward by a sha256 of the decisions file: the renderers re-run their own gate at Step 0, and restamping there would block the very session the clear just created. The live id comes from the run receipts record-run.js already writes (hook payloads carry session_id, Bash does not). Every uncertain case passes — no record, no session id, a record written before stamping existed — because a control that fires on missing evidence reports a context problem for another control's failure and trains operators to bypass it. Two exemptions ride on the record rather than on a flag the caller must repeat: in_session (/build, which cannot clear itself mid-run) and waived_by (a headless lane with no human to clear)."
    },
    {
      "id": "planning-gate-receipts",
      "axis": "traceability",
      "type": "computational",
      "cadence": "session",
      "status": "active",
      "scope": "artifacts",
      "wired_at": ".claude/hooks/lib/gate-receipt-sensor.js",
      "signal": "a lane that owns planning gates ended a session with a phase whose artifacts exist but whose review recorded no approving receipt",
      "net_add_justification": "R7 asked for the planning gates to be enforced by a hook or deleted. Enforcement is not available: hooks fire on tool calls and there is no phase-transition event to gate, and plan-approval#phaseRan tests directory EXISTENCE rather than authorship — so in any repo that has run /build once, specs/brd|stories|design stay populated and a hook keyed on them would block every later /feature, /change, /vibe and /refactor, lanes that reach /auto writing no BRD at all. What was actually missing on the audited run was not enforcement but VISIBILITY: two of three gates left no receipt and nobody could tell. plan-approval detects a bad receipt when something asks it; this detects that nothing asked. Distinct signal, and the only one that fires when the gate is skipped rather than failed.",
      "description": "Stop-cadence sensor, reports and never blocks. For lanes that own planning gates (the lanes record-run writes: gated/auto/autonomous/lite* -> brd,spec,design,test; sprint -> brd,spec,design; finalize is not applicable) it reports any phase whose artifacts exist but whose receipt is absent or still changes-requested. A waiver is NOT a finding — a deliberately headless run and a gate nobody ran are different facts. A phase that never ran is not a finding either; it was not reached. Rides the existing review-on-stop Stop hook rather than adding one, so it needs no settings.json change."
    }
  ],
  "drift_sensors_planned": {
    "gap_ref": "G2",
    "note": "The 'drift' cadence is the article's third column (repeatedly, slower, outside the change lifecycle). The architecture / dead-code / dependency-CVE signals are now SHIPPED via drift-report.js (see the drift-* sensors above). The runtime-SLO signal is now covered by the 'runtime-slo' integration sensor (slo-check.js): it ships as an integration-cadence sensor wired into /evaluate Step P4, and is also runnable standalone on a drift cadence (npm run slo -- --url <live>). No drift signals remain planned.",
    "shipped": [
      "drift-architecture",
      "drift-dead-code",
      "drift-deps",
      "drift-design-code"
    ],
    "still_planned": [],
    "existing_drift_like": [
      {
        "id": "upstream-watch",
        "wired_at": ".claude/scripts/upstream-watch.js",
        "note": "Watches the harness's OWN upstream (anthropics/claude-code), not the product codebase."
      }
    ]
  }
}
