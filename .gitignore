.opencode/scheduled_tasks.lock
.opencode/tdd-guard/
node_modules/
dist/
# Generated SKU trees (npm run package:skus); source of truth remains .opencode/ + harness-lite/
dist/skus/
# specs/ is pipeline runtime output, not part of the base scaffold — /scaffold only
# creates empty subfolders; content is produced by running the pipeline against a
# project. This repo dogfoods /code-map + /brownfield against itself (Project Zero)
# but that output is local/regenerable, not shipped scaffold content. Exceptions
# that DO ship (same PR as the code they document, refreshed by the graph-refresh
# hook): the committed DeepWiki (its wiki subdirectory) and the single-file
# interactive graph-explorer.html + wiki-browser.html — see code-map/SKILL.md and
# feature-wiki-committed-contract.test.js.
/specs/*
!/specs/brownfield/
/specs/brownfield/*
!/specs/brownfield/wiki/
!/specs/brownfield/graph-explorer.html
!/specs/brownfield/wiki-browser.html
# Local toolchain installs (gitleaks etc.) — CI installs its own copies
.tools/
build/
packages/harness-core/
packages/harness-lite/
packages/harness-full/
!.opencode/skills/build/
.next/
.venv/
venv/
__pycache__/
*.pyc
.DS_Store
.env
.env.local
.env.*.local
.symphony/
symphony_clone/.symphony/
symphony_clone/workspaces/
symphony_clone/logs/
*.jsonl
# .opencode/state/ is a tracked harness snapshot (learned rules, failures, iteration
# log, coverage baseline). Only runtime churn stays out: telemetry-ledger.jsonl and
# the live pending-reviews.jsonl are already covered by *.jsonl above; also exclude
# logs, log offsets, transient counters, and archived state.
.opencode/state/archive/
.opencode/state/*.log
.opencode/state/*.offset
.opencode/state/*.lock
.opencode/state/review-block-count
.opencode/state/auto-continue-count
.opencode/state/auto-continue-progress
.opencode/state/inflight-agents.json
# Local regenerate receipt for docs/CODEBASE.md (product is the md file)
.opencode/state/human-codebase.json
# Navigation indexes (regenerated by graph-refresh / nav-query refresh)
.opencode/state/nav-index/
.opencode/state/nav-graph-index.json
.opencode/state/context-pack-last.json
.opencode/state/nav-telemetry-summary.json
# Token-governor compact-runner caches (run-compact.js) — transient
.opencode/state/context-cache/
.opencode/state/tool-output/
!.opencode/templates/state-seeds/pending-reviews.jsonl
test/e2e/results/*
!test/e2e/results/.gitkeep
test/e2e/output/
.playwright-mcp/
# e2e self-test harness live-run output (smoke/plan/auto/semi-auto generate these)
test/e2e/smoke-output/
test/e2e/plan-output/
test/e2e/auto-output/
test/e2e/semi-auto-output/
test/e2e/full-auto-output/
test/e2e/feature-output/
test/e2e/gated-build-output/
test/e2e/vibe-output/
test/e2e/brownfield-run-output/
test/e2e/screenshots/
# Runtime output: review/eval agents (clean-code, security, diff) write reports + verdict
# JSON here each run; the pre-commit gate reads them. Regenerated every time, never shipped.
**/specs/reviews/
# Superpowers SDD execution scratch (task briefs/reports, progress, review diffs)
# generated while implementing a plan. The durable design lives in docs/archive/superpowers/.
.superpowers/

# Bite ledger: per-machine sensor outcomes, regenerated continuously.
.opencode/state/sensor-outcomes.jsonl
